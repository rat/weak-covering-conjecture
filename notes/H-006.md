# H-006: Wirsching's 2003 "Elka function" route to predecessor density

Status: backlog, low-medium. Full narrative moved here 2026-07-31 from `HYPOTHESES.md`'s row,
which had grown into an unreadable wall of text (Rule 7/14). This file is the detail note; the
`HYPOTHESES.md` row now carries only current status and a pointer here.

## What the route is

L-002 (Wirsching 2003, DCDS 9(3)) does NOT extend or use the Weak Covering Conjecture / `R_{j,k}`
(Chapter V) at all, contrary to this project's original assumption. It pursues positive
predecessor density via a completely different apparatus: Elka functions `e_l(k,a)` counting
Collatz-graph paths, Haar-measure averaging over 3-adic integers, a limiting Markov chain with
invariant density `phi`, and three new conjectures.

**Deep tractability scoping (2026-07-29)**: the whole Positive-Density proof reduces to exactly
three conjectures (Pos.Density <= (1) <= [Conj1] (2) <= (3) <= [Conj2] (4) <= [sec7] (5) =
[Conj3]). **Conjecture 3 is the load-bearing analytic gap and it does not live in Collatz**: it is
the near-0 asymptotics of the invariant density `phi`, a two-scale/refinement functional-equation
eigenfunction. Conjectures 1 and 2 are translation/uniformity-near-singularity gaps specific to
the construction, not secretly resolved and not finitely checkable. None of the three is WCC's
`S(t)`/rank-coupling obstruction in disguise: this is a genuinely independent hardness, not a
rephrasing.

**Recommendation, unchanged since 2026-07-29**: stays backlog, low-medium. Rule 1/comparative
advantage: the route uses none of this project's `R_{j,k}`/jstar-fast apparatus, and its core is
Berg-Kruppel's/Rvachev's home turf, not this project's. Rule 10: the realistic near-term
deliverable would be thin or high-risk next to the WCC `j*(l)` data line already in hand.

**Bridge to H-002 (Rule 8e, 2026-07-29)**: the Elka route is the equidistribution *strengthening*
of WCC covering (`e_l(k,a)>0` everywhere = covering; `e_l(k,a)>=mu*average` = the Elka route), and
its Haar average `ebar_l(k)=C(k+l,k)/(2*3^(l-1))` is exactly H-002's expected-count denominator.
Conjecture 1's pointwise-vs-average gap is the same shape as H-002's `N_min` gap, making
`phi`/Berg-Kruppel a candidate source of the fluctuation control H-002 lacks. Thematic, not a
formal reduction; checked 2026-07-31 (see below), no leverage gained.

## Conjecture 3, precisely

`phi` is the unique fixed point of `W3 phi(x) = (3/2) integral_{3x-2}^{3x} phi`: `supp phi =
[0,1]`, `integral phi = 1`, `C^infinity`, polynomial on each interval outside a Cantor set, and
satisfies `(2/3) phi'(t) = 3(phi(3t) - phi(3t-2))`, i.e. the `a=3, lambda=2/3` case of `lambda
phi'(t) = a(phi(at) - phi(at-a+1))`.

Conjecture 3 states `phi(z_l)/phi_0(z_l) -> c > 0` uniformly for `(z_l)` in a class corresponding
to `z_l ~ l*3^{-l}` (the "borderline speed" toward 0 that makes the section-7 quotient tend to a
positive limit), where `phi_0` is the explicit truncated-equation asymptotic solution (Berg-
Kruppel, eq. 9.6): `phi_0(t) = ((2*beta)^epsilon/sqrt(2*pi)) * exp(gamma*ln(t) + delta*ln(-ln t) -
beta*ln^2(t/(-ln t)))`.

## Primary-source verification chain (all read in full, not title-matched)

1. **Berg & Kruppel, "On the Solution of an Integral-Functional Equation with a Parameter," Z.
   Anal. Anwendungen 17(1) (1998), 159-181 (L-097)**: the actual primary source of Conjecture 3's
   premise. Section 9, Proposition 9.1, eq. (9.6) gives `g_0(t)`'s asymptotic. The authors
   themselves state only that they "expect" `phi ~ g_0` up to a bounded, bounded-away-from-zero
   factor — an informal expectation, never even formally conjectured with a proof attempt, and
   explicitly weaker than Wirsching's actual Conjecture 3.
2. **Berg & Kruppel, "Cantor Sets and Integral-Functional Equations," Z. Anal. Anwendungen 17(4)
   (1998), 997-1020 (L-096)**: sequel, gives exact polynomial formulas for `phi` on the Cantor-
   complement, no asymptotic content.
3. **Berg & Kruppel, "Series Expansions...," Results in Math. 41 (2002), 213-228 (L-098)**: purely
   series-expansion/spline content, no asymptotic-as-t-to-0 analysis.
4. **Volk, "Evaluation of the solution of an integral-functional equation," J. Comp. Appl. Math.
   174 (2005), 423-436 (L-099)**: purely computational (Taylor/Horner evaluation algorithm),
   exclusively for `a=2`, not `a=3`. Author corrected from an earlier misattribution to Berg-
   Kruppel (Rule 11 note: the error came from relying on an indirect summary, fixed once the PDF
   was read).

**Net: Conjecture 3 is a genuine, still-open gap, never claimed closed even by its own
originating paper.** This is a primary-source-verified negative, not a search-coverage gap.

## The "analogous framings" search (2026-07-31): phi = h_3, and the ensuing literature exhaustion

At the researcher's request to search disguised/analogous versions of this project's objects
under unrelated names (see `notes/H-003.md` for the full three-object search), Codex identified
that `phi` is not merely analogous to but literally (up to translation) Rvachev's classical
"atomic function" `h_3`: `phi(x) = h_3(x-1/2)`. **Independently verified** by direct symbolic
substitution into the already-established functional equation (`psi(y):=phi(y+1/2)` satisfies
exactly Rvachev's equation at `a=3`), not taken on faith.

This triggered a full read of the entire published Kabaya/Iri/Moriguti/Rvachev/Derfel line
connected to this identity, described below. **All of it is now read, not sampled.**

- **Kabaya & Iri, "Sum of Uniformly Distributed Random Variables and a Family of Nonanalytic
  C^infinity-Functions," Japan J. Appl. Math. 4 (1987), 1-22 (L-105)**: read in full (22pp).
  Confirms `phi` is exactly this paper's `alpha=2/3` family member (a second, independent
  parametrization matching the `h_3` identity: `X_n = alpha*[U_n+(1-alpha)U_{n-1}+...]`, matching
  via `1-alpha=1/3`). Proves existence/smoothness and Cantor-set nonanalyticity (Theorems 1-2):
  for `alpha>1/2` (our case), the density is polynomial on each Cantor-complement interval,
  independently cross-checking L-096's finding via a wholly different 1987 derivation.
  **Contains no boundary/near-0 asymptotic result of any kind.**
- **Kabaya-Imai & Iri, "On Operators Defining a Family of Nonanalytic C^infinity-Functions,"
  Japan J. Appl. Math. 5 (1988), 333-365 (L-108)**: read in full (33pp), the 1987 paper's own
  sequel. Full spectral theory of the operator `Phi` (eigenvalues `(1-alpha)^n`, eigenvectors
  `p_infinity^(n)` = derivatives of `phi`, dual operator `Phi*` with orthogonal polynomial
  eigenvectors `q_n`). **No near-0 boundary asymptotic for `phi` itself.** Section 7.2 records an
  unproven numerical conjecture that `q_n(x)` converges to `cos/sin(pi*x/alpha)` as `n ->
  infinity` — resolved by the next paper.
- **Moriguti, Iri & Kabaya-Imai, "On Asymptotic Properties of the Eigenfunctions of a Linear
  Operator," Japan J. Appl. Math. 7 (1990), 203-229 (L-109)**: given the title, this was the
  round's top candidate to close Conjecture 3 outright. Read in full (27pp). **Confirmed real,
  rigorous theorem (generating-function/Jacobi-theta technique), confirmed NOT applicable**: it
  proves `q_n(x) -> sin/cos(pi*x/alpha)` as `n -> infinity` (the DUAL family's DEGREE INDEX, for
  fixed `x`), linear rate `max{2^-n, (1-alpha)^n}`. This is an index asymptotic, not the spatial
  (`x -> 0`) asymptotic Conjecture 3 needs — the same mismatch pattern as L-106 (Brysina-
  Makarichev): a title promising "asymptotic properties of eigenfunctions" means convergence in
  the family/degree index, not decay near a boundary point.
- **Derfel, "Functional-Differential and Functional Equations with Rescaling," Operator Theory:
  Advances and Applications 80 (1995), 100-111 (L-107)**: read in full (12pp). **Major
  contextualization finding**: Theorem 1 (from the author's 1977 PhD thesis) proves that
  `exp(-gamma*ln^2(1+|t|))` (matching Berg-Kruppel's `g_0` form exactly) is the GENERIC asymptotic
  rate for the entire class of rescaled functional-differential equations (the "Kato-McLeod"
  theory, after Kato & McLeod, Bull. AMS 77 (1971), 891-937), not something ad hoc to Wirsching's
  equation. Theorem 2 shows the bound is essentially sharp for the pure-dilation sub-case. Section
  3 gives a general existence theorem (ref. [Der6]) for compactly-supported solutions of exactly
  `phi`'s equation type, independently confirming the `h_3` identity from a third tradition. This
  survey's own bibliography is what led to finding L-109.
- **Rvachev, "Compactly Supported Solutions of Functional-Differential Equations and Their
  Applications," Russian Math. Surveys 45:1 (1990), 87-120 (L-111)**: the comprehensive,
  authoritative 1990 survey of the entire Soviet/Ukrainian atomic-function tradition. Read in full
  (34pp). **Confirmed: entirely scoped to `a=2` (`up(x)`) and to approximation-theoretic questions
  (generalized Taylor series, spline/trigonometric approximation, interpolation-node theorems).
  No near-0/boundary asymptotic question appears anywhere, including in the paper's own explicit
  "unsolved problems" section.** The field's own most authoritative stocktaking did not consider
  Conjecture-3-type asymptotics a live question, reinforcing from the Soviet/approximation-theory
  side what Berg-Kruppel's papers already showed from the German side.
- **Brysina & Makarichev, "On the asymptotics of the generalized Fup-functions," Adv. Pure Appl.
  Math. 5 (2014), 131-138 (L-106)**: abstract-level read only (paywalled). Same index-vs-spatial
  mismatch: "asymptotics" here means convergence of a family index `n -> infinity`, not near-0
  decay of a fixed function.

**Self-decomposable/stable-law techniques ruled out definitively (not just deprioritized)**:
`phi`'s characteristic function has zeros (sinc-product form `prod_r sin(xi/3^r)/(xi/3^r)`), but
every infinitely divisible or self-decomposable characteristic function is nowhere-zero by the
Levy-Khintchine representation. ~0% confidence, confirmed via the zero-set argument alone.

**A separate, unproven, checkable mechanism for the ratio question (not yet tested)**: taking logs
of `phi`'s Laplace transform gives a base-3 harmonic sum (Flajolet-Gourdon-Dumas sense); such sums
generically produce a log-periodic correction under Mellin analysis (de Bruijn 1948 precedent),
explaining why Berg-Kruppel only claim boundedness. Codex derived that the specific sequence
`t_l ~ l*3^{-l}` may "phase-lock" this multiplier (saddle-point inversion puts `log_3(s(t_l))` at
a fixed phase as `l -> infinity` along that sequence). Cheapest test if picked up: compute
`phi(t_l)/g_0(t_l)` numerically at several `l`. **Attempted and deferred, not run**: this is not
actually cheap — it needs either Berg-Kruppel's exact eq. (9.6) constants (not yet extracted with
sufficient precision) or a from-scratch high-precision evaluator for `phi` near `t=0`, the exact
numerically delicate problem Volk's whole paper (L-099) solved only for `a=2`. Would need its own
properly scoped experiment.

## Net status (2026-07-31)

The entire published operator-theoretic and approximation-theoretic literature connected to
`phi`'s functional equation (Kabaya/Iri/Moriguti 1987-1990, Rvachev's own 1990 survey, Derfel's
1995 survey, Berg-Kruppel/Volk 1998-2005, Brysina-Makarichev 2014) has now been read, not sampled.
None of it supplies Conjecture 3's near-0 ratio-convergence statement for general `a`. This is a
primary-source-verified negative across two independent research traditions (German
functional-equation school and Soviet/Ukrainian atomic-function school), not a coverage gap.

**2026-07-31, both next steps attempted, both real negatives**:

1. **Kato & McLeod (1971) primary source: inaccessible from this environment.** Tried four
   sources (Project Euclid, direct DTIC technical-report mirror, SciSpace, Cambridge Core);
   all blocked by bot-protection, a dead service, or a 403, across both `WebFetch` and direct
   `curl` with a browser user agent. Not a content problem, a network-access problem; would
   need the researcher's own institutional access or a different network path.

2. **Numerical phase-locking check (`experiments/E-006-phi-asymptotic/`): attempted properly,
   genuinely fails, not just slow.** Built `phi(t)` via Fourier inversion of the exact
   characteristic function (`phihat(xi) = exp(i xi/2) * prod_r sin(xi/3^r)/(xi/3^r)`) using
   `mpmath` arbitrary precision. **Validated correctly** against a real, checkable fact
   independent of the solver (`phi(0.3) = phi(0.7)`, since `X` is symmetric about 1/2) — the
   implementation is conceptually right. But for `t_l` with `l >= 8`, the oscillatory integral
   does not converge under `mp.quad`: doubling the cutoff changes the answer by orders of
   magnitude and even flips its sign, which is impossible for a density. `mpmath.quadosc`
   (its dedicated oscillatory-integral routine) was tried as a second method and also gave a
   result inconsistent with the expected order of magnitude. **This is a genuine numerical
   failure of generic quadrature at this task, confirming rather than merely repeating the
   earlier assessment**: reliably evaluating `phi` near its boundary needs the exact rational
   polynomial-piece construction (Kabaya-Iri Theorem 2/Lemma 4, or equivalently Berg-Kruppel's
   Cantor-complement polynomials), the same kind of dedicated numerical method Volk's whole
   paper (L-099) built for the simpler `a=2` case, not generic high-precision quadrature. Full
   detail, including the failure diagnostics, in `experiments/E-006-phi-asymptotic/README.md`.

**If picked up again**: the numerical check needs the exact polynomial-piece recursion
implemented with `fractions.Fraction` (exact rational arithmetic, sidesteps the convergence
failure entirely), not another attempt at numerical quadrature. The Kato-McLeod primary source
needs either institutional access or the researcher fetching it directly.

Chamberland's "An Update on the 3x+1 Problem" survey remains unreachable from this environment
(`ECONNREFUSED` from both known mirrors, same IP — a server/network-level block, not transient).

**2026-07-31, same day, superseding item 2 above: a working numerical evaluator, via a
different technique.** As round 2 of a structured 4-round Codex consultation (prompts/responses
at `/home/rat/.claude/jobs/a8d2d60e/tmp/codex_stuck_round{1,2}_{prompt,out}.txt`), Codex proposed
a real-variable saddlepoint evaluator instead of Fourier inversion: solve the monotone real
saddle equation `M(s) = s*t` (`M(s) := -s*K'(s) = sum_r m(s/3^r)`, `m(x) = 1-2x/(e^{2x}-1)`,
`K := log` of the Laplace transform `L(s) = prod_r (1-e^{-2s/3^r})/(2s/3^r)`), then evaluate
`phi(t) ~ s/sqrt(2*pi*V(s)) * exp(K(s)+s*t)` at the solved `s` (`V(s) := s^2*K''(s) = sum_r
v(s/3^r)`, `v(x) = 1-(x/sinh x)^2`). No oscillation anywhere: every quantity is a sum of a
smooth, rapidly-decaying real function over `r=1,2,3,...`.

**Implemented and validated (`experiments/E-006-phi-asymptotic/saddlepoint.py`)**: (a) `L(0)=1`
holds exactly; (b) at moderate `t` (where the failed Fourier method still worked before breaking
down), the two independent methods agree to within the saddlepoint approximation's own expected
error (`t=0.3`: Fourier gives `1.4867` (trusted), saddlepoint gives `1.5093`, ~1.5% off, exactly
what a genuine asymptotic approximation at a small effective scale should look like); (c) swept
`t_l = l*3^{-l}` for `l=3..40` with **zero failures of any kind**: `ln(phi(t_l))` decreases
smoothly and monotonically throughout (from `-0.98` at `l=3` to `-804` at `l=40`), no sign flips,
no cutoff-dependence -- the polar opposite of the Fourier attempt's behavior, and reaching `l=40`
(`t_l ~ 3.3e-18`), far beyond anything the Fourier method could reach even in principle.

**This does not yet test Conjecture 3 itself**: it gives a validated, internally-consistent
asymptotic for `phi(t_l)`, not (yet) a comparison against Berg-Kruppel's specific `phi_0`
normalization, which is what the actual ratio-convergence statement needs. Codex also supplied
explicit formulas for the discrete-scale correction series `A(z)`, `C(z)`, `D(z)` and derived the
phase-locking mechanism cleanly (`z_l := s_l/3^l = 1 + A(1)/l + O(l^-2)`, i.e. the saddle point's
scaled position provably approaches `z=1` along `t_l = l*3^{-l}` specifically, with a stated
diagnostic for detecting a missed periodic prefactor: compare against `t_{l,c} = c*l*3^{-l}` for
several fixed `c`, which should sample different phases `z_l -> 1/c`). Concrete numbers now fed
back to Codex (round 3) to identify the precise next comparison against Wirsching/Berg-Kruppel's
actual normalization.

**Also resolved in round 2**: Codex's own honest re-examination confirmed the GAP A Frobenius-
mod-3 result (see `notes/H-003.md`) does not lift to a magnitude bound on `S(t)`, with a concrete
counterexample (`1+zeta` is a 3-adic unit yet arbitrarily small at some complex embeddings) showing
why no general 3-adic-to-complex transfer exists here. Confirms this project's own earlier
assessment; not pursued further on this specific sub-question.

### Round 3-4: Conjecture 3 tested directly, strong (not conclusive) numerical support found

**Round 3** asked Codex to derive `gamma`, `delta`, `beta` (the exponents in `phi_0(t) ~
t^gamma*(-ln t)^delta*exp(-beta*ln^2(t/(-ln t)))`) directly from the same saddlepoint expansion,
rather than needing Berg-Kruppel's inaccessible primary source at all. Result: `beta =
1/(2*ln 3) = 0.45511961...`, matching this session's own independent hand-derived estimate
(de Bruijn/Mahler-partition balance argument) exactly -- a genuine cross-check, not a
coincidence. `gamma = -1.8649154949`, `delta = 0.4546762683`, both derived in closed form.

**Implemented and ran the actual test** (`experiments/E-006-phi-asymptotic/conjecture3_test.py`):
computed `R(l) := phi(t_l)/phi_0(t_l)` for `t_l = l*3^{-l}`, `l=5..500`, using the validated
saddlepoint evaluator for `phi` and the freshly-derived exponents for `phi_0` (no external
source needed at all once the exponents were re-derived). **`R(l)` decreases from `0.20996`
(`l=5`) to a minimum `0.18272` near `l=19-20`, then increases steadily to `0.19953` at `l=500`**,
with no clear leveling off in the directly-computed range (root-finding became unreliable past
`l=500`, an implementation limit, not a mathematical one).

**Round 4** fed this exact data back, asking for an honest final read rather than a confident
guess. Codex proposed the leading omitted correction is a quadratic-in-`log L` polynomial
divided by `L` (not a single term), fit it on only `l in {20,40,60,80}`, and predicted: the
dip-then-rise shape is exactly what this structure produces (turning point at `L~18.2`); an
out-of-sample prediction `R_500 ~ 0.19951` (actual `0.19953`); a limiting constant `C ~ 0.2050`.

**Independently re-verified, not taken on faith** (`experiments/E-006-phi-asymptotic/
fit_check.py`): refit the identical model on this project's own data. Confirmed both claims:
out-of-sample `R_500` prediction error `0.0127%` (Codex claimed `~0.014%`, same order), and the
turning point (re-derived correctly via the chain rule, catching and fixing an initial naive-
derivative mistake of my own along the way) at `L=18.19`, matching the observed minimum
(`L_19=17.93`, `L_20=18.98`) almost exactly.

**Net assessment, matching Codex's own honest framing**: this is real, independently-verified,
strong numerical evidence THAT Conjecture 3's ratio-convergence claim holds, with a specific
candidate limit `C ~ 0.205` -- not a proof. No finite numerical range can rule out an arbitrarily
slow divergence engineered to mimic convergence through `l=500`. What would actually settle it:
an analytic next-order saddlepoint expansion with a controlled remainder term
(`O((log L)^4/L^2)`), not attempted. This meaningfully upgrades H-006's standing evidence from
"genuinely open, no evidence either way" to "genuinely open, but with real, cross-validated
numerical support for the conjecture being true" -- still backlog (Rule 1/10: this project's own
apparatus plays no role here, and a rigorous proof of the remainder term is a substantial,
separate undertaking), but no longer merely an unexamined literature gap.

### A second consultation cycle (2026-07-31, up to 10 rounds), round 1: a candidate rigorous proof sketch

At the researcher's request, a fresh, longer (up to 10-round) "where are we stuck" Codex
consultation was started (`/home/rat/.claude/jobs/a8d2d60e/tmp/codex_stuck2_round1_{prompt,out}.txt`),
covering GAP A, GAP B, and H-006 together, given the full state established above.

**Codex proposed what could be a genuine rigorous proof of Conjecture 3**, not just a fit.
Starting from the exact identity `K(3s)-K(s)=f(s)` (trivial from `K(s):=sum_r f(s/3^r)`, verified
by hand), it derives a log-periodic decomposition `K(e^x) = -x^2/(2h) + bx + rho(x) + O(...)`,
`h:=ln3`, `b:=1/2-ln(2)/h`, with `rho` exactly `h`-periodic and explicit non-vanishing Fourier
coefficients `mu_hat(k) = -(1/h)*2^{i*omega_k}*Gamma(1-i*omega_k)*zeta(1-i*omega_k)` (nonconstancy
following from `zeta` not vanishing on `Re=1`, explaining why the correction is small but real).
From this, a detailed asymptotic expansion for `log phi(t_l)` through order `1/l` is derived
(eq. 4 in the transcript), together with an argument for a genuinely controlled remainder (via
exponential tilting: the tilted characteristic-function ratio for each uniform factor is bounded
uniformly, giving the needed local-CLT-style control, referencing Daniels 1954 and
Chaganty-Sethuraman 1993 as the proof template).

**Independently checked the one piece checkable without deep symbolic work**: the leading
(`l^2`) and linear (`l^1`) coefficients of this new, more detailed formula, evaluated at `eta=0`
(matching our `t_l = l*3^{-l}` normalization exactly), against the ALREADY-VALIDATED round-3
boxed formula from the earlier 4-round cycle (cross-checked numerically against real computed
values up to `l=500`). **Exact match** (`coef of l^1`: `1.9547712524422194` vs
`1.9547712524422192`, floating-point-identical). This is a real, substantive consistency check,
not just Codex asserting agreement.

**Not yet checked**: the full `1/l` correction term (the actually new content), and whether
Codex's `C` (the ratio's limiting constant) can be matched to Wirsching's literal `phi_0`
without the exact eq.(9.6) constants (Codex itself flagged this as the remaining gap: "I cannot
honestly identify the exact constant C ... because I do not have Wirsching's one-line formula for
phi_0"). Codex's own confidence: 0.85 that this proves Conjecture 3, conditional on `phi_0` being
the standard non-periodic leading saddle expression already recorded in this project (L-002.md's
citation of Berg-Kruppel eq. 9.6). Queued for round 2: supply this project's own already-derived
`gamma, delta, beta` (from the first cycle's round 3, itself cross-validated) and ask Codex to
complete the matching calculation using its own machinery.

**GAP A, same round**: a new, more sophisticated proposal (matrix-valued Riesz product / transfer
operator over the character tree mod `3^k`, seeking a pressure gap rather than a pointwise
non-vanishing statement) explicitly designed to survive the p-adic-to-complex obstruction that
closed the Frobenius route. Codex's own honest confidence: 0.6 useful discriminator, only 0.15
that it proves the covering bound. Not yet tested; would need real transfer-operator computation
on our end. See `notes/H-003.md` for the corresponding GAP A record.

### Round 2: an independently-derived theoretical value for C, matching the fitted value to ~0.02%

Supplied Codex with this project's own already-derived `gamma, delta, beta` (from the first
cycle's round 3) and asked it to (a) confirm these fall out of its own round-1 formula too, and
(b) complete the matching calculation for the limiting constant `C`.

**(a) Confirmed exactly**: Codex's own formula reproduces `beta=1/(2h)=0.4551196133134188`,
`gamma=-1.8649154949311382`, `delta=0.4546762683043006` (`h:=ln3`), identical to the earlier
independent derivation to all displayed digits.

**(b) A full theoretical value for C, derived from real asymptotic analysis, not just fitted**:
Codex gave an explicit formula for the periodic correction's constant term,
`c0 = -h/12 - (pi^2/12 - gamma_E^2/2 - gamma_1)/h` (`gamma_E` = Euler-Mascheroni constant,
`gamma_1` = the first Stieltjes constant), and `mathcal_A = (1/sqrt(2*pi*h)) *
exp(c0 - c^2/(2h) - c/2)` (`c := ln(2/(3h))`), giving `mathcal_A = 0.205029557059207`, and,
folding in the periodic phase's limiting value along `t_l=l*3^{-l}` specifically
(`rho(ln2-h) = -0.000204121904398`), a final predicted limit
**`C_bare = 0.204987710306627`** (relative to the "bare exponential" `phi_0` convention, i.e. the
one this project has actually been using in `conjecture3_test.py`).

**Independently verified, not taken on faith** (`experiments/E-006-phi-asymptotic/
verify_round2_constant.py`): plugging the STANDARD, independently-known values of `pi`, the
Euler-Mascheroni constant, and the first Stieltjes constant into Codex's stated formula
reproduces `mathcal_A` and `C_bare` exactly (to all displayed digits) -- the arithmetic is
self-consistent, not a made-up number. (This checks the arithmetic, not the underlying
Mellin/Perron complex-analysis derivation of the formula itself, which was not independently
re-derived here.)

**The comparison that matters**: this THEORETICAL prediction, `C_bare = 0.204988`, sits within
**0.0164%** of the value this project independently FITTED from real numerical data earlier this
session (`experiments/E-006-phi-asymptotic/fit_check.py`, `C = 0.204954`, fitted purely from
computed `phi(t_l)/phi_0(t_l)` values with no input from Codex's theoretical derivation at all).
Two independent routes -- a real asymptotic/complex-analysis derivation, and a numerical fit to
actual computed data -- landing within 0.02% of each other is strong, substantive evidence that
both are correct, not a coincidence.

**Net assessment**: this is now materially stronger support for Conjecture 3 than the earlier
numerical-only evidence. Still not a complete, checked proof (the underlying Mellin/Perron
contour argument producing `c0`'s formula has not been independently re-derived, and the
controlled-remainder argument from round 1 has not been fully verified either), but the
quantitative agreement between an independent theoretical prediction and independent numerical
measurement is about as strong as informal cross-validation gets without a full written proof.
Codex also flagged a second convention (folding a derived `epsilon=7.077915706553678` into the
literal Berg-Kruppel-style prefactor form) under which the limit becomes `~0.9998`, essentially 1
-- a convention-dependent restatement of the same result, not a different finding.

### Round 3: the actual Mellin derivation, checked, plus a real self-caught error in round 2

Asked Codex to write out the actual complex-analysis derivation of `c0` (not just its numerical
value), since checking arithmetic alone (round 2) doesn't verify the underlying mathematics. The
first attempt at this round was killed mid-run by the harness before finishing; retried
successfully.

**Codex identified `K(s) = sum_r f(s/3^r)`, `f(x) = -log(1-e^{-1/x})`, as exactly the a=3 case of
the classical Mahler-partition kernel** (`K(s) = log prod_r 1/(1-e^{-3^r/s})`), citing de Bruijn's
1948 "On Mahler's partition problem" and the Flajolet-Gourdon-Dumas Mellin-harmonic-sums survey
(explicitly flagging and correctly citing a known corrigendum to that paper, relevant to exactly
the `f(1/x)` sign convention used here -- a level of citation care worth noting). Derived
`Mellin(f)(z) = Gamma(-z)*zeta(1-z)`, then, via the Laurent expansion of `Gamma(w)*zeta(1+w)` at
`w=0`:

    Gamma(w)*zeta(1+w) = 1/w^2 + A + O(w),   A := pi^2/12 - gamma_E^2/2 - gamma_1

(no `1/w` term -- the claimed cancellation), giving `c0 = -h/12 - A/h` exactly as used in round 2.

**Independently verified the actual mathematical content this time, not just the arithmetic**
(`experiments/E-006-phi-asymptotic/verify_round3_laurent.py`): computed `Gamma(w)*zeta(1+w) -
1/w^2` directly via `mpmath` for `w` from `0.1` down to `1e-7`, confirming clean, monotone
convergence to the claimed constant `A = 0.728693917...` (`0.695232` at `w=0.1` -> `0.728694` at
`w=1e-7`, matching to 6 significant digits at that scale, exactly the expected `O(w)` residual).
An attempt at `w=1e-20` produced a numerical-precision artifact (mpmath's `zeta` needs more
working digits than `dps=50` that close to its pole), not a real discrepancy -- noted honestly,
not hidden.

**Codex also self-caught a genuine issue in its own round-2 answer**: flagged that round 2's
statement of where `c0` enters `K(s)`'s expansion may have a sign convention inconsistency
("If the round-2 notes wrote `K=...+c0`, that line has a sign error. If they defined `c0` as the
Laurent coefficient, as above, everything is consistent") -- self-critical, not defensive, and
gave an independent numerical sign-check recipe (`(1/h)*integral_{Nh}^{(N+1)h}[K(e^L) -
L^2/(2h) - L/2] dL -> -c0`) rather than just asserting the fix is right. This specific numerical
check was not run by us this round; flagged for follow-up if pursued further.

**Codex's own honest final status, verbatim in spirit**: "I do NOT yet call Conjecture 3 'proved
modulo writing up known techniques.' The remaining substantive risk is the bare-to-actual
comparison: a uniform controlled remainder or a genuine pressure gap excluding correlated/overlap
configurations. The 0.0164% gap between C_bare and the existing fit is encouraging, but it is not
negligible relative to a proof claim until its finite-size error is bounded." Also explicitly
declined to claim external publication of the exact `gamma_1` combination ("I have not
independently verified a published source that prints this exact combination in this exact
normalization").

**Net assessment**: the Mellin/Mahler-partition framework and the specific Laurent-expansion
mechanism producing `c0` are now independently verified at the level of their actual mathematical
content (not just arithmetic), and situated correctly against real, citable classical literature
(de Bruijn 1948, Flajolet-Gourdon-Dumas + its own corrigendum). This is substantially stronger
than round 2's arithmetic-only check. Still not a complete proof of Conjecture 3 itself, by
Codex's own calibrated admission: the controlled-remainder/pressure-gap step remains the real
open risk.

### GAP A: concrete implementation guidance for the pressure-gap test (not yet attempted)

Same round, gave concrete state-space sizing for the transfer-operator proposal: for block length
`B`, one-copy boundary state is a `B`-bit mask (`2^B` states), the second-moment (pair) state is
`4^B` states (or a smaller unordered-pair count under replica-swap symmetry). `B=6,8` are
computationally comfortable (4,096 and 65,536 pair-states); `B=10` (1,048,576 pair-states) needs
sparse/matrix-free methods, not a dense matrix. Gave the key methodological point: the
**independent sector** (`Q_ind = T tensor T`, `rho(Q_ind)=rho(T)^2`, from uncorrelated block
fillings) must be established as the baseline FIRST, then the actual pressure gap is the strict
inequality `rho(Q_collision) < rho(T)^2` for the correlated/collision sector -- conflating the two
sectors would hide the gap even if a real one exists. Declined to give a full worked `B=3`
numerical example without this project's exact local coverage-update rule (a reasonable,
honest limitation, not a dodge). Not yet implemented; queued for a future round once we can
supply the exact combinatorial rule needed.

### Round 4: the controlled-remainder attempt -- honest partial progress, gap explicitly not closed

Asked Codex to attempt the actual controlled-remainder estimate (the piece it flagged as the
real remaining risk after round 3). It produced a genuine tilted-local-limit-theorem calculation
(centered tilted characteristic function `Phi_{s,t}`, a central-arc bound `|Phi_{s,t}(u) -
e^{-V_s*u^2/2}| <= C*s*|u|^3*e^{-c*V_s*u^2}` for `|u|<=eta`, an off-axis decay bound using
`~log_3(s)` early independent factors giving `O(s^{-delta})`, `delta` an explicit function of the
tilt parameters), concluding `Pr(S_s=n) = [saddle form] * (1 + O(s^{-1/2} + s^{1/2-delta}))` --
"a genuine controlled remainder, with an unspecified but finite constant once (1)-(2) are proved."

**But explicitly, honestly NOT sufficient on its own**: "This does NOT manufacture the overlap-
sector pressure gap. A separate structural estimate ... is still needed." Codex derived that if
there are `O(s^p)` overlap/correlation sectors to sum, the argument needs `delta > p + 1/2`, and
that the basic per-factor bound alone ("(1+o(1))") is too weak -- it must be upgraded to a
summable error, "otherwise the product is only `s^{o(1)}`." Concluded plainly: "The 0.0164%
numerical gap remains evidence, not yet a proof."

**Net assessment**: this is honest, calibrated partial progress, not a completed proof and not
overclaimed as one. The local-limit-theorem machinery (Daniels/Chaganty-Sethuraman-style) is
confirmed to genuinely apply to the "central" saddlepoint approximation error, narrowing what
remains unproven to a specific, named structural gap (an overlap/correlation-sector estimate),
rather than a vague, whole-conjecture "we're not sure." Whether that remaining structural
estimate is itself tractable is now the open question, not yet attempted.

### Round 5: the overlap gap here is confirmed structurally distinct from GAP A's

Asked whether H-006's remaining overlap-sector gap and GAP A's residue-tracking difficulty are
secretly the same problem. Codex's answer: no. "GAP A is a one-branch, pointwise-support
problem... H-006 is a two-branch problem: how much does coincidence of two constructions increase
their joint target probability... a compression for the first does not supply the covariance
estimate for the second."

Gave a real, checkable formalization for H-006's overlap sector: for fixed-size branches of size
`k` out of `n`, pairs organized by intersection size `j`, `N_j = C(n,k)*C(k,j)*C(n-k,k-j)`
(standard, correct pair-overlap counting), an "exponent" `E(rho) = pair-entropy(rho) -
joint-tilted-rate(rho)` at macroscopic overlap `rho=j/n` needing a strict sector gap, plus a
separate near-diagonal sub-problem: anti-concentration for the symmetric-difference contribution
at `q := k-j = o(n)` ("q deletions versus q insertions"), which the current tilted-LLT argument
does not control uniformly -- "exactly why it cannot close H-006." No estimate proving either
piece was found this round; explicitly stated.

**Codex's own honest bottom line, this round**: "H-006 has a clean sector-rate formulation and a
near-diagonal anti-concentration subproblem, but I do not currently see the estimate that proves
either. If [GAP A's] Fourier audit is not attractive to run, this is a natural honest stopping
point rather than a reason to generate more variants." Recorded as a real signal the consultation
may be approaching its productive limit on this specific front, not treated as a demand to stop
outright (per the researcher's own instruction, the cycle continues until two consecutive rounds
produce nothing new on every front).

### Round 6: a fully self-contained near-diagonal statement, plus our own direct enumeration

Asked Codex to state the near-diagonal anti-concentration sub-problem precisely enough to check
numerically. It gave a complete, self-contained formalization: for `[n]={0,...,n-1}`, `k`-subset
`A`, `q`-subset `D subset A` (deletions), `q`-subset `I subset [n]\A` (insertions),
`B := (A\D) union I`, and the branch statistic `W_k(S) := sum_{r=1}^k 3^{k-r}*2^{s_r}` (this
project's own generating-polynomial exponent, `S` sorted increasing), define
`Delta := W_k(A)-W_k(B)` and `alpha_{n,k,q} := max_d Pr[Delta=d]` (point anti-concentration).
Noted explicitly: "replacing D by I changes the ranks of retained elements, hence their powers
of 3... That rank reweighting is the substantive difficulty H-006 isolates."

**Directly enumerated this ourselves** (`experiments/E-007-frobenius-mod3-valuation/
near_diagonal_check.py`, exact enumeration not simulation, `(n,k)` in `{(8,4),(10,5),(12,6)}`,
all `q` from 1 to `min(k,n-k)`). Real data, not from Codex:

    n=12,k=6:  q=1: alpha=0.00758   q=2: 0.00094   q=3: 0.00025   q=4: 0.00020   q=5: 0.00036   q=6: 0.00217

**Confirms Codex's own concern with real numbers**: `alpha` is WORST (highest) at the extremes
(`q=1`, nearly-identical pairs; and `q=k`, maximally different pairs), and best in the middle
range. This is exactly the "near-diagonal" weak spot Codex flagged: small `q` genuinely has
markedly worse (roughly 30x higher at `n=12,k=6`) anti-concentration than the bulk, real evidence
supporting (not yet proving) that this is a genuine, nontrivial obstruction rather than an
overcautious hedge. The `(8,4)` and `(10,5)` data show the same qualitative shape at smaller
scale.

**Status**: a real, checkable, well-posed open sub-problem with concrete supporting data, not a
proof either way. Queued for round 7: report this data and ask whether the observed trend (worse
at small `q`, and whether it seems to IMPROVE or WORSEN as `n` grows at fixed small `q`) is
consistent with what a valid proof strategy would need, or whether it's a warning sign.

### Round 7: GAP A confirmed exhausted; a genuine error caught in Codex's H-006 closed-form claim

**GAP A**: Codex's own words, unprompted for hedging: "GAP A is exhausted for this cycle. I do
not have a genuinely distinct route beyond the ones already closed." Consistent with this
project's own independent assessment (round 6). GAP A treated as closed for the remainder of this
consultation cycle.

**H-006**: Codex proposed a specific closed form for the `q=1` (single-exchange) case at the
central point `n=2k`: `alpha_{2k,k,1} = 1/Catalan(k) = (k+1)/C(2k,k)`, citing it as "the single-
exchange/ballot layer of the rank computation, not an extrapolation from three points," and gave
predicted values for `(8,4)`, `(10,5)`, plus new predictions for `(14,7)` through `(20,10)`.

**Checked directly against our own already-computed data, and against one new computation: THE
FORMULA IS WRONG.** Our actual enumerated values are `alpha_{8,4,1}=1/56`, `alpha_{10,5,1}=1/90`,
`alpha_{12,6,1}=1/132`, matching Codex's Catalan prediction ONLY at `n=12` (`132` happens to equal
both `12*11` and `Cat_6`, a numerical coincidence -- Codex was given only the `n=12` value
explicitly in the prompt and appears to have guessed the general formula from that single point,
not verified it). Ran one more exact enumeration, `(n,k)=(14,7)`: `alpha_{14,7,1}=1/182` exactly,
matching this project's own independently-observed pattern `alpha_{2k,k,1}=1/(n*(n-1))` (`182 =
14*13`), NOT Codex's Catalan prediction of `1/429`. **Four confirmed exact matches for `1/(n(n-1))`
(`n=8,10,12,14`), zero for `1/Catalan(k)` beyond the single coincidental point.**

This is exactly the kind of error this project's verify-before-trusting discipline exists to
catch (Rule 8c), and it worked: a beautiful-looking, specific, wrong closed form was caught before
being relied on, using data Codex was not given in full. `experiments/E-007-frobenius-mod3-
valuation/near_diagonal_check.py` updated with the side-by-side comparison table for the record.
Queued for round 8: report this correction plainly and ask Codex to reconsider given the actual
data, including our own now-conjectured formula `alpha_{2k,k,1} = 1/(n(n-1))`.

### Round 8: correction accepted immediately, a real proof supplied, and a genuine setback for
### the near-diagonal proof program uncovered

Codex accepted the correction with no defensiveness ("You were right; my Catalan claim was
wrong") and, rather than just fitting the corrected data, gave an actual combinatorial proof of
the STRONGER, more general statement `alpha_{n,k,1} = 1/(n(n-1))` for EVERY `1<=k<n` (not only
the central point `n=2k`): an explicit lower-bound construction (the `0 -> 1` adjacent exchange,
fixing `A = {0} union R` for `R` a `(k-1)`-subset of `{2,...,n-1}`, giving exactly `C(n-2,k-1)`
realizations of one common `Delta` value, hence `Pr = C(n-2,k-1)/[C(n,k)*k*(n-k)] = 1/(n(n-1))`
exactly by direct binomial-coefficient algebra) and a matching upper bound via a parity split
(even `Delta`: an exact two-term Pascal recursion `N_{n,k}(2d)=N_{n-1,k}(d)+N_{n-1,k-1}(d)`,
bounded by induction; odd `Delta`: a uniqueness argument recovering the exchange pair from the
low bits of `Delta`, bounding the free-suffix count by the same `C(n-2,k-1)`).

**Independently re-verified, this time across 7 NEW, non-central `(n,k)` pairs** (`(9,3),(9,4),
(10,3),(10,4),(11,4),(12,4),(12,5)`, chosen specifically to test `k != n/2`, which the earlier
Catalan claim could not even address): **exact match at every single point**, confirming the
general formula is real, not a coincidence or a fit to the specific points supplied.

**The important, honest, negative implication Codex drew out itself**: this materially worsens
the near-diagonal outlook, not improves it. The `q=1` layer has `k(n-k) ~ n^2/4` exchange
neighbors at the center, each with worst-atom probability exactly `1/(n(n-1)) ~ 1/n^2`. Their
PRODUCT tends to a CONSTANT (`1/4`), not to zero: "A crude max-atom union bound therefore gives
only constant-scale control -- no exponential slack or automatic pressure gap. The false Catalan
rate would have made that layer negligible" (i.e. the earlier, WRONG formula's much-faster
`~4^{-k}` decay would have made this problem look easy; the correct, slower `~n^{-2}` decay does
not).

**Net assessment**: a genuinely significant finding, not a minor correction. The `q=1` sub-case is
now solved exactly (a real, checked proof, not a heuristic), but its exact solution reveals the
naive uniform-max-atom approach to the near-diagonal problem cannot work as originally hoped --
a real, structural obstruction, discovered specifically BECAUSE an error was caught and chased
down rather than accepted. This downgrades H-006's near-diagonal sub-problem from "an open
target with encouraging-looking data" to "an open target now known to need target-sensitive or
sector-summed control, not a uniform bound." Whether such a sharper argument exists is unknown;
queued for round 9.

### Round 9: the O(1) mass is real but not fatal -- genuine signed cancellation exists

Asked directly whether the `q=1` layer's O(1) union-bound mass is a fundamental obstruction (any
correct argument must confront it and cannot make it vanish) or merely a weakness of the specific
crude technique used. Codex's answer, precise and calibrated: closer to "weakness of the
technique," with a real caveat.

**The O(1) mass at zero frequency is genuinely non-negotiable**: `E[e^{i*0*Delta}] = 1` trivially,
so no uniform-in-frequency cancellation claim can be true, and any rescue must show the final
transform removes/centers the zero mode rather than retaining it with positive weight.

**But away from zero frequency, real cancellation exists, not just hoped for.** Codex gave an
explicit signed calculation: for a `q=1` swap `a -> b` (`a in A`, `b not in A`) with phase
`e^{i*theta*(b-a)}`, averaged over uniform `k`-subsets `A`,

    E_A[(1/(n(n-1))) * sum_{a in A, b not in A} e^{i*theta*(b-a)}]
      = k(n-k)/(n(n-1))^2 * (|D_n(theta)|^2 - n),   D_n(theta) := sum_{r=1}^n e^{i*theta*r}

which equals the full O(1) mass `M_1` at `theta=0`, but drops to `O(1/n)` at any nonzero
`n`-th-root-of-unity frequency (where `D_n(theta)=0` exactly) -- real cancellation the crude
absolute-value union bound cannot see.

**Independently verified by direct enumeration** (`experiments/E-007-frobenius-mod3-valuation/
verify_round9_signed_cancellation.py`, `n=9,k=4`, brute-force LHS against the closed-form RHS):
exact match at `theta=0` (`0.27778`, matching `M_1=k(n-k)/(n(n-1))=20/72`) and at three nonzero
9th-root frequencies (`-0.03472` each, matching `-k(n-k)/(n(n-1)^2)=-20/576`), confirming the
roughly 8x drop from O(1) to O(1/n) exactly as claimed.

**Codex's own explicit caveat, taken at face value**: "It applies only if this is the project's
actual phase/kernel and the final quantity averages in the corresponding way; it is not a
pointwise, fixed-center bound." This is a real, verified mechanism, not yet shown to be the
actual mechanism this project's full argument needs.

**Codex's calibrated final verdict for Conjecture 3's status**: "numerically supported and open;
no valid controlled-remainder proof currently. The near-diagonal route is not dead, but its
surviving version is a materially different, sign-sensitive Fourier calculation with an explicit
zero/low-frequency analysis -- not a repair of the former union-bound argument." Recorded as the
honest, current state: real progress (the exact `q=1` solution, the signed-cancellation
mechanism, both proven and independently verified), genuinely short of a complete proof, with a
precisely identified remaining task (a zero-mode-aware, sign-sensitive extension of the argument)
rather than a vague "still working on it."

### Round 10 (final round of this cycle): honest synthesis

Asked for a wrap-up synthesis: the single most valuable result, calibrated confidence Conjecture
3 is TRUE, the first task for a future session, and any overextension to retract beyond the
already-caught Catalan error.

**Codex's answers, verbatim in substance**:
1. Most valuable result: "the precise identification of the remaining obstruction: the
   overlap/near-diagonal sector, together with a real signed-cancellation effect inside it...
   that is the actual proof lever" -- not the impressive-looking Mellin/Mahler main-term
   derivation, but the narrower, harder-won round 8-9 finding.
2. Calibrated confidence Conjecture 3 is true: **65%**. "The independently matching leading
   constant, numerical agreement, and sound literature-based Mellin/Mahler framework are
   meaningful evidence. But the unclosed overlap-sector estimate is exactly the kind of gap that
   can change an asymptotic, so I would not put this at high confidence yet."
3. First task for a future session: "formulate and prove a uniform signed-cancellation bound for
   the full overlap sector, not just the one-frequency near-diagonal model... before further
   refining constants, transfer operators, or modulus decompositions."
4. Explicit retraction, beyond the Catalan error: "the main thing I would retract is any
   implication that the existing 'controlled remainder' work [round 4] was close to a proof. It
   is controlled only away from the sector that matters most; the overlap contribution is not
   presently dominated." Also flagged three specific overextension risks to guard against:
   Frobenius non-vanishing is structural, not quantitative; the pressure-gap/coarse-modulus/
   conductor-Fourier routes are genuinely audited-closed, not latent shortcuts; and the round-9
   signed cancellation is "presently a local model result" -- extending it to the full
   multi-frequency overlap sum "would be unjustified" without further work.

**This project's own assessment agrees with all four points.** No reason found to discount this
synthesis; it matches the actual verified record of this 10-round cycle exactly.

## Overall status after the full 10-round consultation cycle

**Conjecture 3 is not proved.** It has, however, moved from "an old, cited-as-open conjecture
with no independent scrutiny this project could bring" to a precisely mapped target: a trustworthy
main-term/normalization-constant derivation (independently verified at three separate levels:
arithmetic, the underlying Laurent-expansion mathematics, and agreement with real numerical data
to 0.0164%), plus one sharply defined, partially-solved hard lemma (uniform signed cancellation
over the full near-diagonal overlap sector) standing between this project and an actual proof.
A real error (the Catalan-formula guess) was caught and corrected in the process, itself
producing a genuine, proven, independently-verified new fact (`alpha_{n,k,1}=1/(n(n-1))` for all
`k<n`) and a substantive, honest downgrade of what had briefly looked like an easier path.

Next step if this hypothesis is picked up again: attempt the uniform overlap-sector
signed-cancellation bound Codex named as the first task, per its own honest scoping (a real,
nontrivial estimate, not a small addendum).

## Second consultation cycle (2026-08-03): aimed explicitly at the standalone paper

Researcher's explicit instruction: advance H-006 toward a standalone, citable paper (independent
of the main WCC/`j*(l)` line), via a Codex consultation cycle of up to 50 rounds (default config),
same produce-verify-refine discipline as the H-003 mega cycles, stop early if Codex recommends it,
run a fresh-context critic pass (Rule 8/15) once done, report back before any paper prose is
drafted (Rule 5).

**Round 1: Codex proposed a genuinely different route (exponential tilting / local CLT) and, in
the process, found something much bigger than the requested overlap-sector bound: literal
Conjecture 3 (constant-ratio convergence) is probably FALSE, with an explicit, nonconstant,
Riemann-zeta-valued log-periodic correction -- though the SCALE-3 QUOTIENT Wirsching's argument
actually needs is claimed to survive.**

Setup: with `X = sum_j (2/3^j) U_j` (`U_j` iid Unif`[0,1]`, so `phi = h_3(.-1/2)` as already
established), `F(r) = E[e^{-rX}]`, `kappa(r) = log F(r)`, `t(r) = -kappa'(r)`. Proposed theorem
(formula A): `phi(t(r)) = exp(kappa(r)+r*t(r)) / sqrt(2*pi*kappa''(r)) * (1+o(1))` as `r -> infty`,
proved via exponential tilting: under the `e^{-rX}`-tilt, the `N ~ log_3(r)` "active" summands
become bounded-density variables whose characteristic factors admit a genuine local-CLT argument
(Gaussian core + uniform contraction + an explicit high-frequency tail bound), replacing the
previous, only partially-solved signed-cancellation decomposition entirely. **This IS exactly the
`phi_saddle` formula already implemented and validated in `saddlepoint.py` from the prior cycle**
(confirmed by direct algebraic comparison: `V(s)=s^2*K''(s)` makes the two formulas identical).

Separately, working with `w := log(r)`, `c := log(3)`, `L(w) := kappa(e^w)`: Codex derived the
exact recurrence `L(w+c) - L(w) = log(1-e^{-2*e^w}) - w - log(2)` (a genuine self-similarity
relation from the base-3 recursive structure of the infinite product), and, writing
`L(w) = Q(w) + H(w) + o(1)` for a specific quadratic `Q` matching the recurrence's smooth part,
claimed `H` is a genuinely NONCONSTANT periodic function (period `c`) with explicit Fourier
coefficients `Hhat''(m) = (2^{i*omega_m}/c) * i*omega_m * Gamma(1-i*omega_m) * zeta(1-i*omega_m)`,
`omega_m := 2*pi*m/c`, nonzero for `m != 0` by Hadamard's zeta zero-free-line theorem. Consequence
claimed: `phi(t(r))/phi_0(t(r)) = C * e^{H(log r)} * (1+o(1))` (formula B, NOT a constant limit),
but the scale-3 ratio `[phi(3t)/phi(t)] / [phi_0(3t)/phi_0(t)] -> 1` (formula C, since `w -> w-c`
lands on the SAME phase, so `H` cancels exactly) -- claimed to be the actual publication-grade
statement Wirsching's argument needs, replacing literal Conjecture 3. Cited (not yet independently
verified as accurately representing their content beyond what's already been read in the prior
cycle): Berg-Kruppel 1998 sec. 9 (an explicit arbitrary-periodic-factor `Q` in their own truncated
transform, cited as consistent with this finding); Hadamard 1896 (for the zeta non-vanishing).

**Independently verified, decisively, in three stages, using the already-validated `K_series`
function from `saddlepoint.py` (not re-deriving anything from scratch, reusing trusted
infrastructure)**:

1. **The recurrence `L(w+c)-L(w) = log(1-e^{-2e^w})-w-log(2)`**: verified by hand (re-derived
   independently from the definition of `kappa`, matches Codex's formula exactly) and numerically
   at 4 test points `w0=0.3,1.7,5.5,12.0` (`round1_periodicity_test.py`), differences at the level
   of `1e-49` to `1e-51` (pure floating-point noise at 50-digit precision).

2. **`H` is genuinely nonconstant (the qualitative core of the whole claim), tested completely
   independently of the Gamma/zeta formula**: computed `D(w) := L(w)-Q(w)` at 4 different phases
   `theta = 0, 0.25c, 0.5c, 0.75c`, each at depths `n=5..30` (`w = theta + n*c`). **Converges
   essentially instantly (already stable by `n=5`) to a DIFFERENT value at each phase**:
   `-0.627130933134703` (`theta=0`), `-0.626880145941178` (`theta=0.25c`),
   `-0.626722715471134` (`theta=0.5c`), `-0.62697344986013` (`theta=0.75c`) -- a real, small
   (`~4e-4`) but unambiguous phase-dependence, decisively refuting "H is constant" on its own,
   before touching the Gamma/zeta formula at all.

3. **The specific closed-form Fourier-coefficient formula**: first attempt (`round1_fourier_check.py`,
   coarse `mp.quad`) matched well at `m=1` (relative error `5.9e-5`) but badly at `m=2,3`
   (relative error `>1`), diagnosed as the quadrature being too coarse to resolve the rapidly
   `m`-shrinking true coefficients (`|Gamma(1-i*omega_m)|` decays like `e^{-pi*omega_m/2}`), NOT a
   flaw in the formula. **Redone with a proper equally-spaced 64-point DFT at 60-digit precision**
   (`round1_fourier_dft_check.py`): **`m=1,2,3,4` all match to the precision limit** -- relative
   errors `4.4e-18` (`m=1`), `4.0e-14` (`m=2`), `1.8e-10` (`m=3`), `6.5e-6` (`m=4`), degrading
   purely from finite-DFT/finite-precision truncation exactly as expected, not from any real
   discrepancy. **A fully verified, novel, and striking fact**: the Riemann zeta function
   genuinely appears in the asymptotic structure of Wirsching's invariant density `phi`, via a
   legitimate Mellin/Perron-type periodicity mechanism in the log-Laplace-transform recursion.

**Not yet independently verified**: the "cancels in the scale-3 quotient" claim (formula C) itself
(structurally plausible from the exact periodicity of `H`, since going from `t` to `3t` shifts `w`
by exactly `-c`, one full period -- but not yet checked numerically end-to-end); the completeness
and rigor of the tilted-local-CLT proof sketch for formula (A) itself (a real, standard-looking
probabilistic technique, but given only as a sketch, not a full proof checked line by line); and
whether formula (B)/(C) is the mathematically CORRECT replacement for Conjecture 3 as Wirsching's
argument actually uses it (needs rereading L-002's actual usage of Conjecture 3, not just trusting
Codex's restatement). Sent to Codex for round 2: report the verification results (recurrence and
periodicity both confirmed independently; Fourier formula confirmed to extreme precision at 4
frequencies after fixing our own quadrature bug), ask it to (a) verify or complete the scale-3
cancellation claim rigorously, (b) tighten the tilted-local-CLT sketch into a real, checkable
proof, and (c) identify whether this specific "zeta-valued log-periodic correction from a
Mellin/Perron-type recursion" phenomenon is already a known, named, citable result in the
literature (given its structural resemblance to Flajolet-Sedgewick-style Mellin-transform
harmonic-sum asymptotics, or de Bruijn/Flajolet-Gourdon-Dumas-style periodic fluctuations already
used earlier in this project for the constant `C`), so the paper can situate it properly rather
than presenting it as if entirely new.

**Round 2: Codex delivered on all three fronts, with a rigorous local-CLT proof for formula (A),
a cancellation argument for formula (C), and real, primary-source-groundable literature
(de Bruijn 1948, naming it the "de Bruijn-Mahler phenomenon" already known in the literature).**

(a) Cancellation argument: `phi(t)/phi_0(t) = exp(H(w_0(t)))*(1+o(1))` (1), with `w_0` the SMOOTH
(non-periodic) saddle defined by `t=e^{-w_0}*B_0(w_0)`, `B_0(w):=-Q'(w)=w/c-a`. Under `t->3t`,
`w_0 -> w_0-c-1/B_0(w_0)+O(B_0(w_0)^{-2})` (2), so `H(w_3)-H(w_0)->0` by periodicity, proving (C).
Refined diagnostic (3): `log[(phi(3t)/phi(t))/(phi_0(3t)/phi_0(t))] = -H'(w_0)/B_0(w_0) + O(B_0^{-2})`.

(b) A tightened, fully explicit local-CLT proof for formula (A), replacing the earlier informal
sketch: three named estimates (a Berry-Esseen-type Gaussian core bound (5), an explicit
high-frequency modulus bound (6) using only truncated factors `j<=N-2`, and `r^2*v(r)=N+O(1)` (4),
`N=floor(log_3 r)`), combined via a three-region Fourier-inversion split (core `|x|<=N^{1/12}`,
Gaussian tail, exponential tail), giving a fully explicit error: `phi(t(r)) = [saddlepoint formula]
* (1+O((log r)^{-1/4}))`.

(c) Named the general phenomenon (log-periodic fluctuations, "de Bruijn-Mahler phenomenon" for
base-`b` infinite products) and gave specific citations.

**Independently verified, with one confirmed result, one real numerical discrepancy flagged
honestly, and one citation confirmed / one not yet confirmed:**

1. **Formula (4), `r^2*v(r) = N+O(1)` (equivalently, using the already-validated `V_series`
   function which computes exactly this quantity): CONFIRMED.** Tested `r=10^2` through `10^40`
   (`round2_cancellation_check.py`): the difference `V_series(r) - floor(log_3 r)` stays bounded
   (`-0.63` to `+0.19` across the whole range, no growth trend), consistent with genuine `O(1)`
   boundedness, itself plausibly another manifestation of the same periodic-correction mechanism.

2. **The QUALITATIVE cancellation claim (formula C, ratio -> 1) is empirically supported: the
   diagnostic `log[(phi(3t)/phi(t))/(phi_0(3t)/phi_0(t))]` shrinks in magnitude as `l` grows**
   (`t=l*3^{-l}`): `0.0113` (`l=10`) down to `-0.00035` (`l=100`), with a clean, monotonically
   shrinking tail from `l=30` (`-0.00084`) to `l=100` (`-0.00035`), a genuine decay trend. **But
   the SPECIFIC quantitative prediction (formula 3, `-H'(w_0)/B_0(w_0)`, reconstructed from the
   already-verified DFT Fourier coefficients of `H`) does NOT match well**: the ratio
   `diagnostic/predicted` stabilizes around `~127-131` for `l=60..100` (a roughly CONSTANT
   multiplicative discrepancy across that range, not a growing one), rather than `~1`. Not yet
   diagnosed: could be an error in this project's own reconstruction (e.g. `w_0` was approximated
   crudely as `log(s_t)` -- the actual saddle including the periodic correction -- rather than
   solving `t=e^{-w_0}*B_0(w_0)` for the true smooth-only `w_0` as formula (1)-(3) require; or a
   convention/normalization mismatch in the truncated 4-term Fourier reconstruction of `H'`), or a
   real gap in Codex's formula (3) itself. Flagged honestly rather than either accepting the
   quantitative match on faith or over-concluding the cancellation claim is wrong (the qualitative
   trend clearly favors cancellation; only the specific rate formula is unconfirmed).

3. **De Bruijn 1948 ("On Mahler's partition problem," published in both KNAW Proceedings 51(6),
   659-669 and cross-indexed as Indag. Math. 10, 210-220): CONFIRMED as a real primary source
   matching Codex's description closely.** Fetched and read the first 4 pages directly: derives
   an asymptotic formula for Mahler's partition-counting function `p(rh)` with an EXPLICIT
   periodic-function correction term (`psi((log h - log log h)/log r) + o(1)`, `psi` periodic with
   period 1, equation 1.4), via the same Mellin-transform-plus-Gamma-function technique Codex's
   own derivation uses (equation 2.2, `log F(s) = (1/2*pi*i) * integral sum_k sum_m Gamma(z) *
   m^{-1} * (s*r^k*m)^{-z} dz` -- the `sum_m m^{-1-z}` term is exactly a harmonic sum that becomes
   `zeta(1+z)` on summing, the precise mechanism by which zeta enters). A real, on-point,
   primary-source-verified citation, not a hallucination.

4. **Erdős-Richmond 1976 (the specific "de Bruijn-Mahler phenomenon" naming attribution): NOT YET
   CONFIRMED.** `WebFetch` on the given Cambridge Core URL was rate-limited (HTTP 429) on two
   attempts; a `WebSearch` found real, adjacent literature (L. B. Richmond, "Mahler's partition
   problem," Ars Combin. 2 (1976), 169-189, confirming Richmond DID write on this exact topic in
   1976) but did not confirm the specific joint Erdős-Richmond paper or the exact phrase "de
   Bruijn-Mahler phenomenon." **Per Rule 11, this specific citation/naming should NOT be used in
   the paper until independently confirmed against the primary source**; the de Bruijn 1948
   citation itself is independently sufficient and already confirmed, so this doesn't block
   anything essential, only the specific terminology attribution.

Sent to Codex for round 3: report the confirmed formula-(4) result and de Bruijn citation; report
the cancellation formula's numerical discrepancy in full detail (exact reconstruction method used)
and ask Codex to help diagnose whether it's this project's implementation error or a gap in
formula (3); flag Erdős-Richmond 1976 as unconfirmed and ask for a more precise citation (exact
title, venue) if it actually exists, or drop the specific naming claim if uncertain.

**Round 3: Codex found the exact missing citation and correctly diagnosed the round-2 numerical
bug (in this project's OWN implementation, not in Codex's formula).**

**Erdős-Richmond 1976, located precisely**: P. Erdős, B. Richmond, "Concerning periodicity in the
asymptotic behaviour of partition functions," J. Austral. Math. Soc. Ser. A 21 (1976), 447-456,
DOI 10.1017/S1446788700019285, explicitly using "the de Bruijn-Mahler phenomenon" on p.448 per
Codex. **Partially independently verified**: `WebFetch` on both the direct PDF URL and the DOI
landing page was rate-limited (HTTP 429) on repeated attempts (Cambridge Core appears to be
blocking this environment specifically, not a transient issue); a `WebSearch`, however,
independently confirms the paper's real existence (Journal of the Australian Mathematical
Society, Cambridge-hosted, authors P. Erdős and B. Richmond, University of Waterloo) and its exact
topic ("examines De Bruijn's finding that in Mahler's partition problem there is a periodic
component in the asymptotic behaviour of such partition functions" -- a third-party description,
not this project's own or Codex's). **Honest epistemic note for the eventual paper**: this
citation is confirmed to exist and be on-topic via secondary corroboration, but the primary PDF
text itself has not yet been read directly (unlike de Bruijn 1948, which WAS read in full) --
flag this distinction if the citation is used before a direct primary-source read succeeds.

**The round-2 numerical discrepancy (diagnostic/predicted stabilizing at ~127-131x instead of
~1) was correctly diagnosed by Codex as this project's OWN implementation bug, not a flaw in
formula (3), and is now fully fixed and verified end-to-end.** Codex's diagnosis: `phi_0_same`
(the smooth-only comparison target) must use the EXACT SAME saddle-formula structure as
`phi_saddle` (with `Q` in place of `L=Q+H`, i.e. `H=0`), not the separately-derived Berg-Kruppel
closed form with its own different smooth prefactor/`O(1/B_0)` terms -- comparing against the
wrong baseline was mixing a second, unrelated `O(1/B_0)`-sized effect into the diagnostic, at
exactly the same order as the real periodic signal.

**Rebuilt `phi_0_same` per this exact spec and found a real bug of our own along the way**
(`round3_cancellation_fixed.py`): the first attempt still didn't match, tracked down to a missing
leading prefactor `s=e^{w_0}` (the exact analogue of `phi_saddle`'s own `P = s/sqrt(2*pi*V)*
exp(K+s*t)` structure) -- the discrepancy this introduced (`~-1.1` to `-1.2` in the log-diagnostic)
matched `-c=-log(3)` almost exactly, correctly self-diagnosed via the missing-prefactor's
predicted contribution to the scale-3 difference before even checking the code. **After fixing
this, both of Codex's two predicted convergence checks hold cleanly**:
`diagnostic/D_FD -> 1` (`0.64, 2.02, 1.62, 1.45, 1.29, 1.22, 1.17` for `l=10..100`, visibly
converging) and `D_FD/D_1 -> 1` (`-0.09, 0.45, 0.63, 0.73, 0.82, 0.86, 0.89` for `l=10..100`,
also visibly converging), exactly the pattern Codex predicted for a real `O(B_0^{-2})`-suppressed
higher-order correction. **This is now a complete, quantitative, end-to-end verification of the
scale-3 cancellation mechanism (formula C)**, not just the earlier qualitative trend.

**Status after 3 rounds**: this cycle has now produced a real, largely-verified, substantially
novel finding -- literal Conjecture 3 is very likely false (explicit zeta-valued log-periodic
correction, verified to extreme precision), but the corollary Wirsching's argument actually needs
survives and is now verified quantitatively, not just qualitatively. Formula (A) has a tightened,
explicit-error proof sketch (not yet independently checked line-by-line for full rigor). Sent to
Codex for round 4: report all of the above; ask for a genuine primary-source-accessible mirror of
Erdős-Richmond 1976 if one exists (arXiv, author's personal page, ResearchGate) so a direct read
can happen; and ask what the next concrete step toward a complete, submittable result is -- is
formula (A)'s proof sketch actually complete and rigorous as stated, or does it need the same kind
of line-by-line scrutiny that just caught two real implementation bugs in formulas (B)/(C)?

**Round 4: Codex gave a careful, self-critical audit of estimate (6), correctly flagged two real
subtleties before certifying it, and provided an honest admission it could not run code itself
this round.**

Audit result: estimate (6) IS rigorous with sharp constant `C=1`, via
`chi_r(u) = E_r(u) * prod_{j<=N-2} F_{j,r}(u)`, PROVIDED (i) the retained factors are genuinely
normalized characteristic-function factors satisfying `|F_{j,r}(u)| <= (1+u^2/r^2)^{-1/2}` with
the comparison direction requiring `lambda_{j,r} <= r` (not just asserted), and (ii) the omitted
two factors are themselves honest characteristic-function factors (`|E_r(u)|<=1` automatic only
then -- otherwise the proof has a real gap and `C` needs a separately-proved uniform supremum).
Also flagged: for a LATTICE-valued random variable, `|chi_r(u)|<=(...)^{-(N-2)/2}` cannot hold for
all real `u` (periodicity), so the bound needs restricting to the actual Fourier inversion
interval. Gave a concrete numerical-audit recipe. Explicitly stated it "could not run the
repository calculation here... not claiming numerical results" -- an honest limitation, not
papered over.

**Independently ran the numerical audit Codex specified, for our ACTUAL `X` (not a generic
setup)**: built `chi_r(u) := F(r-iu)/F(r)` directly from the exact complex-argument Laplace
transform product (`round4_estimate6_check.py`), scanned `r=100,1000,10000` and
`u` from `0.1r` to `10^5 r` (a huge range, far beyond any plausible inversion cutoff). **The bound
with `C=1` holds cleanly in every single test** (27 `(r,u)` pairs), worst observed ratio
`|chi_r(u)|/bound = 0.993` (comfortably `<=1`, tight near `u~0.1r` as expected since `C=1` is
optimal at `u=0`). **A useful clarification for Codex's own lattice caveat: our `X` has a
CONTINUOUS density (an infinite sum of independent continuous uniforms), not a lattice
distribution** -- confirmed directly by the data, since `|chi_r(u)|` decays smoothly and
monotonically to zero as `u` grows (down to `~1e-38` at the largest tested `u`, no periodicity,
no bouncing back up), unlike what a lattice characteristic function would do. **This means the
"restrict to the Fourier inversion interval" caveat likely does not apply to this specific
problem** -- worth confirming with Codex whether this removes that caveat entirely or whether a
different, non-periodicity-related restriction is still needed for the full proof.

**Erdős-Richmond 1976: Codex claims to have read the primary PDF directly this round ("the
direct Cambridge PDF was accessible to my browser and I read its ten pages"). This project's OWN
tools still cannot independently confirm this** -- `WebFetch` was retried a third time on the
exact same URL and hit the identical persistent HTTP 429. Per Rule 8c (verify a claim before
trusting it, including a claim ABOUT a citation), **Codex's own read-claim is recorded as
Codex's unverified assertion, not as this project's independent primary-source confirmation**,
unlike de Bruijn 1948 which this project's own tools did read directly. If this citation is used
in the eventual paper, it should be flagged for a manual check (the researcher's own institutional
access, or a retry once whatever is blocking this environment's access to Cambridge Core clears)
before being treated as fully verified per this project's standing citation discipline.

Sent to Codex for round 5: report the clean numerical confirmation of estimate (6) and the
continuous-vs-lattice clarification; ask whether that resolves the periodicity caveat entirely;
note honestly that its own primary-source read of Erdős-Richmond remains unverified by this
project's tools; ask what remains before formula (A)'s proof is complete and rigorous end to end
(estimate (5), the Gaussian-core bound, hasn't been independently audited yet either), and whether
enough now exists to start thinking concretely about the paper's actual theorem statements.

**Round 5: Codex confirmed the periodicity caveat vanishes for our continuous `X`, gave a
rigorous analytic proof of estimate (5) with an explicit constant, and sketched theorem
statements for A/B/C -- but the estimate-(5) proof turns out to be for a DIFFERENT, simplified
model than our actual construction, caught by independent numerical verification.**

Confirmed: "a continuous density cannot have a periodic characteristic function," removing the
lattice caveat cleanly, but flagged a real remaining requirement -- an ANALYTIC (not numerical)
tail bound `integral_{|u|>r/2} |chi_r(u)| du = o(v(r)^{-1/2})` is still needed to complete the
local-CLT theorem. Gave a clean derivation of (5) via the EXACT factorization
`e^{-iut(r)}*chi_r(u) = prod_j sinc(u*a_{r,j}/2)`, describing "each uniform summand as uniform on
an interval of width `a_{r,j}`," deriving `R_r(u)>=0` (nonnegative, purely real remainder, since
"centered uniforms are symmetric, so odd cumulants vanish") with an explicit sharp constant
(`1/5120`, much better than the originally claimed `O(1)`). Sketched four theorem statements
(Gaussian Fourier core; local CLT conditional on the tail bound; Mellin/log-periodic expansion;
scale-three cancellation), with an honest caveat: "extreme Fourier agreement is excellent
validation, but is not itself theorem evidence" -- B and C need proven convergence/remainder
arguments, not just numerical confirmation, to be real theorems. Also explicitly retracted its
earlier Erdős-Richmond direct-read claim when asked again, agreeing it should stay unverified.

**Independently tested the FINAL claimed inequality (not just trusting the "uniform summand"
derivation route) directly against our REAL construction, and found a genuine mismatch**
(`round5_gaussian_core_check.py`): computed `log(e^{-iu*t(r)}*chi_r(u))` directly from the exact
complex-argument Laplace transform (as in round 4) for `r=100,1000`, `u/r=0.01..0.5`. **The
imaginary part is NOT negligible -- it grows smoothly and non-trivially with `u`** (from `~1e-6`
at `u/r=0.01` up to `~-0.11` to `-0.19` at `u/r=0.5`, well beyond floating-point noise). **This
directly contradicts Codex's own stated basis for the derivation**: "centered uniforms are
symmetric, so odd cumulants vanish" -- but round 1's OWN, already-established fact (confirmed
independently that round) is that the `e^{-rX}`-tilted summands `Y_{j,r}` have TRUNCATED
EXPONENTIAL density `e^{-y}/(1-e^{-b_j})` on `[0,b_j]`, which is manifestly ASYMMETRIC (nonzero
skewness, nonzero third cumulant). **The nonvanishing imaginary residual is exactly the
signature of that real asymmetry, and directly falsifies the "uniform summand, no cubic term"
premise for OUR actual construction** -- round 5's elegant sinc-product derivation is
mathematically correct for a genuinely different (symmetric uniform-summand) toy model, not
(without further work) a proof about the real, asymmetric, truncated-exponential-summand process
this project actually has. The real-part deviation itself (`R_r(u)`, the quantity Codex's proof
targets) stayed small and broadly consistent in ORDER OF MAGNITUDE with the original
`O(N|u|^3/r^3)` bound (e.g. `r=100,u/r=0.5`: `|R|~0.037` vs. the bound's scale `N|u|^3/r^3~0.52`,
comfortably inside), so the FUNCTIONAL FORM of estimate (5) may still be salvageable -- but the
specific proof, sign structure (`R_r(u)>=0`, purely real), and constant `1/5120` given this round
rest on a premise that does not hold for this project's actual random variable. Sent to Codex for
round 6: report this precisely (the real, nonvanishing, growing imaginary residual, and the
concrete conflict with round 1's own established truncated-exponential summand density); ask for
either a corrected derivation of (5) built on the ACTUAL asymmetric summand structure (which will
need a genuine cubic/skewness term, not a vanishing one), or an explanation of why the uniform
substitute is nonetheless legitimate (e.g. some majorization/comparison argument connecting the
two models) if one exists.

**Round 6: Codex confirmed the round-5 error cleanly and without defensiveness ("This was a
real error: the uniform proof does not apply to the tilted process, and its 1/5120, pure-real
remainder, and sign claims do not carry over"), and gave a corrected derivation from the actual
truncated-exponential summand law.**

Derived the exact one-summand log-CF via `H(w):=log(sinh(w)/w)`: `K_b(z) = bz/2 + H(x(1-z)) - H(x)`
(`x:=b/2`), giving exact cumulants `kappa_2(b)=1-x^2*csch^2(x)`,
`kappa_3(b)=2-2x^3*csch^2(x)*coth(x) > 0` (genuinely nonzero, confirming the asymmetry),
`kappa_4(b)`, with small-`b` expansions `kappa_2~b^2/12`, `kappa_3~b^4/120`. Corrected expansion:
`Log(e^{-iut(r)}*chi_r(u)) = -V_r*u^2/2 - i*T_r*u^3/6 + R_{4,r}(u)`, `V_r:=sum_j kappa_2(b_{j,r})`,
`T_r:=sum_j kappa_3(b_{j,r})`, with an explicit integral remainder formula (6.3)-(6.4) for
`R_{4,r}`. Also gave a uniform-limit comparison (6.5)-(6.6) showing the ORIGINAL sinc-based
formula is recovered as the missing-phase term's leading order, and directly addressed option (b):
"phase is irrelevant only in a downstream argument that has already reduced everything to
`|chi_r(u)|`... the uniform sinc factor is a controlled approximation to the modulus, not a valid
substitution. For any complex Fourier inversion or phase-sensitive estimate, it is unusable" --
i.e. option (b) does NOT save the original argument in general.

**Independently verified the corrected cubic-term prediction directly against our real data, with
one more of our own scaling bugs caught and fixed along the way**
(`round6_cubic_term_check.py`). First attempt (naively plugging the SAME `u` used for `chi_r(u)`
directly into `T_r*u^3/6`) was off by 6 ORDERS OF MAGNITUDE -- traced to a real scaling error in
our own check, not Codex's formula: since `X = sum_j Y_{j,r}/r` (round 1's own definition), each
individual summand's frequency argument in `chi_r(u) = E_r[e^{iuX}]` is `u/r`, not `u` (matching
how `v(r):=V_series(r)/r^2` already correctly rescaled the REAL/quadratic term in round 5's
check). **After correcting to `-T_r*(u/r)^3/6`, the match is excellent**: ratio of observed to
predicted imaginary part `0.999, 0.999, 0.995, 0.981, 0.958` for `r=100` and `1.001, 0.999, 0.995,
0.979, 0.954` for `r=1000`, across `u/r=0.01..0.3`, converging cleanly to `1` as `u/r` shrinks --
exactly the expected `O(u^5)`-suppressed pattern. **The corrected cubic-term formula (6.2) is now
independently, quantitatively confirmed against the real construction.**

**Status**: round 5's error was real, correctly self-diagnosed by Codex once flagged, and its
replacement is now verified. Estimate (5) needs restating in the paper with the genuine cubic
term (asymmetric, not symmetric), which changes the local-CLT error analysis quantitatively (an
extra `O(u^3)`-scale phase term to control) though probably not the overall O((log r)^{-1/4})-type
conclusion, since the cubic term is itself controlled and small relative to the quadratic one in
the relevant range. Sent to Codex for round 7: report the confirmed corrected cubic term; ask
whether the presence of this genuine cubic/skew term changes the FINAL local-CLT error rate
(still `O((log r)^{-1/4})`, or does the odd term force a worse or different rate?), and whether
formula (A)'s overall theorem statement from round 5 needs restating given this correction.

**Round 7: Codex confirmed the cubic term does NOT worsen formula (A)'s rate, and gave a candid,
calibrated overall status: "a strong, coherent pre-proof rather than a finished theorem."**

Standardized skewness `gamma_r := kappa_{3,r}/sigma_r^3 = O((log r)^{-1/2})` -- strictly smaller
than the claimed `O((log r)^{-1/4})` local-CLT rate, so the coarse absolute local-CLT statement
survives with just the proof (not the theorem statement) needing to retain the cubic term; only a
finer, Edgeworth-level relative approximation would actually need it. **Independently verified
the scaling claim** (`round7_gamma_scaling_check.py`, reusing round 6's `T_r`/`V_r` machinery):
`gamma_r/L^{-1/2}` stays close to a slowly-drifting constant across 5 orders of magnitude in `r`
(`r=10^2` to `10^8`: ratio `2.03, 2.06, 2.07, 2.08, 2.08`), confirming genuine `Theta(L^{-1/2})`
scaling, strictly better than `L^{-1/4}`.

**Candid status assessment, unprompted and consistent with everything verified so far**: "B and C
being independently verified at high precision are excellent evidence that the structure is
right, and the correction of the false cubic cancellation actually makes the story more credible
rather than less -- because the checks are capable of falsifying a tempting simplification."
Single most important remaining gap named precisely: "a fully rigorous, uniform Fourier-inversion
argument for (A): correct cubic retained, central expansion controlled on the required window,
and -- most importantly -- an explicit bound for the medium/large-frequency contribution strong
enough to close the claimed `O(L^{-1/4})` rate." Explicit verdict: "compellingly supported but not
yet proof-complete."

**Session status at round 7 of up to 50**: a substantial, largely-verified body of new work
(zeta-valued log-periodic correction to Conjecture 3, verified to extreme precision; the scale-3
cancellation Wirsching's argument needs, verified end-to-end; a corrected, verified local-CLT
cumulant expansion with genuine skewness; three real errors caught and fixed across the cycle --
two in this project's own verification code, one in Codex's own derivation, each caught by
independent computation before being trusted). Sent to Codex for round 8: asked for the concrete
next step toward closing the single named gap (the medium/large-frequency Fourier-inversion
bound), continuing the same produce-verify-refine discipline.

**Round 8: Codex gave a concrete three-way frequency split (central `|t|<=T_c=2*sqrt(log L)`,
medium `T_c<|t|<T_6=theta_6*sqrt(L)`, tail `|t|>=T_6`, `L:=log r`, `t` the standardized frequency)
with an honest admission the medium region is NOT yet closed: "If (6.2) is only available as its
already-exponentiated additive central approximation, then... there is a genuine unproved strip.
Endpoint estimates alone cannot rule out a narrow characteristic-function peak in that strip."
Proposed target modulus bound for the medium region, `|psi_r(t)| <= e^{-c_m*t^2}` with `c_m=1/4`,
and a concrete numerical test (`M_r := sup{log|psi_r(t)|+t^2/4} <= 0` over the medium range,
ideally interval-certified).

**Ran the proposed numerical test (sample-based, not interval-certified -- flagged as a real
limitation) and found a genuine red flag, not just noise**
(`round8_medium_freq_check.py`): converting `t` to the raw frequency `u` via `u=t/sigma_r`
(`sigma_r:=sqrt(V_r)/r`), the bound holds only right at `t=T_c` itself, then FAILS immediately for
every larger `t` tested, across every `r` from `10^2` to `10^8` (worst margins `-86` to `-153`,
large and growing, not borderline). **Diagnosed the likely cause**: `T_c` itself already
corresponds to `u/r ~ 0.85` to `1.34` across the tested range (not a small fraction as the
central/medium split implicitly assumes) -- because this sum has only `N~log_3(r)` active terms
(a SMALL, slowly-growing count, unlike a classical many-term local CLT), the transition from
Gaussian-type decay to `estimate-(6)`-type power-law decay (`log|chi_r(u)| ~ -(N-2)*log(u/r)` for
large `u/r`, only LOGARITHMIC, not quadratic) appears to happen very close to `u/r~O(1)`, leaving
little or no room for a genuinely separate "medium" Gaussian-shaped region between the central
window and the tail. **This may mean the proposed three-region strategy doesn't fit this specific
sum's structure** (few terms, not many), rather than just needing better constants.

**Given this is now round 8 of a deep technical proof-completion effort, and per this project's
standing discipline of periodically checking whether continued effort has real expected value
(the same check that served the H-003 mega cycle well)**, sent to Codex for round 9 both the
concrete numerical red flag above AND a direct strategic question: given B and C are fully,
independently verified as real, citable results, and (A) has real, verified STRUCTURE (the
corrected cumulant expansion, the verified `C=1` tail bound) but an increasingly deep unresolved
gap in between, is closing (A) into a fully rigorous theorem still a good use of further rounds,
or should the paper honestly present (A) as a well-supported, precisely-scoped CONDITIONAL/partial
result (per Rule 10b's labeling discipline) while B and C stand as the paper's proven core, with
(A)'s medium-frequency gap named as explicit future work?

**Round 9: Codex gave a direct, unhedged verdict -- stop pursuing unconditional (A) via this
decomposition; present it as a precisely-scoped conditional result; make B and C the paper's
unconditional core.**

"Your diagnosis is essentially right... there is no demonstrated separation between the central
cumulant regime and the product/power-law tail regime. An unconditional (A) would now require a
genuinely different argument... not another iteration of the Gaussian-medium bound." Explicit
guidance for how to present (A) honestly: isolate the minimal Fourier-integral estimate actually
needed (not the numerically-disfavored `e^{-t^2/4}` envelope); prove every implication from that
estimate to (A); state clearly which pieces are rigorous; label the `M_r` numerical evidence as
diagnostic support only, not proof; explicitly flag the three-region Gaussian-middle route as
currently unsupported and numerically disfavored. "Make B and C the unconditional core theorems,
with their independent proofs and citations. That is mathematically honest and gives the paper a
durable contribution. Reopen unconditional (A) only if you have a new structural idea, not more
constant-chasing within this decomposition."

**Stopping the round-by-round proof-audit cycle here (round 9 of a planned 50), per Codex's own
direct recommendation, matching the discipline that served the H-003 mega cycle well** (recognize
when continued grinding on one specific approach stops having positive expected value, per Rule 9).

## Summary of this cycle's real, independently verified content (rounds 1-9)

**Critique round (fresh-context subagent, Rule 8/15) found a real overclaiming problem in an
earlier version of this summary, corrected below.** The original draft labeled B and C
"theorem-grade" as statements about Wirsching's actual invariant density `phi` (hence directly
about literal Conjecture 3). But every script in rounds 1-3 (`round1_periodicity_test.py`,
`round1_fourier_dft_check.py`, `round2_cancellation_check.py`, `round3_cancellation_fixed.py`)
computes with `K(s)` (the EXACT log-Laplace transform of `X`, via `saddlepoint.py`'s `K_series`)
and/or `phi_saddle` (the saddlepoint/tilted-CLT APPROXIMATION to `phi`, explicitly documented in
`saddlepoint.py` as `phi(t) = P*(1+O(1/l))`) -- never the true density `phi` itself, directly. The
bridge from `K(s)`'s exact periodic structure to a claim about `phi(t)` itself is precisely
formula (A), which this same file already labels conditional (round 8-9's unresolved
medium-frequency gap). So the leap from "`K(s)` has a proven periodic correction" to "literal
Conjecture 3 (a statement about `phi`) is false" silently rides on the same unproven bridge.
Relabeled below to keep the unconditional and conditional parts honestly separated, per Rule 10b.

**Theorem-grade, unconditional (B'): the exact log-Laplace transform `K(s)` of `X` has a
genuinely nonconstant, `c=log(3)`-periodic correction `H(w)` to its smooth part, with Fourier
coefficients given by a closed form involving the Riemann zeta function:
`Hhat''(m) = (2^{i*omega_m}/c)*i*omega_m*Gamma(1-i*omega_m)*zeta(1-i*omega_m)`, `omega_m=2*pi*m/c`.**
Verified independently at extreme precision (relative error `4e-18` at `m=1`, degrading only to
`6e-6` at `m=4` from finite-DFT truncation) via a 64-point high-precision DFT, against real,
nonzero, phase-dependent numerical data (not just algebra). The underlying recurrence
`L(w+c)-L(w)=log(1-e^{-2e^w})-w-log(2)` is verified exactly (and re-derived independently by hand
by the critic subagent). This is a real, novel, structurally grounded fact about `K(s)` (the "de
Bruijn-Mahler phenomenon," de Bruijn 1948 AND Erdős-Richmond 1976 both now read in full and
confirmed as genuine primary-source matches -- the researcher supplied the Erdős-Richmond PDF
directly 2026-08-03, resolving this project's own repeated access block; see "Item 2 resolved"
section below for the full confirmed read).

**Conditional on (A) (B): the extension of B' to a claim about the actual density `phi`, and hence
to literal Conjecture 3 being false, inherits formula (A)'s open gap.** Not yet an unconditional
theorem about Conjecture 3 itself.

**Theorem-grade, unconditional (C'): the same scale-3 cancellation
`[K(s)-based/phi_saddle-based ratio at 3t] / [analogous ratio at t] -> 1` holds for the
saddlepoint/`K(s)` construction, verified end-to-end quantitatively** -- two predicted convergence
ratios both confirmed converging cleanly to 1 (`diagnostic/D_FD` and `D_FD/D_1`), after two real
implementation bugs (missing `s` prefactor; wrong smooth-baseline comparison) were caught and
fixed.

**Conditional on (A) (C): the extension of C' to a statement about Wirsching's actual `phi`
quotient likewise inherits formula (A)'s gap**, same as (B).

**Conditional/partial (A): a real-variable saddlepoint/tilted-local-CLT approximation for `phi`
itself, with substantial verified structure but a genuine, currently unresolved gap.** The corrected
cumulant expansion (with a real, nonzero skewness term, `gamma_r=Theta((log r)^{-1/2})`, verified
numerically) and the high-frequency tail bound (estimate 6, `C=1`, verified numerically over 27
`(r,u)` pairs, `|chi_r(u)|` itself spanning 30+ orders of magnitude) are both solid. The
medium-frequency region connecting them is NOT closed: the proposed three-region Gaussian-
decomposition strategy is numerically DISFAVORED (large, growing negative margins, not
borderline), traced to a real structural feature (only `N~log_3(r)` active summand terms, unlike
a classical many-term local CLT) rather than a fixable constant. This is exactly the gap that (B)
and (C) need closed to become unconditional statements about `phi`/Conjecture 3. Should be
presented in any paper as a clearly labeled conditional result (Rule 10b), not a theorem.

**A promising, not-yet-attempted route to close (A)'s gap, surfaced by the critique itself (Rule
8e: every lead a critique surfaces gets tracked and given a real look), not chased yet**: de
Bruijn's own 1948 paper (already read in full, cited for B'/C') and Erdős-Richmond 1976 establish
their OWN periodic-correction results for the actual partition-counting FUNCTION (not just its
generating/Laplace transform) via a Mellin-Perron contour-integration argument, NOT via a local-
CLT/tilted-saddlepoint medium-frequency estimate. Since that is precisely the same kind of
transform-to-function inversion this project's formula (A) is stuck on, the classical Mellin-Perron
route (already in hand as a cited, read primary source) may be a genuinely different way to close
the `K(s)`-to-`phi` bridge than the tilted-CLT approach rounds 5-9 attempted and found
structurally unsuited to this problem's small `N~log_3(r)` term count. Flagged for the next round
of consultation rather than assumed to work.

**Real errors caught and fixed this cycle (Rule 8c discipline working as intended)**: two in this
project's own verification code (a missing `s=e^{w0}` saddle prefactor; a `u` vs `u/r` frequency-
scaling error), one in Codex's own derivation (the symmetric-uniform-summand premise for estimate
5, which is false for the actual asymmetric truncated-exponential tilted summands), and one
overclaiming issue in this very summary (the B/C theorem-grade mislabeling above), caught by the
critic subagent and corrected here.

**Critic subagent's overall verdict**: every numerical claim it re-ran matched exactly; three
hand-rederivations (the recurrence, the Laurent-expansion constant, the truncated-exponential
cumulants) all checked out; every "bug caught and fixed" diagnosis was verified to be a genuine,
correct mechanism, not a second wrong turn papering over the first; citations honestly scoped. The
one substantive finding was the B/C overclaiming issue above, now fixed. `HYPOTHESES.md`'s H-006
row also flagged as needing a status/impact update once this pass completes (addressed in the same
commit as this correction).

This cycle now has enough information to consider the paper's scope decided, with the caveat above
about (A)'s gap propagating into B/C. Reported back to the researcher 2026-08-03; researcher asked
to continue the cycle to specifically chase (1) the Mellin-Perron route, (2) an Erdős-Richmond
primary-source read, (3) further progress toward an unconditional Conjecture 3 statement.

## Round 10 (cycle continuation, per researcher's explicit instruction): a possible real closure of (A)'s gap

**Item 2 (Erdős-Richmond)**: still blocked. A sixth `WebFetch` attempt (across four rounds) hit
the identical HTTP 429; treating this as a genuinely inaccessible source from this environment,
not worth further direct-fetch retries. `WebSearch` corroborates existence/topic only, as before.

**Item 1 (Mellin-Perron): Codex gave an honest, precise answer -- the classical Mellin-Perron
route does NOT sidestep formula (A)'s gap the way hoped, but developing it surfaced a genuinely
new, different, and (on first verification) STRONGER medium-frequency estimate that may close the
gap by a different mechanism.** Key clarification: the periodic poles found in round 1-3 belong
to a MELLIN variable for `K` (an auxiliary contour-integral representation), not to the LAPLACE
variable `s` used in `phi`'s own Bromwich inversion; `F(s)=e^{K(s)}` is entire (its factors have
zeros, not poles), so shifting `phi`'s own inversion contour picks up no residues, and periodicity
enters `phi` only indirectly, through the saddle point `r(t)` and `K(r)`, `K''(r)` -- formula (A)'s
integral (2) is still the thing that needs bounding. Codex was explicit and unhedged about this
before offering anything else: "Mellin-Perron does not sidestep the gap."

**The new estimate (3), however, may be a genuine repair**: an EXACT-PRODUCT bound (not a
three-region decomposition), `|chi_r(u)| <= e^{C*N^{-A}}*(1+u^2/r^2)^{-M/2}`, `N~log_3(r)`
(count of "active" terms `x_j>=1`), `M=N-O(loglog N)` (count of terms with `x_j>=A*log(N)`),
derived from the per-factor bound `|1-e^{-x}e^{-i*theta}|/(1-e^{-x}) <= coth(x/2)` applied only to
the `M` "most active" factors (all OTHER factors are honest characteristic functions, hence
`<=1` in modulus, contributing no error at all -- an inequality, not an approximation, avoiding
round 8's failure mode entirely).

**Independently verified, comprehensively, and it holds cleanly** (`round10_estimate3_check.py`):
(1) the per-factor inequality, tested over a `6x5` grid of `(x,theta)` values, worst
`lhs/rhs` ratio `1.000000` (tight, consistent with a known equality case, never violated); (2) the
full product bound (3), with `A=2` and the simplest possible constant `C=1`, tested at
`r=100,1000,10000` and `u/r=0.1,0.3,0.5,1.0,2.0,10.0` (18 total points spanning 3 orders of
magnitude in `r`): **the bound holds at every single point, with a POSITIVE and GROWING margin**
as `u/r` increases (e.g. at `r=100`: margin `0.065` at `u/r=0.1` growing to `4.26` at `u/r=10`) --
the opposite pattern from round 8's failed test (which had large, growing NEGATIVE margins). This
is a much more comprehensive and decisive confirmation than round 8's, and covers exactly the
`u/r=O(1)` regime that broke the earlier three-region strategy.

**Not yet resolved**: whether estimate (3), combined with the already-verified central cumulant
expansion (formula 6.2) and the `r^2*K''(r)=N+O(1)` fact, is now actually SUFFICIENT to close
formula (A) into a genuine, unconditional theorem, or whether stitching the central and this new
single-estimate tail together still needs an explicit intermediate step. Also flagged: Codex's own
proposed "smallest first check" (a convergence test on `sqrt(2*pi*K''(r_N))*phi(t_N)*e^{-K(r_N)-
r_N*t_N} -> 1`) appears CIRCULAR as literally stated if `phi(t_N)` there means the saddle-formula
approximation itself (the ratio would then be trivially 1 by construction, not a real test); this
needs clarifying with Codex before attempting it -- an independent computation of true `phi`,
which this project does not currently have for large `t` (the old Fourier-inversion method breaks
down well before this regime), would be needed for a genuinely non-circular check. Sent to Codex
for round 11: report the strong confirmation of (3); flag the apparent circularity in the proposed
check and ask for a corrected, genuinely independent verification target; ask directly whether (3)
plus the existing central expansion now closes formula (A) unconditionally, or what (if anything)
is still missing.

**Round 11: Codex gave a precise, honest "no" with the remaining gap narrowed to something
specific and well-defined -- not a reopening of round 8's problem, a genuinely smaller one.**

"On the evidence stated, formula (A) is still conditional. The remaining issue is now narrow and
explicit: an integrated crossover lemma, not the old `u/r=O(1)` medium-frequency gap." Precisely
located WHERE round 10's test didn't probe: the needed handoff between the central (Gaussian)
region and estimate (3)'s tail is at `u/r = N^{-a}`, `1/3<a<1/2` (e.g. `N^{-5/12}`), i.e. `u/r->0`
as `N=log_3(r)->infinity` -- round 10's tests, all at FIXED `u/r` in `[0.1,10]`, never actually
approached this shrinking crossover point. Also confirmed our circularity concern about the
originally-proposed numerical check was correct, and gave a corrected, genuinely non-circular
version: split the exact saddle-line inversion integral at `U_N=r_N*N^{-5/12}` into a near-1 core
`C_N` and a tail `T_N` (both computed from the EXACT complex product for `K(r_N+iu)`, never from
the saddle formula itself), with `|sqrt(2*pi*K''(r_N))*phi(t_N)*e^{-K(r_N)-r_N*t_N} - 1| <=
|C_N-1| + T_N` -- if `T_N=o(1)` (using (3) to bound it), formula (A) closes; if not, it doesn't,
regardless of how well (3) matches at fixed `u/r`.

**Tested estimate (3) specifically AT the crossover point `u/r=N^{-5/12}`** (not yet the full
integrated `T_N`, just the pointwise bound Codex's argument depends on)
(`round11_crossover_check.py`), for `r_N=3^N/2`, `N=10,15,20,25,30`: **the bound continues to
hold at every tested `N`, with a positive margin** (`0.053, 0.035, 0.027, 0.022, 0.018` for
`N=10..30`) -- genuinely different from round 8's outright failure, but the margin is SHRINKING
as `N` grows (decelerating: successive differences `0.018,0.008,0.005,0.004`), so this is not yet
conclusive evidence the margin stays bounded away from `0` as `N->infinity` (the actual claim
formula (A) needs); it also only tests the boundary POINT, not the full integrated tail `T_N`.

**Status: real, substantive, honest progress -- the medium-frequency gap has gone from
"structurally disfavored, large and growing negative margins" (round 8) to "a specific, narrower,
still-open integrated-tail-lemma question with encouraging but inconclusive pointwise evidence"
(round 11).** This is exactly the kind of deep, specialized technical territory that could
absorb many more rounds without a guaranteed resolution. Given the depth reached, reporting back
to the researcher now with this status rather than continuing to grind unilaterally, per the
researcher's original request to "verify what can be advanced" on these three items -- item 1
(Mellin-Perron) produced real, verified, non-trivial progress even though it didn't directly
close the gap; item 2 (Erdős-Richmond) remains genuinely blocked; item 3 (Conjecture 3 itself)
has not moved beyond "conditional on formula A," now with a narrower, better-characterized
condition than before this round of the cycle.

## Item 2 resolved: Erdős-Richmond 1976 read in full via researcher-supplied PDF, 2026-08-03

The researcher located and supplied the PDF directly, resolving the access block this project's
own tools hit six times across four rounds. **Read in full (10 pages), primary-source-verified
against every claim made about it so far:**

- Title, authors, venue confirmed exactly: "Concerning Periodicity in the Asymptotic Behaviour of
  Partition Functions," P. Erdős and B. Richmond, J. Austral. Math. Soc. 21 (Series A) (1976),
  447-456.
- The phrase **"the de Bruijn-Mahler phenomenon" appears on p. 448 exactly as Codex claimed**,
  confirming Codex's earlier citation (round 4) was accurate, not fabricated, even though this
  project could not verify it directly until now.
- Confirms and extends the de Bruijn 1948 picture already verified: de Bruijn (1943, cited here
  as the Indag. Math. reference already read in full by this project) showed the periodic
  correction `U` for `p_A(rh)` (Mahler's problem, `a_v=r^v`) and "determined the Fourier expansion
  of `U`" -- this paper's own Theorem 1 extends the phenomenon to sequences with `a_v ~ v`, and its
  Theorem 2 investigates how LARGE the oscillatory term can get, finding a striking result: for
  `A` = the primes, the size of the oscillatory component is **directly tied to the location of
  the zeros of the Riemann zeta function** (a real connection to RH, not a superficial one --
  Theorem 2 states matching upper and near-matching lower bounds on `log p_A(n) - F(alpha)` in
  terms of `theta`, the sup of real parts of zeta's zeros).
- Technique confirmed as genuine Mellin-Perron contour-shifting (Roth-Szekeres asymptotic form,
  Mellin transformation of `sum_a a/(e^{a*alpha}-1)`, contour shifted past poles of
  `Gamma(t)*zeta(t+1)*zeta_A(t)` at `t=2*pi*i*k/log(r)`, using classical zero-free-region bounds
  -- Montgomery 1971 p.87, Prachar 1957 pp.77-88 -- for rigorous error control), matching this
  project's own round 10 derivation almost exactly in structure (same `Gamma(t)*zeta(t+1)` pole
  mechanism), independent confirmation that this project's own Mellin-Perron derivation (round 10,
  eq. MP) is following a real, established, correctly-applied classical method, not an invented
  one.

**Relevant to formula (A)'s open gap**: this paper's own rigorous error control (parts a/b/c of
its Theorem 2, using the classical zeta zero-free region plus a Prachar-style Gamma-function
residue argument) is applied to a genuinely different object (`log p_A(n)`, a discrete
partition-counting asymptotic, reached via Perron's formula for `P_1(u)` in `s`, not via a
tilted-CLT/saddlepoint density inversion). Codex's round 10 finding stands: this classical
technique does not directly transplant to `phi`'s own Bromwich inversion, since `F(s)=e^{K(s)}` is
entire (no poles for a residue calculation to hit) -- the periodicity mechanism here operates on a
different auxiliary sum than the one needed to recover `phi(t)` itself. No new leverage on the
crossover-lemma gap found from this read, but the citation itself is now fully primary-source
verified and safe to use in the eventual paper without qualification.

## Round 12 (researcher's explicit instruction to continue into specialized territory): a full analytic proof attempt for the crossover lemma

**Codex worked through the actual tail integral analytically and produced a complete, explicit
proof chain that `T_N=o(1)`, conditional only on (3) and (6.2) holding as GLOBAL/UNIFORM
statements (not just at the scattered points tested so far).**

Exact tail-integral identity: `J_M(a) := integral_a^infty (1+t^2)^{-M/2} dt = (1/2)*
B_{(1+a^2)^{-1}}((M-1)/2, 1/2)` (incomplete Beta function), with an elementary bound
`J_M(a) <= 1/(a*(M-1))*(1+a^2)^{-(M-2)/2}`. Applying this to estimate (3)'s RHS integrated over
the tail gives an explicit `R_N` (formula T-bound), which Codex shows decays like
`O(N^{-1/12})*exp(-0.5*N^{1/6}*(1+o(1))) = o(1)`. For the core, the already-verified cumulant
expansion (6.2) gives `sup_{|u|<=U_N}|E_N(u)| = O(N^{-1/4}) -> 0`, hence the truncated Gaussian
integral `G_N -> 1` (since `sigma_N*U_N ~ N^{1/12} -> infinity`). Chaining
`|I_N-1| <= |I_N-C_N| + |C_N-G_N| + |G_N-1| -> 0` gives a complete proof of (A) along the
subsequence `r_N=3^N/2`, explicitly conditional on: (i) estimate (3) proved for ALL real `u`
(not just tested points) with uniform constants, and (ii) the central expansion (6.2) holding
uniformly. Also named precisely what's needed to extend from the subsequence to all large `r`:
the same three estimates uniform across each "trinary `r`-block."

**Independently verified every checkable analytic piece, all confirmed exact**
(`round12_tail_integral_check.py`): (1) the incomplete-beta tail identity, tested at 4
`(M,a)` pairs, exact match to `1e-8`+ precision against direct numerical quadrature; (2) the
elementary bound, holds at all 4 test points, comfortably (not tight); (3) the resulting `R_N`
bound, computed directly for `N=10,15,20,25,30,40,50`, confirmed MONOTONICALLY DECREASING
(`0.584 -> 0.440 -> 0.375 -> 0.337 -> 0.311 -> 0.276 -> 0.254`), consistent with the claimed
`o(1)` limit -- though, honestly, the convergence is extremely slow (the `exp(-0.5*N^{1/6})`
decay only becomes numerically small at astronomically large `N`, since `N^{1/6}` grows very
slowly), so this is a real asymptotic fact, not something directly visible as "small" within any
practically computable range -- an expected, not concerning, property of this kind of very slow
polynomial-in-log tail decay, but worth stating honestly rather than implying the numbers "look
convincingly small" when they don't yet at these `N`.

**Where this leaves formula (A)**: the analytic PROOF STRUCTURE is now complete and verified
piece by piece. What remains is confirming that the two ingredients already have real,
already-derived justifications strong enough to count as the "global/uniform" versions the proof
needs: estimate (3)'s per-factor `coth(x/2)` bound (round 10, verified exactly at every tested
point, and structurally a genuine inequality with no approximation, so plausibly already global
in `u` by construction) and the cumulant expansion (6.2)'s remainder bound (round 6, derived via
an exact integral remainder formula, not just a Taylor truncation). Sent to Codex for round 13:
ask directly whether these two ALREADY-DERIVED pieces (not just the numerically-spot-checked
versions) are, as stated, actually the global/uniform statements the round-12 proof needs, or
whether real additional derivation work remains to upgrade them from "verified at test points" to
"proved for all real `u`, uniform in `r`."

**Round 13: Codex gave a direct "yes" -- "formula (A) is now a complete proof modulo routine
write-up."** Justification: the per-factor bound follows from the elementary triangle inequality
`|1-e^{-x}e^{-i*theta}| <= 1+e^{-x}` (true unconditionally for every real `theta`, `x>0`, "without
any discretization in `u`"), and estimate (3)'s factor-selection (`M`, via the threshold
`x_j>=A*log(N)`) depends only on `r` (through `N=log_3(r)`), not on `u` -- so there is no
`u`-dependent-selection subtlety to resolve, contrary to the more cautious general caveat Codex
initially raised. The remaining item, per Codex, is "bookkeeping" (writing the selection rule
explicitly), "not a new analytic gap."

**Independently hand-verified the core elementary claim, exactly, with no numerical approximation
involved**: `(1+e^{-x})/(1-e^{-x}) = coth(x/2)` is a standard, exact algebraic identity (confirmed
by direct symbolic manipulation and cross-checked numerically to `1e-12` at 5 test values of `x`),
and the per-factor bound follows immediately from the triangle inequality
`|1-e^{-x}e^{-i*theta}| <= |1| + e^{-x}*|e^{-i*theta}| = 1+e^{-x}`. This is airtight and
unconditional -- no subtlety, no discretization, genuinely holds for every real `theta` and every
`x>0`. Confirms the load-bearing building block of Codex's "yes" verdict is solid.

**This is exactly a Rule 11b moment (escalate to a genuinely independent model before declaring a
result done), not a moment to accept a same-model "yes" as settled.** After 13 rounds of a single
model's sustained, consistent effort, the risk of a confident, subtly-wrong closing verdict (a
belief this model holds that survives its own scrutiny because the scrutiny shares the same blind
spots) is exactly what Rule 8/15 and 11b exist to catch -- a fresh-context same-model critic helps
with drift and forgetting, but not with a shared analytical blind spot, and this claim (a complete
rigorous proof of formula (A), hence real, unconditional progress toward refuting/confirming
Conjecture 3 itself) is consequential enough to need a second, genuinely different-vendor opinion
before being written up as settled, not just banked on this round's "yes."

**Status: real, substantial, carefully verified progress across 13 rounds (recurrence, periodic
correction, Fourier coefficients, scale-3 cancellation, corrected cumulant expansion, tail
estimate, tail-integral proof chain, all independently checked piece by piece, with the load-
bearing elementary inequality behind today's "yes" now hand-verified exactly). But the claim
"formula (A) is essentially proved" should be treated as a strong, promising, NOT-yet-independently-
confirmed verdict until a second model (or the researcher directly) reviews the full round 10-13
chain.** Reporting this status to the researcher rather than continuing to grind past round 13
without that check, per the discipline that's served this whole project well.

## Independent second opinions (Rule 11b): Opus (max effort) and Gemini, 2026-08-03

Per the researcher's explicit instruction, a self-contained review packet (the full round 10-13
proof chain, with six specific questions) was sent to two genuinely independent reviewers:
Gemini (run by the researcher directly, no CLI access from this environment) and a fresh-context
Opus subagent at maximum effort (`~26 minutes`, `59` tool calls, extensive independent numerical
re-derivation). Per the researcher's instruction, Opus's verdict is weighted more heavily.

**Gemini's verdict: "genuinely incomplete."** Confirmed sound: the per-factor bound (question 1)
and the saddle-line inversion identity (question 4, citing `phi`'s compact support and
`C^infinity` smoothness as a Rvachev atomic function, guaranteeing absolute integrability of
`chi_r`). Flagged as real gaps: the `R_N` asymptotic-decay claim (question 2, "checking 7 discrete
values... is mathematically void as a proof of an asymptotic limit"); the core bound `C_N->1`
(question 3, needs an explicit uniform bound on `K_b^{(4)}`, not inferred from numerics); and,
most seriously, the subsequence restriction `r_N=3^N/2` (question 5, "a major structural gap...
extending to all `r` requires parameterizing `r=3^{N+tau}/2` and proving uniformity in `tau`").

**Opus's verdict: "complete modulo three named routine lemmas."** Explicitly NOT "complete" as
Codex claimed, and explicitly NOT "genuinely incomplete" either -- a real, calibrated middle
verdict, reached only after extensive independent work, not a compromise between the other two.

**The single most important development from this review: Opus independently verified formula
(A) is actually TRUE, by direct numerical Fourier inversion of `phi(t(r))/phi_saddle(t(r))` --
something no one had actually computed across all 13 rounds.** Every ingredient (cumulant
expansion, tail bound, skewness scaling) had been tested; the actual CONCLUSION never had been.
Opus's own measured `I-1` (the relative saddlepoint error) tracks the classical second-order
Edgeworth correction with the ratio-to-prediction climbing `0.833 -> 0.942 -> 0.960 -> 0.968 ->
0.973` for `N=7,10,12,13,14`, and -- critically -- **holds off the subsequence too**
(`rho=1.5,2.2` tested directly, ratio `1.001` to `1.012`). This is real, independent, decisive
numerical evidence that formula (A) itself is correct, not merely that its ingredients are
individually plausible.

**Resolving the Opus/Gemini disagreement on question 2 (the `R_N` asymptotic decay)**: Opus
agrees with Gemini's methodological objection (checking 7 discrete points cannot itself prove an
asymptotic limit) but resolves it differently: `R_N` has an exact closed form (the incomplete Beta
function, independently verified to 40 digits), and evaluating that closed form directly
(`N=10` through `N=10^6`) shows the ratio to the predicted asymptotic
`(4/sqrt(2*pi))*N^{-1/12}*exp(-N^{1/6}/2)` converging cleanly to exactly `0.5000`, with no plateau
or reversal. Opus's answer supersedes Gemini's concern here: the trend was never the real argument
(agreeing this would be weak), the closed-form evaluation is, and it was actually carried out.

**Both reviewers independently agree the subsequence restriction is the single most serious real
gap -- and Opus sharpens WHY it matters far beyond "routine but unfinished":** `log(r_N) mod
log(3)` is CONSTANT along `r_N=3^N/2`, so this subsequence samples exactly ONE PHASE of the very
`log(3)`-periodic oscillation formula (B)/(C) are trying to establish for `phi` itself.
**Restricted to this subsequence alone, the result is logically COMPATIBLE with literal
Conjecture 3 being TRUE -- it refutes nothing.** The extension itself is verified to be
mathematically routine (Opus tested `rho=1.5,2.2` directly in `r=rho*3^N/2`, `rho in [1,3)`,
confirming `V_r=N+O(1)`, `T_r=2N+O(1)`, `M=N-O(loglog N)`, and estimate (3)'s constant are all
continuous and uniformly bounded in `rho`), but "routine" is not "optional": this step carries the
entire scientific payload of the claim.

**Two further real, previously-unidentified gaps found by Opus, both with concrete fixes:**

1. **The `K_b^{(4)}` cancellation lemma is missing, and its naive version diverges.** Bounding the
   two terms of the exact formula `K_b^{(4)}(z) = 6/(1-z)^4 - b^4*a*(1+4a+a^2)/(1-a)^4` separately
   gives `~6` per summand over infinitely many `j` (divergent sum); the sum converges only because
   the pole cancels for small `b` (`b^4*f''''(c) = -6/(1-i*tau)^4 - b^4/120 + O(b^6)`, verified
   numerically: `|K_b^{(4)}(i*tau)|/b^4 -> 1/120` uniformly for `|tau|<=1`, giving
   `sum_j sup|K^{(4)}| = 6N - 11.61`). The exact formula itself was independently re-verified
   against numerical differentiation of the CGF off the real axis, 40-digit agreement.

2. **The proof as stated gives an astronomically ineffective rate, `O(N^{-1/4})`, versus the real,
   measured rate `~-0.075/N` -- fixable in about half a page, and Opus found the fix.** The cubic
   term is purely imaginary and odd, so its first-order contribution to the integral against the
   Gaussian vanishes BY PARITY; expanding `e^E = 1+E+E^2/2+...` properly with this in mind gives
   `O(1/N)` directly, matching the measured rate almost exactly. This changes the result from
   "would need `N~6*10^14` to see numerically" to "effective at `N` in the low hundreds" -- a real,
   valuable mathematical contribution from the review itself, not just an audit finding.

**A conceptual reframing Opus offered, useful for the eventual write-up**: under the `r`-tilt, `X`
is essentially a sum of `N~log_3(2r)` `Exp(1/r)` variables (since `coth(x_j/2)->1` for `x_j>>1`,
each factor becomes exactly `r/(r+iu)`), plus a geometrically small remainder -- formula (A) is
literally the classical saddlepoint approximation for a `Gamma(N, 1/r)` distribution as
`N->infinity`, and the Gamma distribution's own classical `-1/(12n)` Edgeworth correction is
exactly what Opus's measured numbers reproduce. This makes the exponent window `alpha in
(1/3,1/2)` (in `U_N=r_N*N^{-alpha}`) transparent: need `N*(U/r)^2->infinity` (tail) and
`N*(U/r)^3->0` (core), and `5/12` is just one point in that interval, not a magic number.

**Two further, previously-unflagged gaps in the bridge from formula (A) to Conjecture 3 itself
(separate from formula (A), not yet addressed anywhere in this cycle)**: (i) `L(w)=Q(w)+H(w)+o(1)`
does not automatically imply anything about `K''(r)` (needed for `phi_saddle`'s own denominator);
differentiating an asymptotic expansion twice is not automatic and needs its own short derivation
(recoverable from the exact recurrence, not yet written). (ii) The reparametrization from `w=log r`
to `tau=log(1/t(r))` does NOT commute with translation by `log 3`:
`tau(w+c)-tau(w) = c - c/w + O(1/w^2)`, so "periodic in `log r`" only gives "asymptotically
periodic in `log(1/t)`, with an `O(1/log r)` phase drift" -- not periodic outright. Opus tested
numerically whether the oscillation survives this step (the residual of `log(phi_saddle)` against
`log(1/t)`, after removing a quadratic fit, has a first Fourier mode of magnitude `1.358e-4`,
comparable to `Hhat(1)=1.047e-4` computed directly -- does not cancel), suggesting the result
likely survives, but "the argument isn't written."

**A useful clarifying point: the NUMERICAL evidence against literal Conjecture 3 does not actually
need formula (A) proved at all.** `phi(t)/phi_saddle(t)`, computed by direct Fourier inversion
(exactly what Opus did to verify formula A), directly exhibits the oscillation without any
asymptotic theory. Formula (A) is needed for the PROOF that this reflects the true `phi`, not for
the evidence itself.

**Anti-confabulation check (given this project's known risk of an AI's confident report hiding
tool-execution issues)**: Opus independently re-verified several of the review packet's own
numeric claims without being asked to (`gamma_r/(log r)^{-1/2}` ratio: Opus gets `2.021` at
`r=10^2`, `2.087` at `r=10^8`, limit `2*sqrt(log 3)=2.0963`, matching the packet's `2.03`/`2.08`;
`kappa_2~b^2/12`, `kappa_3~b^4/120`, `kappa_3->2`; the Beta identity; the `K^{(4)}` formula; the
`L` recurrence; the `Hhat` closed form -- all independently confirmed, with `R_N` values ~30%
different from this project's own due to different constant bookkeeping but the same functional
form). The numerical chain across this whole cycle is real, not confabulated.

## Corrected status after independent review

**Formula (A) is TRUE** (now independently confirmed by direct computation, not just a plausible
proof sketch) **and its proof is complete modulo three concrete, routine lemmas** (the `K^{(4)}`
cancellation argument; a one-line geometric-sum estimate for estimate (3)'s constant; and,
carrying real scientific weight despite being mathematically routine, the extension from
`r_N=3^N/2` to all `r`, without which the result doesn't actually refute anything). **Separately,
the bridge from formula (A) to literal Conjecture 3's refutation needs two further, currently
unwritten steps** (propagating the periodic expansion to `K''(r)`; handling the
`w=log(r) -> tau=log(1/t)` reparametrization's phase drift), neither part of formula (A) itself,
neither yet attempted. Round 13's "essentially proven, modulo routine write-up" framing was too
strong as stated (it did not distinguish formula (A) from the full Conjecture-3 bridge, and missed
the subsequence issue and the `K^{(4)}` divergence entirely) but not far from accurate for formula
(A) narrowly construed; corrected here per Rule 8b (re-check summary claims after a correction
lands). Reported to the researcher 2026-08-03, who asked to continue closing the gaps.

## Round 14: an attempt at all five remaining items, unusually delivered with NO self-checking (Codex's own sandbox failed this round)

Codex's tool environment failed before executing any command this round ("the workspace shell is
currently failing... `bwrap: loopback: Failed RTM_NEWADDR`", and a file-write attempt also
failed), so every derivation below is hand-work with zero numerical self-verification on Codex's
end -- making this project's own independent verification not redundant but the ONLY check these
claims received before being trusted.

**Item 1 (subsequence extension, the most important): a uniform-in-`rho` argument.** For
`r = rho*3^N/2`, `rho in [1,3)`, `b_j = rho*3^{N-j}`: with `v(b):=kappa_2(b)`, `u(b):=kappa_3(b)`
(matching earlier notation), claimed `v(b)=1+O(b^2*e^{-b})` for `b>=1` and `v(b)=O(b^2)` for
`b<=1` (similarly for `u`), giving, after reindexing `m=N-j`: `V_r = N + C_V(rho) + o(1)`,
`T_r = 2N + C_T(rho) + o(1)`, with `C_V, C_T` given by absolutely and uniformly convergent series
in `rho`. Conclusion claimed: `sup_{1<=rho<3} |phi(rho*3^N/2 saddle ratio) - 1| = O(N^{-1})`
uniformly across the whole phase, not just the two spot-checked values from the review.

**Independently verified, and this is a strong, clean confirmation**
(`round14_rho_uniformity_check.py`): computed `V_r - N` and `T_r - 2N` directly (not the claimed
asymptotic form, the actual quantities) at `rho = 1.0, 1.3, 1.5, 2.0, 2.5, 2.9` and
`N = 10, 20, 30, 40` for each. **`V_r-N` and `T_r-2N` are EXACTLY constant across all four tested
`N` for each fixed `rho`** (not merely bounded -- unchanged to the full precision shown, e.g.
`rho=1.5`: `V_r-N=-1.038962`, `T_r-2N=-3.025829` at every one of `N=10,20,30,40`), and vary
smoothly with `rho` (from `-1.417`/`-3.727` at `rho=1.0` to `-0.447`/`-1.781` at `rho=2.9`). This
directly confirms the claimed `C_V(rho)`, `C_T(rho)` are genuine, well-defined, finite,
smoothly-varying constants, with the `o(1)` correction already numerically invisible by `N=10` --
strong support for the whole uniform-in-phase extension.

**Item 2 (K^{(4)} cancellation lemma): a real, checkable generating-function identity, fully
confirmed.** Claimed `F(x) := e^{-x}(1+4e^{-x}+e^{-2x})/(1-e^{-x})^4 = sum_{n>=1} n^3*e^{-nx}`
(a standard Eulerian-type generating function) with Laurent expansion
`F(x) = 6/x^4 + 1/120 - x^2/504 + O(x^4)` as `x->0`, giving the cancellation
`K_b^{(4)}(i*tau) = -b^4/120 + b^6*(1-i*tau)^2/504 + O(b^8)` for `|tau|<=1`, hence
`sup_{|tau|<=1}|K_b^{(4)}(i*tau)| <= C*b^4` for small `b` (resolving the divergence).
**Independently verified, both pieces, exactly** (`round14_closure_checks.py`): the closed-form
`F(x)` matches its defining series to `1e-30`+ precision at 4 test points; the Laurent expansion's
error term divided by `x^4` converges to a constant (`~0.0001736`) as `x -> 0.001,0.01,0.05,0.1`,
confirming the stated `O(x^4)` order exactly.

**Item 3 (estimate 3's geometric-sum bound): also fully confirmed.** Claimed
`sum_{k>=0} log(coth(x_k/2)) <= 3q` for `x_k = 3^k*x_0`, `q := e^{-x_0}`, `q<=1/3`, via
`log(coth(x/2)) = 2*arctanh(e^{-x}) <= 2e^{-x}/(1-e^{-2x})` and geometric domination.
**Independently verified**: both the loose `3q` bound and the tighter intermediate bound
`2q/(1-q^2)^2` hold comfortably at 4-6 test values of `x0`, including right at the `q=1/3`
boundary (`x0=log(3)`).

**Item 4 (rate improvement to O(1/N)): a specific, falsifiable numerical prediction, not yet
independently checked.** Claimed second-order correction
`phi(t(r))/phi_saddle(t(r)) = 1 + K^{(4)}(r)/(8*B^2) - 5*K'''(r)^2/(24*B^3) + O(N^{-2})`,
`B:=K''(r)`, predicting `N*(phi/phi_saddle - 1) -> -1/12 ~ -0.0833`, with the explicit remark that
"the observed `-0.075` is plausibly a finite-`N` approach to `-0.08333`." This requires the same
direct-Fourier-inversion machinery Opus used (not yet replicated in this project's own tooling) to
check properly; not independently verified this round. Flagged for a follow-up round.

**Item 5 (the two bridge steps to Conjecture 3 itself): both addressed, not yet independently
checked.** (a) An exact recurrence-based derivation for `K''(r)`'s own asymptotic expansion
(avoiding differentiating an unspecified `o(1)`), via an explicit remainder `R(w)` with
`R^{(m)}(w) = O_m(e^{mw - d*e^w})` (doubly-exponentially small, hence differentiable term by term).
(b) The `w=log(r)` to `tau=log(1/t)` reparametrization: derived
`tau(w+c)-tau(w) = c - c/w + O(w^{-2})`, giving a "chirped oscillation"
`H(w(tau)) = H(tau + log(tau) - log(c) + O(log(tau)/tau))`, NOT literal `c`-periodicity in `tau` --
and a genuinely new closed-form non-cancellation certificate,
`Hhat_m = -(d^{i*lambda}/c)*Gamma(-i*lambda)*zeta(1-i*lambda)` (nonzero, by the same
Gamma/zeta-nonvanishing argument as the original `Hhat''` formula), replacing the earlier numerical
`|Hhat(1)|~1.047e-4` estimate with a proof-level nonvanishing statement. Also flagged an important
methodological correction for future numerical checks: "a raw Fourier coefficient in `tau` should
not be compared directly with `Hhat_1`... de-chirping via `w(tau)`, or sampling fixed `w mod c`, is
the correct numerical test" -- i.e. earlier informal comparisons of residuals in `tau`-space
(including one Opus itself did) were using the wrong variable and need redoing correctly.

**Status after round 14**: three of five items (1, 2, 3) now have real, independently-verified
derivations, not just Codex's own unchecked hand-work (doubly important this round since Codex's
own tools failed and self-checked nothing). Two items (4, 5) have concrete, checkable claims laid
out but not yet independently verified. Sent to Codex for round 15: report the strong
confirmations of items 1-3; ask for the exact numerical setup needed to check item 4's `-1/12`
prediction properly (does it require literally inverting the Fourier integral for `phi(t(r))`
itself, or is there a cheaper equivalent check using only `K`, `K''`, `K'''`, `K^{(4)}` directly,
matching how items 1-3 were checkable without a full inversion); and ask for the corrected,
"de-chirped" numerical test for item 5(b)'s Fourier-coefficient nonvanishing claim, precise enough
to actually run.

**Round 15: Codex gave the exact direct-inversion setup for item 4, the corrected de-chirped test
for item 5(b), and an appropriately un-inflated verdict** ("a strong derivation with two
outstanding falsifiable audits, not fully verified" -- explicitly retracting the earlier "prior
Item 5 evidence should be treated as invalid rather than partial support").

**Independently implemented item 4's direct inversion from scratch (not copying Opus's own code,
built fresh from this project's own established `K_series`/`M_series`/`V_series` machinery), and
this is a decisive, high-value result.** First attempt used mpmath's adaptive `quad` on the
complex oscillatory integral and was too slow to complete; switched to a fixed-grid Simpson
integration (300 points, `y` in `[-15,15]`), which ran in ~1.5s per `N`. **First run produced
`R_N` essentially `0` for every `N` (`N*(R_N-1) approx -N` exactly) -- an immediate red flag,
correctly diagnosed as a sign error in the Bromwich-inversion exponent**: hand-rederived the
inversion from first principles (`phi(t) = (e^{st}/2pi) * integral e^{K(s+iu)} e^{iut} du` via the
standard Laplace-inversion contour substitution `z=s+iu`), giving `R(s) = sqrt(K''(s)/(2pi)) *
integral e^{K(s+iu)-K(s)+iut} du` -- confirming the PLUS sign on the `iut` term (Codex's own
formula, in its own relabeled `t_N`/`r` notation, appears to use the opposite convention; rather
than risk a translation error matching notations, this project's own independently-rederived sign
was used and tested directly). **Testing both signs numerically settled it immediately**: `+iut`
gives `R approx 0.993` (correct, near 1 as required); `-iut` gives `R approx 0` (the bug). This is
the fourth real sign/scaling bug this project's own verification code has caught and fixed across
this whole cycle (after the missing-`s`-prefactor bug in round 3, the `u`-vs-`u/r` scaling bug in
round 6, and this one) -- a genuine, recurring risk in this kind of Bromwich/Fourier-inversion
work, caught every time by insisting on a sanity check (`R approx 1`) before trusting any
downstream number.

**After the fix, `N*(R_N-1)` for `N=7..12` came out as `-0.0530, -0.0591, -0.0633, -0.0664,
-0.0687, -0.0705` -- matching Opus's own independently-computed values to 4-5 significant
figures** (Opus reported `I-1` at `N=7,10,12`: `-0.0075748, -0.0066405, -0.0058777`, i.e.
`N*(I-1) = -0.05302, -0.066405, -0.070532` -- essentially exact agreement with this project's
completely independent reimplementation, different integration method, different code). **A real,
mutual cross-validation between two independent computations, strong evidence against either
being confabulated.** Extended further, `N=16,20,25` give `-0.0749, -0.0770, -0.0786`, clearly
and monotonically approaching the predicted `-1/12 approx -0.0833`, with the residual gap
shrinking roughly consistently with the claimed `O(1/N)` next-order correction (residual times `N`
stays in a `0.12-0.15` band across `N=12..25`, slowly drifting, not exploding or reversing).
**This is now real, decisive, independently-reproduced numerical confirmation of item 4's `-1/12`
rate prediction, not just a plausible-looking trend.**

**Item 5(b)'s de-chirped Fourier test** (sampling at fixed `w mod c` across increasing blocks
`ell`, per Codex's corrected procedure) has NOT yet been implemented -- a real, precise, checkable
specification exists, but building the phase-locked sampling correctly (matching `w(tau_{ell,j}) =
ell*c + u_j` on the correct asymptotic branch) is nontrivial and not yet attempted this round.

**Status after round 15**: items 1, 2, 3, and now 4 all have independently-verified derivations
(item 4 verified about as strongly as anything gets in this cycle -- two independent
implementations agreeing to several significant figures on a nontrivial asymptotic rate). Item
5(a) (the `K''(r)` expansion via the exact-recurrence remainder) has a real derivation, not yet
independently checked. Item 5(b) has a precise spec, not yet implemented. Given the substantial
length of this cycle (15 rounds) and the strength of what's now confirmed, this is a natural point
to report back to the researcher with a status update before continuing further. Researcher asked
to continue toward closing item 5.

## Round 16: item 5(a) independently confirmed; item 5(b)'s own framing self-corrected as underspecified; a calibrated overall verdict

Codex gave a clean, independently-checkable test for item 5(a) (`Q(r) := (V_series(r)-A(r)) *
e^{d*r}/r^2` should be `O(1)`, using `A(r)` built from `H, H', H''` reconstructed from the
recurrence remainder, NOT from `V_series` itself -- a genuinely independent route). On item 5(b),
Codex caught its OWN earlier framing as underspecified: "`chi(tau)` was not actually defined by
the material you quote... the representation has a phase-gauge ambiguity... you should not
implement a de-chirp from `w`'s asymptotic alone." A real, useful self-correction rather than
patching over the gap. Gave a calibrated overall verdict: "for the checked portion of formula
(A) -- especially the `R_N=1-1/(12N)+o(1/N)` claim -- the evidence is now very strong... I would
stop spending rounds hunting a leading-order failure... That is still numerical confirmation, not
a proof." On item 5/the Conjecture-3 bridge: "looks plausible but is a genuinely separate
analytic obligation."

**Independently verified item 5(a), and it holds cleanly at every precision-limited data point**
(`round16_item5a_check.py`): reconstructed `H'(w)`, `H''(w)` SPECTRALLY (not by finite differences,
per Codex's own warning that finite differences would be swamped at the relevant `e^{-dr}` scale)
from the already-verified `Hhat(m)` Fourier coefficients (`m=1..4`), built `A(r)` and tested
`Q(r) := (V_series(r)-A(r)) * e^{2r}/r^2`. **First attempt found `delta:=V_series(r)-A(r)` was
`O(1)`, not tiny -- traced immediately to this project's own implementation error** (comparing
`V_series(r)/r^2` against `A(r)` when the correct comparison, per Codex's own formula, is
`V_series(r)` [undivided] against `A(r)` directly, both representing `r^2*K''(r)`). **After
fixing: `delta` shrinks explosively fast** (`0.0045` at `r=5` down to `6.8e-15` at `r=20`), and
**`Q(r)` holds at a clean, stable, near-exact value of `~4.0`** at `r=5,10,20`
(`4.000363, 4.000000, 3.993589`) before hitting a genuine PRECISION FLOOR at `r>=30` (not a real
blow-up: increasing this project's own working precision from 40 to 120 digits did NOT push the
floor further, confirming the limitation is the cached `Hhat(m)` values' own intrinsic precision
`~1e-17` to `1e-18`, documented from round 1, not a flaw in the formula or this check). **A real,
clean, independent confirmation of item 5(a)'s `K''(r)` expansion.**

**Item 5(b) remains genuinely open** -- not merely unimplemented but, per Codex's own honest
retraction, not yet even well-posed (the "chirp" `chi(tau)` needs a real derivation from the exact
complex asymptotic before any numerical test can be built; Codex flagged that if the true
asymptotic is simply `E(tau)=H(w(tau))+o(1)` with `chi=0`, no de-chirping is needed at all, but
this hasn't been established either way).

## Status after round 16: formula (A) now very strongly supported; item 5(a) independently
confirmed; item 5(b) (the last piece needed to rigorously refute literal Conjecture 3 itself) is
the one genuinely remaining open question

**Formula (A)** (the saddlepoint/tilted-CLT approximation for `phi` itself): all five original
closure items now have real derivations; four are independently verified exactly or numerically
to high confidence (items 1, 2, 3 exact; item 4 confirmed via two independent full numerical
reimplementations agreeing to several significant figures on a nontrivial `-1/12` asymptotic
rate, itself only reached after catching and fixing this cycle's FOURTH real sign/scaling bug).
Codex's own calibrated read -- "very likely right... not a proof" -- matches this project's own
assessment. This is now a strong, largely-verified, near-complete proof, appropriate to present in
a paper as "proof modulo routine write-up of items 1-3 (independently verified) and item 4
(independently confirmed numerically, asymptotic rate matching two separate implementations)."

**Item 5** (the bridge from formula (A)/the `K(s)`-level periodicity to a rigorous statement about
literal Conjecture 3 itself): part (a) now independently confirmed; part (b) is the single
remaining genuinely open technical question in this entire cycle, not yet even fully well-posed.
Until it resolves, the refutation of literal Conjecture 3 remains -- per this project's own
correction after the critique round -- a strong, structurally-grounded, numerically-supported
claim, not a proven theorem. Reported to the researcher 2026-08-04, who asked for an Opus
max-effort deep dive on item 5(b) specifically, replacing Codex for this one question.

## Opus max-effort deep dive on item 5(b), 2026-08-04: a real resolution, plus a significant new finding that reframes the whole refutation

**Bottom line, from a genuinely independent, from-scratch derivation (not a review of Codex's
work, a fresh solution to the actual open problem): the item-5(b) gap closes, there is no gauge
ambiguity, and a sharper, more citable statement of what "Conjecture 3 is false" means falls out
along the way -- along with an honest, precisely-located account of what's still needed.**

### The derivation

Working directly from `K(s)=Q(w)+H(w)+o(1)` (Fact 1, already established) and formula (A) (Fact
2), Opus derived, via a variational (envelope) argument rather than naive substitution of
`w(tau)` into `H` (naive substitution amplifies the `O(log(tau)/tau)` uncertainty in `w(tau)` into
an `O(log tau)` error -- genuinely divergent; the envelope method avoids this because the error
enters only at the level of a stationary-point correction, second order):

```
log(phi(e^{-tau})) = P(tau) + H(w_0(tau)) + O(log^2(tau)/tau),   w_0(tau) := tau + log(tau/c)
```

with `P(tau)` an explicit smooth (non-oscillating) function. **This IS the "chirp"**: not a
separate multiplicative phase factor (Codex's `e^{i*chi(tau)}*H(w(tau))` framing was, per Opus,
"a category error -- H is not a single Fourier mode, so a multiplicative complex phase is not the
right object"), but a real, additive, explicit phase argument `w_0(tau)` inside `H` itself. Proved
a uniqueness lemma (any two oscillation-free/periodic-residual decompositions of `log(phi)` must
agree up to `o(1)` and a shift in `c*Z`) showing this is NOT subject to any gauge freedom.

**Independently cross-checked, three ways, all matching to extreme precision**: (1) re-derived
`H`'s Fourier coefficients via an independent Mellin-inversion route (not copying the project's
own DFT-based `Hhat` computation), matching the already-established closed form exactly, AND
producing a NEW closed form for `Hhat(0)` (the mean of `H`) that the project didn't have before,
involving `pi^2/12 - gamma_E^2/2 - gamma_1`; (2) matching `P(tau)`'s expansion against the already-
established Berg-Kruppel exponents `gamma, delta` gives agreement to all 17 reported digits, plus
a clean new identity `gamma+delta = -(1/c+1/2)`; (3) the constant `C(lambda=1) =
exp(const_P+H(0))` matches this project's already-established, independently-Mellin-derived
`C_bare=0.204987710306627` to `7.5e-14` relative error.

**This project's own independent verification, using ONLY already-validated exact machinery from
round 1 (`H(w):=L(w)-Q(w)` via the true `K_series`-based `L`, NOT Opus's own code, NOT the
truncated Fourier reconstruction), confirms the single most important and novel claim decisively**
(`round17_phase_locking_check.py`): computed `H(theta)` exactly at `theta = -log(lambda) mod c`
for `lambda = 1.0, 1.2, 1.5, 1.8, 2.0, 2.4, 2.8, 3.0`. **`H(lambda=1)` exactly equals
`H(lambda=3)`** (to 40-digit precision, confirming the multiplicative-period-3-in-`lambda`
structure exactly as required), **and the pattern is genuinely non-monotonic, peaking near
`lambda~1.8-2.0`** (values: `-0.627131` at `lambda=1`, rising to `-0.626717` at `lambda=1.8`,
falling back to `-0.627131` at `lambda=3`), **with a spread of `4.135e-4`, matching Opus's
independently-reported oscillation amplitude (`~4.19e-4`) closely.** Also hand-verified the
`gamma+delta=-(1/c+1/2)` identity algebraically from this project's own already-established
`gamma, delta` formulas (round 3 of the first cycle): exact symbolic cancellation confirms it.

### The reframing: Wirsching's own comparison class is "phase-locked"

**The key new insight, not something either this project or Codex had identified before**:
Wirsching's actual comparison sequence `z_l ~ l*3^{-l}` corresponds to `lambda=1` FIXED as
`l->infinity` -- along this specific family, `w_0(tau_l) mod c` FREEZES at a constant phase
(`-log(lambda) mod c`, here `0`), rather than sweeping through all phases. Consequences, precisely
stated:

- **Statement A (unrestricted `t->0`)**: genuine non-convergence, with a RIGOROUSLY nonzero
  amplitude bound `osc(H) >= 2*|Hhat(1)| = 2.0937e-4` (via `Gamma`/`zeta` nonvanishing, already
  established), giving `limsup/liminf >= e^{osc(H)} = 1.000418...`.
- **Statement B, the sharp/citable one**: for `z_l = lambda*l*3^{-l}`, fixed `lambda`, the limit
  `phi(z_l)/phi_0(z_l) -> C(lambda)` EXISTS for each fixed `lambda`, but `C(lambda)` is a genuine,
  nonconstant, real-analytic, multiplicatively-3-periodic function of `lambda` (independently
  confirmed above). **This means literal Conjecture 3 (a single constant across the whole
  comparison class) is false, provided that class contains more than one value of `lambda`** --
  and it directly explains why this project's own earlier `l=5..500` numerical sweep (all at
  `lambda=1`, i.e. `t_l=l*3^{-l}` exactly) saw clean, convergent-looking evidence FOR the
  conjecture: it was only ever sampling one frozen phase.
- **Statement C, the repair**: a uniform POSITIVE two-sided bound survives across the whole class
  (`C_min <= phi/phi_0 <= C_max`, differing by only `0.042%`), which may be all Wirsching's actual
  argument needs (an open question, since this project has not reread L-002's exact usage of
  Conjecture 3 to confirm whether the exact constant or just positivity is required) -- if so, the
  honest headline becomes "literal Conjecture 3 is false, but the corrected/weaker statement
  Wirsching's own proof needs is TRUE," a more defensible and more citable outcome than a bare
  refutation.

**A concrete, precisely-specified numerical test was also given** (a "de-oscillated surrogate"
construction that structurally cancels the smooth part rather than subtracting it, since naive
subtraction is swamped by `O(log^2(tau)/tau)` truncation error at any practically reachable `tau`)
along with reported results (a clean sinusoid at the predicted amplitude/phase in a fixed-depth
phase sweep; a decisive `lambda`-dependence test at `l=300,1200` matching the predicted `H` values
to 1-2.5% at practically reachable depths, converging at the predicted `O(log(l)/l)` rate) -- not
yet independently reproduced by this project (the phase-locking confirmation above already gives
strong independent support for the underlying claim by a different, cheaper route).

### What Opus itself flagged as still needed (honest, not overclaiming)

1. **Formula (A) needs to hold at TWO DISTINCT PHASES, not one.** This sharpens item 1's earlier
   concern precisely: the existing proof (rounds 10-15) is built along `r_N=3^N/2`, a SINGLE fixed
   phase -- by the very phase-locking mechanism just established, a formula-(A) proof confined to
   one phase is logically compatible with Conjecture 3 being TRUE. The good news: only the
   qualitative `o(1)` form of (A) is needed at a second phase (e.g. `r_N=lambda*3^N/2` for one
   more `lambda` with `H(-log(lambda_1)) != H(-log(lambda_2))`), not the specific `O(1/N)` rate --
   so this is a smaller ask than the full formula (A) proof, but a real, unaddressed gap.
2. **Wirsching's primary source (L-002) needs rereading**: does the comparison class
   `tilde A_{delta_5}` actually contain a nondegenerate range of `lambda` (needed for the
   refutation to bite at all), and does the section-7 step need the exact constant or only
   positivity (determining whether the honest headline is "refutation" or "refutation + repair")?
   Not yet checked.
3. **The oscillation is not numerically observable in `phi` itself at any practical precision**
   (would need `t ~ 10^{-200}`, since formula (A)'s own `O(1/N)` error only gets overtaken by the
   `~4e-4` periodic signal around `N~400`). This is a theorem-level result, to be stated as such,
   not something a numerical plot of `phi` will ever show directly.

**Status**: item 5(b) is now resolved with a real, substantially verified derivation and a
genuinely new, sharper, more citable reframing of the whole refutation. Two concrete follow-ups
remain before the full Conjecture-3 refutation is airtight (formula A at a second phase; rereading
L-002's exact usage), both smaller and more tractable than what's already been done. Reported to
the researcher 2026-08-04.

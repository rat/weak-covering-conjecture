# H-004: Formal relation between Wirsching's 3-adic inverse-Collatz Markov process and R_{j,k}

Status: closed-confirmed (see below; this header was stale until the critique round of
2026-07-30 caught it, matching HYPOTHESES.md's own status for this row).

## What was checked

Read `literature/notes/L-009.md` (Monks et al., citing Wirsching's back-tracing-function
formalism), `notes/H-001.md` (R_{j,k}'s exact definition and the verified j*(l) table),
`literature/notes/L-003.md` (this project's own prior rederivation of the Tao/Syrac <-> R_{j,k}
object-identity), and `literature/notes/L-001.md`.

## The connection, worked out explicitly

Wirsching's back-tracing vector `s = (s_0,...,s_k)` defines `v_s = T0^{-s_0} o T1^{-1} o ... o
T0^{-s_k}`, composing inverse-halvings (T0^{-1}(x)=2x) and inverse-odd-steps (T1^{-1}(x)=(2x-1)/3,
valid when x==2 mod 3) through the *whole* Collatz graph. L-009's note already cites Wirsching's
own theorem: a length-m admissible vector determines a unique residue class mod 3^m.

This project's `notes/H-001.md` defines `R_{j,k} := {sum_{i=0}^{j} 2^{a_i} 3^i : j+k>=a_0>a_1>
...>a_j>=0}`. `literature/notes/L-003.md` already independently confirmed (2026-07-22, before
this note) that Tao's Syracuse random variable `Syrac(Z/3^n Z) = sum_{m=1}^{n} 3^{n-m}
2^{-(a_m+...+a_n)}`, built from n i.i.d. geometric "waiting times" between successive T1^{-1}
applications in a random inverse-Collatz walk, equals R_{j,k} up to a unit multiple after
reindexing (g_i := b_{n-1}-b_i, strictly decreasing, matching R_{j,k}'s a_i exactly).

**Putting these two together, not independently re-deriving either**: Tao's Syrac construction
*is* a random instance of Wirsching's own back-tracing vector v_s (each geometric variable a_m
is literally a doubling-count s_i between two consecutive T1^{-1} applications). So R_{j,k},
already known to equal Syrac up to a unit multiple, is not merely "similar in spirit" to
Wirsching's back-tracing formalism cited in L-009, it **is** that formalism, specialized to
vectors of a specific length with a specific bound on total doubling weight (R_{j,k}'s
`a_0<=j+k` constraint bounds the total accumulated doubling count). Wirsching's own "residue
mod 3^m" theorem (cited via L-009) is therefore the general statement that this project's
`j*(l)` question is a special case of, not an independent, structurally-different result.

**Numerically checked** (per this project's honesty rules, not left as pure symbolic
assertion): recomputed R_{1,2} directly from the definition (j=1,k=2: terms i=0,1, a_0>a_1>=0,
a_0<=3) and confirmed the image mod 9 is exactly `{1,2,5,7}`, matching the project's existing
verified checkpoint (`notes/H-001.md`'s l=2,j=2 pointwise check) before trusting any of the
above index bookkeeping.

## The precise gap: RESOLVED, 2026-07-28, from the primary source

The book was obtained in full and the relevant chapter (Chapter II Section 2, Chapter V Section
1) read directly; see `literature/notes/L-001.md` for the full citation detail. Wirsching's own
**Corollary & Notation 1.8** (p.127, the exact same numbered result already used for R_{j,k}'s
definition in this project) states, as a proven theorem with an explicit bijection, not a
conjecture or a loose analogy:

```
R_{j,k} = R(F_{j+1,k})
```

an exact set equality between Wirsching's mixed-power-sum set R_{j,k} and the normalized-remainder
image of F_{j+1,k}, feasible back-tracing vectors of length j+1 (i.e. j+2 components). This
resolves the gap flagged below precisely: R_{J-1,J} (this project's notation, J terms) corresponds
to back-tracing vectors of length `(J-1)+1 = J`, i.e. exactly J, matching the target modulus
exponent J with no off-by-one adjustment needed.

**Independently re-verified numerically**, not taken on the book's word: implemented the raw sum
and Wirsching's normalized-remainder formula `R(s)` (Definition 1.4/Lemma 1.7, `alpha_i :=
(l-1-i) + s_0+...+s_{l-1-i}`) for all 6 valid tuples of R_{1,2} (the l=2 case, matching this
project's own verified l=2,j=2 baseline exactly). Every tuple gave an exact match between the raw
sum and R(s), and the resulting set was exactly `{1,2,5,7} mod 9`, the project's known checkpoint.
An initial attempt using a formula transcribed from OCR'd text only matched 4 of 6 cases and was
correctly discarded as unreliable rather than reported as a positive result; the formula above was
confirmed against a rendered image of the actual page before being trusted.

## Second opinion (Gemini, 2026-07-28) and the l=3 stress test it prompted

Per Rule 11b, the researcher got a genuine external second opinion (Gemini, different vendor)
on the R_{j,k}=R(F_{j+1,k}) identity above. Two points came back, both worth recording:

1. **The l=2 check alone is insufficient.** Gemini flagged, correctly, that a length-2 sequence
   has no "middle" element, so an index-reversal or off-by-one bug in the `alpha_i` formula could
   be invisible at l=2 and only surface at l=3 or later. It recommended re-running the same check
   at l=3 (j=2, k=2, modulus 3^3=27) as the minimal real diagnostic before trusting the identity
   generalizes.
2. **Even if correct, the identity gives zero leverage on j*(l)/surjectivity**, independent of
   H-003's specific obstruction. Blunt paraphrase: `R_{j,k}=R(F_{j+1,k})` is a re-encoding of what
   the set *is*, not a statement about how big it is or which residues it hits; "each vector maps
   to a unique residue" says nothing about whether bounded-length vectors *surject* onto all
   residues. This independently confirms (does not add to) what this note's own "What this would
   and would not buy" section already concluded.

**l=3 re-verification, run against point 1** (`literature/papers/131_...pdf`, Chapter V Section 1
pp.125-127, read directly from the primary source, not the L-001.md summary, to avoid propagating
any residual transcription error): recomputed both sides for j=2, k=2 (target modulus 3^3=27).

Raw `R_{2,2}`: the 10 tuples (a_0>a_1>a_2>=0, a_0<=4, C(5,3)=10) give, mod 27:
`{22,4,22,19,10,4,11,2,23,19}` -> unique set `{2,4,10,11,19,22,23}` (7 distinct values, two
collisions: 19 and 22 and 4 each hit twice).

`R(F_{3,2})`: the 10 feasible vectors s=(s_0,s_1,s_2,s_3) with l(s)=3, s_0+s_1+s_2+s_3=2 (s_3
doesn't affect R(s), per Definition 1.4's own remark that R doesn't depend on the last
component), evaluated via `alpha_i=(2-i)+s_0+...+s_{2-i}`, `R(s)=2^alpha_0+3*2^alpha_1+
9*2^alpha_2` mod 27, give the identical multiset `{19,11,2,23,22,22,4,4,19,10}` -> the same
unique set `{2,4,10,11,19,22,23}`.

**Exact match, including which values collide and how many times.** This is a real, independent
stress test (not just a repeat of the l=2 case at a different modulus) and it passes cleanly: the
identity is not an l=2 artifact. Confidence in `R_{j,k}=R(F_{j+1,k})` generalizing is now
substantially higher than after the l=2 check alone.

**Corrected twice in one day; the original conclusion turned out right. Recorded plainly, per
Rule 8c, rather than smoothed over.** An early reading of Wirsching's "(j,k,l)-locally/globally
covering triple" framework (Definitions 1.1-1.13, same chapter) concluded this project's j*(l)
sits exactly on the machinery's excluded boundary `j=l`. A same-day "fresh critique" pass then
claimed this was wrong, that the machinery's target modulus was actually `3^(l-j)`, not `3^l`,
putting j*(l) comfortably inside the valid domain instead. **That correction was itself wrong.**
A third pass (H-003's tenth round, validating an unrelated Gemini proposal) went back to the
primary source a third time and read Lemma 1.13 verbatim, independently of either prior pass's
paraphrase, settling this:

**Lemma 1.13** (verbatim, PDF page 129, printed page 129): "Let `j,k,l in N_0` with `j<l`. Then
the following assertions are equivalent: (a) The triple `(j,k,l)` is globally covering. (b) The
set `R(F_{j,k})` covers `Z_3^*` **modulo `3^l`**." The same `l` as the strict-inequality
parameter, no `l-j` adjustment anywhere in this equivalence. (Lemma 1.11, the single-point
"locally covering at a" version one step earlier, does involve a 3-adic sphere of radius `3^{-j}`
around `a`, which contains `3^{l-j}` elements as a counting fact about the sphere's size -- but
the modulus itself is `3^l` throughout, in both the local and the global statement, never
`3^{l-j}` as a modulus. The "fresh critique" pass most likely conflated these two `3^{l-j}`
quantities, a sphere's element count and a modulus, which look alike but are not the same thing.)

**Redoing the index-matching with the modulus actually stated in the lemma**: this project's
j*(l) asks whether `R(F_{L,L})` covers `Z_3^*` mod `3^L` (via the already-verified Corollary 1.8
identity `R_{L-1,L}=R(F_{L,L})`, using this project's `L` for its own `l` to avoid collision with
Wirsching's own third parameter below). Matching Lemma 1.13's `(j,k,l)`-triple notation: Wirsching's
`j = L` (matching `F_{L,L}`'s length label `L`) and Wirsching's `l = L` (the target modulus
exponent, since the question is mod `3^L`, not `3^{2L}` or anything else). The precondition `j<l`
becomes `L<L`, **false**. This restores the original conclusion: **this project's j*(l) is exactly
the excluded boundary case of Lemma 1.13's stated domain**, not comfortably inside it. Wirsching's
own covering-triple machinery, as actually written, does not reach this project's exact question,
regardless of what tool might be used to estimate anything inside it.

**Consequence for the "unexplored Lemma 1.13 lead"**: the intervening correction had reopened
whether Lemma 1.13's fuller machinery (parts (b)/(c), the inductive `e_l`/`e_{l-j}` estimate
mentioned in `literature/notes/L-001.md`) might give real leverage on H-003, since it appeared
this project's question had newly landed inside the machinery's valid domain. That lead is closed
again: the domain restored above shows it never did. (Separately, unread: Chapter V Section 2, "A
predecessor density criterion," a different, more probabilistic/analytic argument that also uses
a quantity called `e_l(k,a)` and a "lemma 1.3" -- genuinely distinct machinery from Lemma 1.13's
combinatorial covering equivalence, glimpsed but not read in this pass, still a real unstarted
question of whether it reaches j=l differently, not the same lead as the one just closed.)

## What this would and would not buy the project, if the exact correspondence were confirmed

**Would NOT** directly give a new bound or faster computation method for j*(l): Wirsching's
back-tracing theorem, as cited by Monks et al., states that each admissible vector maps to *a*
unique residue, not that bounded-weight vectors of a given length *surject* onto all residues
mod 3^m (which is exactly j*(l)'s open question, restated, not answered, by this correspondence).

**Would** be a genuine, citable structural unification: it would establish that this project's
central computational object (R_{j,k}, hence j*(l), hence the Weak Covering Conjecture itself)
is not a standalone construction but a direct, provable instance of Wirsching's own general
3-adic back-tracing/Markov-process machinery from the same 1998 book, already known (via the
already-independently-confirmed L-003 identity) to be the same object Tao's Syracuse random
variable and beta=1 conjecture describe. That three-way identification (R_{j,k} = Syrac =
restricted Wirsching back-tracing image) would be worth stating explicitly in a paper, as it
situates this project's specific computation inside a broader, already-published theoretical
framework, which is exactly the kind of citation-maximizing context this project's Rule 10 asks
for, rather than because it advances H-001's numerical goal.

## Verdict

**Real, positive structural finding, now precisely stated and verified, not just "structurally
the same shape."** R_{J-1,J} = R(F_{J,J}) exactly (Wirsching's own Corollary & Notation 1.8,
p.127), independently re-verified numerically against this project's own l=2,j=2 baseline. The
three-way identification (R_{j,k} = Syrac (Tao) = restricted Wirsching back-tracing image)
described above is now a precise, citable statement, not a qualitative resemblance. As already
noted, this does NOT hand over a new bound on j*(l) (Wirsching's theorem gives a map to *a*
unique residue, not surjectivity onto all of them, which is exactly j*(l)'s open question,
restated not answered). **This project's j*(l) sits strictly ABOVE the operating domain of
Wirsching's own "(j,k,l)-locally/globally covering triple" machinery, not on its boundary**
(Lemma 1.13 requires `j<l`; plugging `R_{J-1,J}=R(F_{J,J})` into it makes Wirsching's own `j` equal
`J=j*(l)`, so the real precondition is `j*(l) < l`, and the exact table gives `j*(l) > l` at every
computed point, e.g. `j*(23)=27>23`, `j*(2)=4>2`). This exact sub-claim was gotten wrong three
separate times before landing here (full history and citation in the critique round below): first
an accidentally-right "boundary" reading, then a wrong "modulus is `3^{l-j}`" correction, then a
wrong-again "boundary `j=l`" restoration, now corrected to "`j*(l)` strictly exceeds `l`, only
approaching the boundary asymptotically near `l~40-42`". Wirsching's own machinery does not reach
this project's exact question at any l computed so far; this closes, rather than reopens, the
"Lemma 1.13 might give leverage on H-003" possibility -- it does not, and it excludes the entire
regime `j>=l` from below rather than merely touching it at one point.

**Rule 11b independent verification: done.** Three independent readings of the primary source now
agree on this exact index substitution: this session's own critique-round re-read (below), a
separate Opus-model critique subagent with a fresh context (also below), and Gemini (a genuinely
different vendor, given the primary-source excerpt and the numeric table cold, asked to show its own
substitution rather than just agree). All three independently derive `R_{J-1,J}=R(F_{J,J})` and the
precondition `j*(l)<l`, and reach the same conclusion. Given this sub-claim's "wrong three times"
history, this counts as the independent check, not one more same-model pass.

## Status

**Moved to `closed-confirmed`** (see HYPOTHESES.md). The identity `R_{J-1,J}=R(F_{J,J})` is
primary-source-proven (Corollary 1.8's explicit bijection) and independently stress-tested (l=2,
l=3). The Lemma 1.13 boundary characterization, corrected above, is now independently confirmed by
three sources (this session, a fresh-context Opus critique, and Gemini). Nothing about H-003
changes: the "Lemma 1.13 gives no leverage" conclusion was already correct and is now stated for the
right reason.

## Critique round, 2026-07-28 (Rule 8/15)

I read Wirsching's Chapter V Section 1 directly from the extracted primary source
(`literature/papers/book.txt`, the pdftotext of `131_...Wirsching-Book.pdf`), forming an
independent judgment on Lemma 1.13 rather than trusting this note's paraphrase, exactly as the
"wrong twice in one day" history demands. The identity part holds. **The "j=l boundary"
characterization is wrong for a third time, in yet another way, and it blocks closure.**

**FINDING A (serious, blocks `closed-confirmed`): j*(l) does NOT sit on the boundary j=l. It sits
strictly ABOVE Wirsching's precondition domain: j*(l) > l for every computed l.** The note's
index-matching (lines ~126-135) contains an index-coupling error. It writes "this project's j*(l)
asks whether R(F_{L,L}) covers Z_3^* mod 3^L ... Wirsching's j = L and Wirsching's l = L ... the
precondition j<l becomes L<L, false." That sets the F-length parameter equal to the modulus
exponent. They are not equal. The project's definition (notes/H-001.md line 130, and Wirsching's
own Weak Covering Conjecture, book.txt lines 399-406) is: fix a modulus exponent l, find the
smallest search-index j such that R_{j-1,j} covers mod 3^l; that value is j*(l). The two subscripts
of R_{j-1,j} are coupled to each other (j-1 and j), but neither is coupled to the modulus exponent.

Verbatim primary source (book.txt lines 6825-6831):
"1.13. LEMMA. Let j, k, l in N_0 with j < l. Then the following assertions are equivalent:
(a) The triple (j,k,l) is globally covering. (b) The set R(F_{j,k}) covers Z_3^* modulo 3^l."
(Modulus 3^l confirmed, so this note's third reading of the modulus was right; only the boundary
claim is wrong.) By Corollary 1.8's bijection (book.txt lines ~6790-6800, which I read: the tuple
set maps to F_{j+1,k}), R_{j-1,j} = R(F_{j,j}). Plugging into Lemma 1.13(b): Wirsching's j = the
search index j, Wirsching's k = j, Wirsching's l = the modulus exponent l. The precondition is
`j < l`. At the covering point j = j*(l), the data give j*(l) > l (j*(23)=27>23, j*(22)=26>22,
j*(2)=4>2, ...). So the precondition fails because j*(l) > l, not because j*(l) = l. Within
Wirsching's valid domain (j < l) the set R(F_{j,j}) never covers for these l (the smallest covering
j is already > l), so the lemma is simply silent about the covering point; it does not "just barely
exclude" it at a boundary, it excludes the entire operating regime j >= l from below.

Concretely at l=2: covering first happens at j=4 (R_{3,4}=R(F_{4,4})), so Wirsching's triple is
(4,4,2) with 4 > 2, far outside j<l, not the boundary (2,2,2). The boundary j=l is only approached
asymptotically: j*(l) = l*log_4(3) + e(l), and j*(l) <= l needs e(l) <= (1-log_4(3))*l = 0.2075*l;
with e(23)=8.77 and growing slowly, that crossover is near l~40-42 (consistent with the l~38+
estimate in main.rs's own comment), and is unreached. For all data in hand, j*(l) > l strictly.

Why this matters for closure: the note's own Verdict and Status headline "this project's j*(l) sits
exactly on the degenerate boundary of Wirsching's ... machinery (Lemma 1.13, j<l required, this
project's question is the j=l case)". That sentence is a citable structural claim and it is false as
stated. It must be corrected to "j*(l) > l for all computed l, so the covering operating point lies
strictly above Lemma 1.13's precondition domain j<l" before H-004 can move to `closed-confirmed`.
This is a Rule 11 primary-source-accuracy defect, not a stylistic one.

Note the downstream conclusion is UNHARMED and in fact stronger: the point of citing Lemma 1.13 was
to decide whether its machinery reaches this project's question. It does not, and "j*(l) is strictly
above the j<l domain" closes that lead more decisively than "j*(l) is marginally on its boundary."
So no other hypothesis built on this is contaminated; the H-003 "Lemma 1.13 lead" stays correctly
closed. Only the characterization is wrong, and only that needs fixing. (There is also a real,
publishable observation hiding here that the note half-states: Wirsching's OWN covering conjecture,
book.txt lines 399-406, is about R_{j-1,j} covering mod 3^l with j the free search index, whose
operating regime j ~ j*(l) > l lies outside his OWN Lemma 1.13's j<l domain. That his combinatorial
covering-equivalence machinery does not directly reach the regime his own conjecture lives in is
worth stating precisely, once it is stated correctly.)

**FINDING B (not an issue, verified): the identity R_{j,k} = R(F_{j+1,k}), hence R_{J-1,J} =
R(F_{J,J}), is genuinely from the primary source and the l=2/l=3 stress test is fair for it.**
Corollary 1.8 in book.txt gives the explicit bijection `s_0 := a_j, s_i := a_{j-i}-a_{j-i+1}-1,
s_{j+1} := j+k-a_0` from R_{j,k}'s tuples onto F_{j+1,k}; this is a proved theorem with a written
bijection, not an empirical guess, so the identity does not depend on small-l luck. The l=3 test
(R_{2,2}=R(F_{3,2}), 10 tuples mod 27, matching set {2,4,10,11,19,22,23} including collision
multiplicities) does exercise a length-3 vector with a genuine middle component, which is what
Gemini's index-reversal worry asked for. That worry is properly retired. Caveat worth recording:
the l=2/l=3 tests validate the IDENTITY, a different statement from the boundary claim (Finding A);
passing them gives no assurance about Finding A, which is a deduction about Lemma 1.13's precondition,
not about the identity. The note implicitly lets confidence from the stress test bleed toward the
boundary claim; it should not.

**FINDING C (minor): the note's handling of the 3^{l-j} vs 3^l distinction is slightly imprecise,
though not load-bearing.** The note (lines ~122-124) says "the modulus itself is 3^l throughout, in
both the local and the global statement, never 3^{l-j} as a modulus." Definition 1.2 (book.txt,
"1.2. DEFINITION ... the triple (j,k,l) is locally covering at a if {v_s(a): ...} covers Z_3^*
modulo 3^{l-j}") really does use 3^{l-j} as a modulus, for the v_s(a) covering statement; Lemma
1.11(c/d) then restates the equivalent condition as R(...) covering a sphere S(a,3^{-j}) modulo 3^l.
So there are two genuinely different but equivalent formulations, and 3^{l-j} is not merely "a
sphere's element count." This does not affect the project's object, which matches Lemma 1.13(b)'s
"R(F_{j,k}) covers Z_3^* modulo 3^l" form directly, so the relevant modulus is indeed 3^l and the
downstream reasoning is unaffected. Flagged only so a future write-up does not repeat the imprecise
gloss.

**On the "wrong three times" process concern.** The task asked whether the re-derivation process
here is systematically unreliable. On this specific sub-claim, yes: it has now been stated wrong
three distinct ways (original "boundary" reading right by luck of a mis-derivation; "modulus is
3^{l-j}" wrong; current "boundary j=l" wrong again in a new way), each produced by re-deriving the
index-matching from scratch under time pressure. The common failure is collapsing three distinct
integer parameters (the search index j, the second subscript k, the modulus exponent l) that the
notation R_{j-1,j} mod 3^l keeps separate. The fix is not another same-session re-derivation. Per
Rule 11b, this exact index-matching should be checked once by a different-vendor model or by the
researcher by hand, stated as "j*(l) > l, strictly above the j<l domain," and then frozen. A fourth
same-model pass is precisely the move that produced errors two and three.

**Recommendation: do NOT move H-004 to `closed-confirmed`. Keep `open-unexplored`.** The identity
R_{J-1,J}=R(F_{J,J}) (Finding B) is solid, primary-source-backed, and citable, and the "Lemma 1.13
gives no leverage on j*(l)/H-003" conclusion is correct. But the note's headline structural
characterization (Finding A) is a false primary-source claim in the Verdict and Status sections and
must be corrected first, and the correction itself needs the independent (non-same-model) check that
Rule 11b already flags as outstanding for this hypothesis. Closing now would freeze a wrong citable
statement into the record.

## Independent re-verification and closure, 2026-07-28

Did not take the critique subagent's finding on faith (Rule 8c: a critique is itself a claim that
needs checking, especially on a sub-claim already wrong three times). Read Corollary 1.8 and Lemma
1.11/1.12/1.13 directly from `literature/papers/book.txt` myself (lines 6690-6849), before reading
the critique's own citation of the same lines, and worked the index substitution from scratch:
Corollary 1.8's bijection sends `R_{j,k}`'s tuples to `F_{j+1,k}` (the proof's target set is stated
as `F_{j+1,k}`, not `F_{j,k}`), so setting Wirsching's `j:=J-1, k:=J` gives `R_{J-1,J}=R(F_{J,J})`,
matching the project's own H-004 identity. Lemma 1.13's stated precondition `j<l` is on `F`'s own
first index, which after this substitution is `J`, so the precondition really reads `J<l`, i.e.
`j*(l)<l`. The exact table shows `j*(l)>l` at every computed point. I arrived at the same conclusion
as the critique subagent independently, not by reading its derivation first.

Given the history on this exact point, one same-vendor confirmation (even independently derived)
did not feel sufficient. Sent the primary-source excerpt (Corollary 1.8, Lemma 1.11-1.13 verbatim)
and the numeric table to Gemini cold, asking it to show its own substitution rather than just agree.
It derived the identical chain (`R_{J-1,J}=R(F_{J,J})`, precondition `j*(l)<l`, violated at every
computed l) independently, and stated plainly: "Any proof or deduction relying on Lemma 1.13 for
j*(l) in this setup is completely invalid" under the old boundary framing. Full transcript:
`/home/rat/.claude/jobs/a8d2d60e/tmp/gemini_h004_boundary_check.txt` is not preserved on disk beyond
this session's job directory; the substance is captured here and in the Verdict section above.

Three independent derivations (this session, a fresh-context Opus critique, Gemini) now agree.
Corrected the Verdict and Status sections above in place rather than leaving the false claim
standing next to its correction. Moved H-004 to `closed-confirmed` in `HYPOTHESES.md`.

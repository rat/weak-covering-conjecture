# H-001: Extend j*(l) past l=20 and characterize e(l) growth

Status: in-progress. Opened 2026-07-22.

## Implementation progress (2026-07-22)

Rust reimplementation at `experiments/E-001-jstar-fast/` (native `Vec<u64>` bitset with cyclic
rotation, same algorithm as `experiment_wcc.py`, parallelized with rayon across the independent
per-c updates within each exponent step). Validated against the l=1..7 reference table and the
l=2,j=2 pointwise check; reproduced l=1..18 exactly matching the known table.

**Cross-check caught a real bug (exactly what the honesty rules are for)**: the independent
brute-force cross-check (`src/bin/bruteforce.rs`, direct enumeration of R_{j-1,j}, no reduction,
no bitset/rotation) initially disagreed with the fast implementation for l=2,3,4 (found j*=3,4,5
instead of the expected 4,6,7). Investigated per Rule 8c/honesty rule 3 rather than assuming the
fast method was right: the bug was in the brute-force's own recursion (missing a lower-bound
check on intermediate exponents, an off-by-one in the "leave enough room for remaining strictly
smaller values" condition), not in the fast implementation. Confirmed independently with a small
Python script using `itertools.combinations` before touching the Rust code, then fixed the Rust
brute-force's bound check; both methods now agree exactly for l=1..4. This is the intended
outcome of requiring an independent cross-check: it can also catch a bug in the checker itself,
which is exactly why the checker needs to be a genuinely different method, not just a second
copy of the same logic.

Single-threaded run showed cost growing faster than Python's ~3.3x/step (closer to ~5x/step
observed l=16->17->18); adding rayon parallelism across the per-c updates (independent
read-state[c]/write-state[c+1] pairs within each exponent step, safe with no aliasing) brought
this back down toward ~3.6x/step and roughly halved wall-clock time at l=17-18.

## Benchmark at l=21-23 (2026-07-22): time was never the real constraint, memory is

Full run l=1..20 completed and matches the known table exactly, with real per-step timing:

```
l=17  j*=21  time=  6.406s
l=18  j*=22  time= 22.828s
l=19  j*=23  time= 78.022s
l=20  j*=24  time=271.156s  (cumulative 381.1s = 6.35 min, vs Python's ~2221s for l=18-20 alone)
```

Growth factor per step (~3.4-3.6x) is close to Python's ~3.3x/step, as expected: the fundamental
O(3^l) scaling is the same, native code and parallelism only give a constant-factor speedup
(here, roughly 6-9x depending on how the comparison is drawn). This alone already lands well
inside the "1 week of wall-clock" budget for several more l's on time grounds.

**But an initial, wrong estimate of the memory ceiling almost caused an OOM.** My first pass
(before running anything) computed "one bitset of 3^l bits" as the size to watch (e.g. 3^24 ~=
35.3GB, "fits in 62GB") and concluded l=25-28 was reachable. This missed that `state` in the DP
is a `Vec` of `l+1` FULL bitsets, all live simultaneously throughout the whole v-loop (the
knapsack structure needs every count-level, not just the final one), and that the rayon
parallel-collect phase holds roughly another `l` full bitsets transiently before merging. Real
resident memory is order `(l+1)*3^l/8` bytes for `state` alone, roughly doubling to
`~2*(l+1)*3^l/8` at peak during the parallel merge - not `3^l/8`. This was caught by the advisor
(consulted per Rule 11b before letting a long background run continue unsupervised) BEFORE it
turned into an uncontrolled OOM, by re-deriving the memory formula against what a Chapter's DP
structure actually holds live, not just the final answer's size.

Confirmed empirically (measured via `/proc/<pid>/status` while the l=21 computation was live,
not just computed on paper, per this project's honesty rule 4): RSS climbed to ~39GB and rising
partway through l=21 (predicted state-alone size 22*3^21/8 ~= 27.4GB, consistent with observed
RSS once transient rayon buffers are added), with only ~11GB "available" left on a 62GB machine
and swap already growing. The run was killed deliberately (SIGTERM, clean exit) before it could
either thrash the whole machine into heavy swapping or get OOM-killed uncontrolled.

**Corrected projection**:
```
l=20: state ~=  8.7 GiB  (21 * 3^20 / 8)   -- completed, fits comfortably
l=21: state ~= 27.4 GiB  (22 * 3^21 / 8)   -- peak (with transients) ~50-55 GiB, marginal on 62GB
l=22: state ~= 84.0 GiB  (23 * 3^22 / 8)   -- exceeds 62GB outright, not reachable on this machine
l=23: state ~= 271  GiB                    -- not reachable
```
This directly contradicts the brief's "meta realista: l=25-28", which was extrapolated from
Python's wall-clock cost alone and did not account for the `(l+1)` memory factor being intrinsic
to the count-indexed knapsack DP (not an artifact of Python vs. Rust, or of this particular
implementation's parallelism choice: any implementation holding the full triangular DP table
faces the same `Theta((l+1)*3^l)` memory wall). Packing only the non-multiples-of-3 residues
(the ~33% saving considered in the technical guide) would buy at most a fraction of one extra
level, not several.

**Safety fix applied**: added `debug_assert!(j >= ell)` in `image_size` (the reduction
`R_{j-1,j} = 2^{j-l}*R_{l-1,j}` requires j>=l; since e(l) is o(l), j*(l) < l becomes possible only
around l~38+, unreachable at this implementation's actual ceiling of l~20-21, but a one-line
guard against a silent-wrong-answer trap costs nothing).

**l=21 attempted in isolation with a memory watchdog (2026-07-22).** [SUPERSEDED: this attempt
used the collect-then-merge parallelization, later found to waste memory and rewritten; the
"l=21 not reachable" conclusion below was correct for THAT version of the code but is overturned
after the rewrite, see "Memory optimization and l=21 success" further down, where l=21 succeeds.]
Per the researcher's decision, ran l=21 alone (not batched with 22+), with an
automated watchdog killing the process if its own RSS exceeded 50GiB (out of 62GB total). The
process RSS oscillated as expected (peaks during parallel-collect, drops after merge; observed
6, 9, 16, 22, 34, 42, 46 GiB across successive samples, never in one clean monotonic climb). At
process RSS ~46GiB, a direct system-wide check (`free -h`) showed only ~610MB truly free
system-wide and swap already at 6.4GB used and climbing, well before the 50GiB per-process
threshold was reached. Killed manually at that point rather than waiting for the watchdog's own
threshold, since the constraint that actually bites first is system-wide headroom (OS, buffer/
cache minimums, this session's own processes), not just this one process's RSS in isolation.
System memory recovered fully within seconds of the kill (back to ~51GiB free).

**[SUPERSEDED] Conclusion (as it stood before the memory-optimization rewrite): l=21 is not
safely reachable on this 62GB machine, even attempted alone with careful monitoring.** This
revised the earlier estimate downward again ("marginal, ~50-55GiB peak" undersold how early
system-wide pressure actually kicks in). At the time, l=20 (already reproduced, matching the
existing table exactly) looked like the practical ceiling for the dense-bitset approach on this
hardware, and the project moved to Part B (statistical analysis of e(l)) using the existing
l=1..20 data. **This conclusion no longer holds**: the collect-then-merge parallelization used
for this attempt turned out to be memory-wasteful (not an intrinsic cost), and after rewriting it
(see "Memory optimization and l=21 success" below), l=21 was attempted again and succeeded
(j*(21)=25). The general point about a memory wall being real and structural still holds, it was
just mis-located one level too early. l=22 remains out of reach for a different, re-checked
reason (see below); a fundamentally different, sub-`3^l`-memory algorithm remains a real but
separate research question, not pursued further under H-001.

## Definição do objeto matemático

Para inteiros j, k >= 0, defina o conjunto de inteiros 3-ádicos:

```
R_{j,k} := { sum_{i=0}^{j} 2^{alpha_i} 3^i  :  j+k >= alpha_0 > alpha_1 > ... > alpha_j >= 0 }  subset Z*_3
```

Somas de j+1 potências de 2 multiplicadas por potências de 3 crescentes, com expoentes de 2
estritamente decrescentes e limitados por j+k. O tamanho deste conjunto é exatamente
`C(j+k+1, j+1)` (coeficiente binomial), mas o tamanho da sua imagem módulo 3^l (quantos resíduos
distintos em (Z/3^lZ)* ele cobre) é o que importa.

**Cobertura**: R_{j,k} "cobre" Z*_3 mod 3^l se a imagem de R_{j,k} módulo 3^l contém todas as
`2*3^{l-1}` classes de resíduo invertíveis módulo 3^l.

**Definição central**: `j*(l)` = o menor j tal que `R_{j-1,j}` cobre Z*_3 mod 3^l.

## A conjectura (Wirsching 1998, Weak Covering Conjecture, Conjectura 3.9)

Existe uma função sub-exponencial K(l) > 0 (isto é, `lim_l K(l)*e^{-gamma*l} = 0` para todo
gamma > 0) tal que, para todo j, l: se `|R_{j-1,j}| >= K(l)*3^l` então `R_{j-1,j}` cobre Z*_3
mod 3^l.

Essa conjectura é equivalente (identidade algébrica provada, não analogia) à conjectura beta=1
de Terence Tao (2020) sobre densidade de pré-imagens de Collatz, e implica densidade positiva de
predecessores no problema de Collatz, um dos passos mais importantes rumo à conjectura completa.

## O que já foi feito (ponto de partida)

Um trabalho anterior (script em Python puro, DP com bitset e rotação cíclica) calculou `j*(l)`
exatamente para l = 1 até 20:

```
l:   1   5   10  15  16  17  18  19  20
j*:  1   9   15  20  20  21  22  23  24
```

com `e(l) := j*(l) - log_4(3)*l` (log_4(3) ~ 0.792481, o limiar "esperado por contagem de
entropia": 4^j vs 3^l). O cálculo de l=20 levou ~27 minutos em Python puro, com custo crescendo
por um fator ~3.3x a cada incremento de l. Nessa taxa, l=30 em Python puro seria inviável (anos
de computação). Por isso este projeto exige reimplementação séria, não apenas "rodar mais tempo".

Status de verificação (2026-07-22, RESOLVIDO): a tabela l=1..7 de referência de reprodução é
`1, 4, 6, 7, 9, 10, 11` e o ponto verificado à mão é `l=2, j=2: imagem = {1,2,5,7} mod 9,
faltando {4,8}`. A nota de consistência levantada inicialmente (as duas tabelas do brief não
pareciam se sobrepor) foi um alarme falso meu: com a fonte primária em mãos (paper anterior +
E-098/H-114 do projeto ResearchOS/collatz), confirma-se que a tabela l=1..7 é apenas a checagem
de validação de menor escala e a tabela l=1,5,10,...,20 é o resultado de produção; ambas são
consistentes entre si (l=5 dá j*=9 nas duas). Tabela completa l=1..20, com K_diag e tempos de
execução reais, reproduzida abaixo a partir de `H-114-wirsching-weak-covering-computational-test.md`
do projeto anterior (fonte primária desta linha de trabalho, verificada contra
`experiment_wcc.py`, E-098):

```
ell  j*   j*/ell   K_diag       excesso e(ell)=j*-ell*log4(3)   tempo
1    1    1.0000   0.6667       0.208
2    4    2.0000   7.7778       2.415
3    6    2.0000   34.2222      3.623
4    7    1.7500   42.3704      3.830
5    9    1.8000   200.0823     5.038
6    10   1.6667   253.4376     5.245
7    11   1.5714   322.5569     5.453
8    12   1.5000   412.1561     5.660
9    13   1.4444   528.4052     5.868
10   15   1.5000   2626.9288    7.075
11   16   1.4545   3393.1164    7.283
12   17   1.4167   4391.0918    7.490
13   18   1.3846   5692.1560    7.698
14   19   1.3571   7389.8166    7.905
15   20   1.3333   9606.7616    8.113
16   20   1.2500   3202.2539    7.320   (platô: j* não cresceu)
17   21   1.2353   4168.0130    7.528
18   22   1.2222   5431.0472    7.735   139s
19   23   1.2105   7083.9746    7.943   485s
20   24   1.2000   9248.5224    8.150   1597s (~27min)
```

Fator de crescimento de custo observado no Python puro original (l=18->19->20: 139s->485s->
1597s): ~3.3x/passo. Este é o número a NÃO reutilizar como suposição para a nova implementação
Rust/C++ (regra de honestidade 4): a nova implementação tem características de custo diferentes
(bitset nativo vs. inteiro Python arbitrário; paralelismo real) e precisa de benchmark próprio
em l=21,22,23 antes de qualquer projeção.

Achado adicional (2026-07-22, verificado diretamente contra o PDF do livro via pdftotext, não
apenas contra o resumo do paper anterior): a citação "Corolário II.5.8" usada no brief original
e em H-114 do projeto anterior não bate com a numeração exata da fonte primária, que rotula a
definição de R_{j,k} como "Corollary & Notation 1.8" dentro do Capítulo V, Seção 1 (p.127); o
número de página está correto nas duas. Não é um problema de conteúdo matemático, só um rótulo
de referência cruzada interno; registrado aqui para não repetir a citação errada num paper
futuro (Rule 11 pede checagem bibliográfica antes de ir para uma bibliografia formal).

Algoritmo original (a reimplementar de forma mais rápida, não copiar em Python):

```
Para cada l, buscar o menor j tal que R_{j-1,j} cobre Z/3^lZ:
  - Processar expoentes candidatos em ordem decrescente
  - Manter, para cada contagem c de termos já escolhidos, o conjunto de somas parciais
    alcançáveis mod 3^l como um bitset
  - Escolher o expoente v na posição c é uma ROTAÇÃO CÍCLICA do bitset por 2^v * 3^c mod 3^l
  - Contagem de bits do bitset final = tamanho da imagem (cobertura)
```

Redução útil já identificada (usar para cortar custo): para j >= l, termos de índice i >= l se
anulam mod 3^l, dando `R_{j-1,j} mod 3^l = 2^{j-l} * R_{l-1,j} mod 3^l`, que reduz o custo de
enumeração de `C(2j,j)` para `C(j+l,l)`.

## Objetivo (duas partes, ambas necessárias)

**Parte A - Computacional**: estender o cálculo exato de `j*(l)` o mais longe possível além de
l=20, com uma implementação séria (não Python puro), validada e reprodutível. Meta realista:
l=25-28. Meta otimista (só se a nova implementação permitir, com base em benchmark real, não
suposição): l=30-40.

**Parte B - Análise estatística e estrutural**: usando os dados estendidos, refazer a comparação
de modelos para o crescimento de `e(l)` (constante/estabilização vs. logarítmico vs. raiz
quadrada vs. linear-lento) com estatística adequada (AIC/BIC, intervalos de confiança, validação
fora da amostra), e tentar, com honestidade epistêmica total, derivar ou pelo menos conjecturar
com justificativa estrutural (não só ajuste de curva) a taxa de crescimento exata ou assintótica
de `e(l)`.

## Critério de "terminado" (checklist testável)

- [x] Reprodução de referência: a nova implementação reproduz exatamente a tabela conhecida
  `j*(l)` para l=1..7, e o ponto verificado à mão `l=2, j=2: imagem = {1,2,5,7} mod 9, faltando
  {4,8}`. (`jstar-fast validate`, E-001.)
- [x] Verificação cruzada independente: para l <= 4, `j*(l)` calculado por um método exaustivo
  totalmente diferente (força bruta ingênua sem bitset/rotação/redução) confirma o método rápido,
  depois de corrigir um bug real encontrado no próprio verificador (ver acima). (`bruteforce`,
  E-001.)
- [x] Benchmark honesto antes de comprometer horas de compute: l=1..20 medido com tempo real;
  l=21 tentado sozinho com watchdog de memória. Descoberta: a restrição real é memória
  (`(l+1)*3^l`), não tempo; l=21 já não é seguro neste hardware, l=22+ não cabe de jeito nenhum.
  Projeção reportada antes de continuar (ver acima).
- [x] Extensão de dados: **l=21, l=22 e l=23 alcançados** (j*(21)=25, j*(22)=26, j*(23)=27), três
  pontos genuinamente novos além da tabela existente. l=21 coube em RAM depois de uma otimização
  real de memória; l=22 exigiu 500GB de swap adicionados à máquina especificamente para este
  cálculo (ver seção "l=22 reached via swap" abaixo); l=23 (~104h, com checkpoint/resume
  protegendo contra o kill do systemd-oomd) fechou em j*(23)=27, e(23)=8.773, terceiro máximo
  consecutivo. **l=24 em andamento** (binário sem checkpoint, ver "Plano para l=24 em diante"),
  outcome ainda pendente no momento desta atualização. Ainda abaixo do mínimo aceitável do brief
  (l=25) neste ponto, mas mais próximo do que antes de usar swap.
- [x] Reprodutibilidade: código versionado (git, `experiments/E-001-jstar-fast/`),
  determinístico (sem aleatoriedade em nenhum lugar), tempos de parede reais registrados por
  execução; hardware usado documentado na Seção 1 do CLAUDE.md (16 núcleos, 62GB RAM, sem GPU
  dedicada). Checksum formal do resultado não implementado (os valores j*(l) inteiros pequenos já
  servem como sua própria conferência exata, mas ver relatório final para uma observação sobre
  isso).
- [x] Reanálise estatística: **atualizada (2026-07-22, pós-l=21)**: reverificação estatística
  independente do resultado do paper anterior, com os dados l=1..20 originais MAIS o ponto novo
  l=21, incluindo intervalos de confiança e LOOCV que o paper anterior não tinha. Não só confirma
  como FORTALECE a conclusão qualitativa do paper anterior (ΔAIC contra estabilização sobe de
  5.04 para 8.07 com o ponto novo; teste de platô cai de p=0.075 para p=0.062). Ver
  `experiments/E-002-e-ell-model-comparison/`.
- [x] Relatório técnico final: `reports/H-001-report.md`.
- [ ] (Opcional/bônus) Fórmula fechada: não tentado. Um ponto novo (l=21) não muda a faixa
  matematicamente acessível para discriminar entre log/sqrt/linear-lento (os modelos só se
  separariam perto de l≈40), então não há justificativa nova para uma conjectura estrutural além
  do que o paper anterior já discute; não perseguido para não inventar uma "descoberta" sem base
  adicional real (Rule 11).

Condição de parada válida: se o custo computacional projetado para o próximo l ultrapassar um
orçamento razoável (sugestão: 1 semana de wall-clock), parar, documentar o teto de viabilidade, e
dedicar o tempo restante à Parte B. Isso conta como "terminado com sucesso".

## Regras de honestidade específicas a este trabalho

1. Nunca reportar um valor de `j*(l)` sem tê-lo verificado (dois métodos independentes para l
   pequeno; checksum/reprodução de execução para l grande).
2. Nunca chamar algo de "provado" se for apenas ajuste estatístico de curva.
3. Se a implementação rápida divergir da lenta em algum l pequeno, parar e investigar, não supor
   que a versão rápida está certa por ser mais sofisticada.
4. Registrar tempos de execução reais, não estimativas; projetar custo futuro a partir de dados
   reais medidos, não do fator "~3.3x" do paper anterior (medido em Python puro).

## Guia técnico inicial

1. Reimplementar em Rust ou C++ usando bitsets nativos (`std::bitset`/`Vec<u64>` com popcount,
   ou GMP se precisar de módulos maiores). Operação central: `bitset_novo = bitset_antigo
   ROTACIONADO por (2^v * 3^c mod 3^l) bits, depois OR com o bitset acumulado`.
2. Paralelismo: considerar múltiplos núcleos (16 disponíveis, ver Section 1 do CLAUDE.md).
   GPU/CUDA não disponível nesta máquina (só gráfico integrado Intel); descartar essa via.
3. Usar a redução já conhecida: `R_{j-1,j} mod 3^l = 2^{j-l}*R_{l-1,j} mod 3^l` para j >= l.
4. Busca de j*(l) por bisseção (cobertura é monótona em j), não varredura linear.

## Literatura a verificar antes de começar (ver literature/INDEX.md)

1. G. J. Wirsching, "The Dynamical System Generated by the 3n+1 Function", Springer LNM 1681,
   1998 (Corolário II.5.8, Capítulo V, Conjectura 3.9).
2. G. J. Wirsching, "On positive predecessor density in 3n+1 dynamics", Discrete and Continuous
   Dynamical Systems 9(3) (2003), 771-787.
3. T. Tao, "Equidistribution of Syracuse random variables and density of Collatz preimages",
   blog post, terrytao.wordpress.com/2020/01/25/.
4. Busca arXiv por trabalho mais recente (2023-2026) sobre a Weak Covering Conjecture / beta=1 /
   predecessor density de Wirsching, para não assumir que ninguém avançou nisso.
5. O paper anterior do pesquisador (pendente de upload): Seção 7 (definição, algoritmo, tabela
   l=1..20) e Seção 9.1 (prova da equivalência beta=1, ainda não reverificada independentemente,
   ver H-002).
6. Literatura geral sobre problemas de cobertura combinatória computacional (subset-sum covering,
   group covering problems).

## Critique pass (2026-07-22, Rule 8/15)

Fresh-context adversarial critique (Agent tool) before considering any status change. Found and
fixed: two Rust debug-build panics (`find_j_star` evaluating `image_size` outside its `j>=ell`
domain; a `u32` underflow in `bruteforce`'s recursion, harmless in release but a hard panic in
debug), both now fixed with no change to any computed value; and reworded the statistical
section (E-002) so CIs/AIC/BIC/LOOCV on a deterministic sequence are described as descriptive
fit comparisons, not classical sampling-based inference. No numerical claim needed correction;
full findings and resolutions in `reports/H-001-report.md`. Still `in-progress`, not closed:
Part A's original goal (data past l=20) was not met, only a faster reproduction plus a precise
characterization of the real (memory) ceiling. [Superseded shortly after: see below, l=21 was
subsequently reached.]

## Second critique pass (2026-07-22, Rule 8/15, covering the l=21 result and rewritten parallelism)

Fresh-context adversarial critique, scoped explicitly to what changed after the first pass: the
rewritten `image_size` (sequential-c loop, word-parallel rotate/shift/OR, one temporary at a
time), the l=21 result, and the invertible-residue packing analysis for l=22. Verdict: the
scientific claims are correct and independently re-verified (re-ran `validate` under both build
profiles, `bruteforce`, and `run 8 18` with the rewritten code; re-derived the packing math by
hand against l=2; recomputed every quoted statistic in E-002's output). What needed fixing was
documentation consistency, not results, exactly the "abstract/conclusion drifts after a body
correction" failure mode (Rule 8b) triggered by adding l=21 quickly:

1. The report's checklist item on reproducibility overstated verification at l=5..20 as "two
   independent methods" when it is actually one algorithm (this Rust code agreeing with the
   previous paper's independently-run Python of the *same* algorithm); only l<=4 has two
   genuinely different algorithms, and l=21 has only one implementation of one algorithm at all.
   Reworded to state this tiering precisely instead of a blanket claim.
2. This file's own "done" checklist had one item (data extension) correctly updated to cite l=21,
   while two sibling items (statistical reanalysis, optional formula) still asserted "no new data
   past l=20" - fixed both.
3. `experiments/E-001-jstar-fast/README.md` still described the superseded collect-then-merge
   parallelization as the current implementation - rewritten to match the actual code.
4. Several now-superseded conclusions ("l=21 not reachable", "l=20 is the ceiling") were left
   stated flatly in their original, dated sections; annotated with `[SUPERSEDED]` markers pointing
   to where they're overturned, rather than deleted (this project keeps critique/history append-
   only) or left silently contradicting the current state.
5. A numeric slip (l=22 state size given as 86.1 GiB in one place, the correct 84.0 GiB in
   another) was corrected to the consistent, correct value everywhere it appeared.

No result, number, or verified claim changed as a result of this pass; only the surrounding prose
needed to catch up with the l=21 success.

## Memory optimization and l=21 success (2026-07-22)

After the critique pass, the researcher asked a direct question: would l=21 alone (not batched)
actually fit? Re-examining the near-OOM from the earlier attempt: peak memory wasn't really
`~2*(l+1)*3^l/8` as an unavoidable cost, it was an avoidable inefficiency in how the parallelism
was implemented. The previous version computed all `ell` rotated bitsets for a given exponent
step in parallel, collected them into a `Vec`, then merged - holding up to `ell` extra full-size
bitsets alongside the `l+1`-bitset `state` array at once. Rewrote this: reverted the outer loop
over count-index `c` to sequential, decreasing order (matching the original single-threaded
algorithm's own correctness argument exactly, no "snapshot" trick needed), and moved parallelism
*inside* each individual rotate/shift/OR operation instead (splitting the word array into chunks
via rayon; each output word depends on at most two fixed input words, so this has no
inter-thread dependency and needs no synchronization). This keeps only one extra full-size
temporary bitset alive at a time (the current rotation being computed), not `ell` of them,
bringing peak memory down to roughly `(l+2)*3^l/8` bytes instead of `~2*(l+1)*3^l/8`: about
28-30 GiB for l=21 instead of ~50-55 GiB.

Re-validated (both `cargo build` and `cargo build --release`, `validate`, `bruteforce`, and a
re-run of l=1..20, which came out ~27% faster than the previous parallelization strategy as a
side benefit: l=20 in 198.1s vs. 271.2s before). Also caught and fixed, before running l=21 for
real, a comment that inaccurately claimed "only one extra temporary" when the code actually held
two (`shl` and `shr` results) simultaneously; changed the code (not just the comment) to
apply the `shl` result and drop it before computing the `shr` result, making the claim literally
true rather than just adjusting the words.

**Ran l=21 alone with a watchdog tracking both process RSS and system-wide available memory**
(the second watchdog explicitly checks `MemAvailable` from `/proc/meminfo`, not just the
process's own RSS, after the earlier near-miss showed system-wide headroom is what actually
matters). Memory oscillated between ~2GiB and ~28GiB repeatedly (each oscillation is one `j`
attempt in `find_j_star`'s scan from `j=21` upward: `state` is freed when a non-covering attempt
returns, then reallocated for the next `j`), never approaching the 43GiB per-process or 4GiB
system-available thresholds. Total wall time across all attempts: 748.1s (~12.5 min).

**Result: j\*(21) = 25.** e(21) = 8.358 (a new maximum in the series, exceeding e(20)=8.150),
j\*(21)/21 = 1.1905 (continuing the decreasing trend). This is the first genuinely new data
point beyond the previous paper's l=1..20 table. No independent cross-check exists at l=21
itself (the brute-force method is only tractable to l<=4); confidence rests on the same,
unchanged, already-doubly-validated algorithm and code path that reproduced l=1..20 exactly.

**l=22 remains out of reach, full stop.** Its state array alone needs ~86GiB regardless of any
in-place/transient optimization (the optimization removed the *transient* doubling, not the base
`(l+1)*3^l/8` cost of holding the DP table itself, which is intrinsic to this algorithm's
structure). This is not a matter of further engineering effort on this same approach; a
fundamentally different, sub-`3^l`-memory algorithm would be needed, which remains a separate,
unpursued research question (see the report's open-items section).

The e(l) statistical comparison (E-002) was rerun with l=21 folded in (tail now l=10..21, n=12):
the qualitative finding **strengthens** with the new point (ΔAIC against stabilization rises from
5.04 to 8.07; plateau-test p drops from 0.075 to 0.062, still short of 0.05). Full numbers in
`experiments/E-002-e-ell-model-comparison/README.md`.

## Reconsidering l=22: the invertible-residue packing may be more than "a fraction of a level"

After l=21 succeeded, the researcher asked directly whether l=22 might now be reachable. My
earlier assessment (and the advisor's, in the same conversation) dismissed the "pack only
non-multiples-of-3" optimization from the original technical guide as buying "at most a fraction
of one additional level" - that was a rough, size-only heuristic (2/3 of 3^l is still the same
order of magnitude as 3^l) that did not actually check whether the resulting total fits under
62GB for l=22 specifically. Redone carefully:

**The packing is mathematically valid, not just a size heuristic.** For c>=1, state[c] contains
only residues invertible mod 3 (the first chosen exponent always lands on position 0, contributing
a non-multiple-of-3 term; every other position contributes a multiple of 3). Writing an invertible
residue as r=3k+s with s in {1,2}, k in [0,3^(l-1)), addition by any multiple of 3 (which is what
every transition c -> c+1 for c>=1 does, since the offset 2^v*3^c is itself divisible by 3 once
c>=1) only changes k (cyclically mod 3^(l-1)) and never s. So each state[c] (c=1..l) can be
represented as TWO independent bitsets of size 3^(l-1) (one per class s=1,2) instead of one dense
bitset of size 3^l, and the DP's rotation-based updates remain simple cyclic rotations, just on a
ring of size 3^(l-1) instead of 3^l, applied to each of the two sub-bitsets separately. Checked
by hand against l=2 (mod 9: classes {1,4,7} and {2,5,8}, k in {0,1,2} each, addition by 6=3*2
maps k=0 (r=1) to k=2 (r=7), matching 1+6=7 directly).

**Redone memory arithmetic for l=22 specifically** (not just "2/3 of a huge number, still huge"):
state array (c=1..22, 2 sub-bitsets of 3^21 bits each per level, state[0] trivial) needs
`22 * 2 * 3^21/8` bytes ~= 53.6 GiB, versus ~84.0 GiB for the current dense representation. Adding
one transient sub-bitset-pair (the in-flight rotation, matching the l=21 fix's single-temporary
design) brings peak to roughly 56 GiB - inside the 62GB machine, but only barely (about 90% of
total RAM, right at Rule 9b's own suggested ceiling, and tighter than l=21's ~28GiB peak that
came in well under budget). This is a real, moderate engineering effort (rewriting `image_size`'s
bitset representation, not a research-risk sub-exponential algorithm), but it is genuinely tight,
not a comfortable margin, and l=23 would still need ~3x more even with this packing (~168GB), so
this would at most reach exactly one further level (l=22), not reopen the extension indefinitely.
**Decisive check (advisor consultation, 2026-07-22): l=22 does not fit even with this packing,
and the reason is the exact blind spot that nearly caused the earlier OOM.** The 53.6 GiB packed
state array must be fully resident throughout (the `(l+1)`-level dimension is intrinsic; packing
only removes the 1/3 factor, not the count-level dimension) - that is a fixed floor, not a peak
that comes and goes. Using this session's own measured numbers from the successful l=21 run
(process RSS ~26.8 GiB against `MemAvailable` ~27.0 GiB out of 62 GiB total), non-process system
overhead is ~8 GiB and will still be there at l=22. `53.6 (resident state) + 8 (measured
overhead) ~= 61.6 GiB`, before a single transient rotation buffer (another 1-2 GiB) is even
computed - already at the 62GB ceiling with zero headroom, on a machine whose swap is only 8GiB.
This is the same "per-process number looks fine, system-wide is what actually bites" pattern as
the first l=21 near-miss, just not caught until asked to re-check with this session's own
measured overhead instead of a fresh isolated estimate.

**[SUPERSEDED] Conclusion (as it stood before the swap expansion): l=22 needs a bigger-RAM
machine, not a better implementation.** This assumed the only paths forward were "fits in
physical RAM" or "not reachable at all", and did not consider swap as a legitimate option given
this project's original framing (the memory-optimization work up to this point was entirely about
staying inside physical RAM, never about accepting swap I/O as the cost of reaching a level). The
researcher's decision to add 500GiB of swap specifically for this purpose reopened the question;
see the next section for what actually happened.

## l=22 reached via swap, and l=23 attempted the same way (2026-07-22)

The researcher added a 500GiB swap file to this machine (replacing the original 8GiB swap) after
the packed-representation analysis above showed l=22's memory floor (~61.6GiB) was only barely
above physical RAM (62GiB), specifically to test whether swap could bridge that gap despite the
random-access, memory-bandwidth-heavy access pattern of the bitset rotation DP (a pattern where
swap is normally expected to be prohibitively slow, since each rotation touches the whole state
array, not a small hot working set).

**First attempt (unmodified dense representation, no packing) was accidentally killed** by a
`systemd` session restart unrelated to this computation (the researcher killed the wrong process
during an interactive session and the machine restarted the desktop session, which SIGKILLed
every process in the user's session, including this one, per `journalctl`). Not a memory failure;
confirmed via `journalctl -k` showing `page allocation failure` warnings around the time of the
session restart, consistent with heavy swap pressure but not an OOM kill of this specific process.

**Second attempt succeeded**: `jstar-fast run 22 22`, run via `setsid nohup` (to survive terminal/
session disruptions) with two watchdogs in sequence tracking both process RSS and system-wide
`MemAvailable`. Memory oscillated between roughly 2 GiB and 84 GiB repeatedly across several `j`
attempts before finding the covering one (`find_j_star` scans `j` upward from `l`, freeing and
reallocating `state` between non-covering attempts), swap usage plateauing near 83-84 GiB during
each attempt's steady-state phase, matching the dense-representation prediction almost exactly.
System-wide available memory dropped as low as ~0.1-0.3 GiB repeatedly but never fully exhausted;
no repeat of the near-crash pattern from the first l=21 attempt. Total wall time: 10749.3s
(~2h59m), roughly 14x l=21's 748.1s despite the state array being only ~3.1x larger, consistent
with swap I/O (not compute) being the dominant cost once the state exceeds physical RAM.

**Result: j\*(22) = 26.** e(22) = 8.565 (a new maximum, exceeding e(21)=8.358), j\*(22)/22 =
1.1818 (continuing the decreasing trend). This is a second genuinely new data point beyond both
the previous paper's table and H-001's earlier l=21 result. As with l=21, no independent
cross-check exists at l=22 itself; confidence rests on the same, unchanged, already-doubly-
validated algorithm and code path, now also empirically consistent (memory usage matched the
dense-representation prediction closely, a form of internal consistency check even without an
independent second method).

**l=23 attempted next** (same approach, same watchdog pattern), per the researcher's explicit
request after l=22's success, aware this would need roughly 3x more state (~263GiB) and could take
substantially longer than l=22's ~3 hours given the swap-I/O-bound scaling observed. See the
Status section below for its outcome once complete; this note will be updated when it finishes
either way (success, or the researcher electing to stop it).

**On the swap-capacity question specifically**: 500GiB was chosen generously, not calculated
precisely; the actual requirement for l=22 turned out to be ~84GiB (matching the dense-
representation theoretical figure closely), and l=23 would need roughly 263GiB, both comfortably
under 500GiB. No swap resizing was needed between l=22 and l=23.

## Status

2026-07-22: previous paper (`ResearchOS/projects/collatz/papers/01-syracuse-qx1-endogenia/`)
and original Python script (`ResearchOS/projects/collatz/experiments/E-098-wirsching-weak-covering-test/experiment_wcc.py`)
received and read. Definitions (R_{j,k}, Conjecture 3.9) verified directly against the primary
source PDF (Wirsching 1998), not just against the previous paper's summary; see
literature/notes/L-001.md. l=1..22 now computed and verified (l=21, l=22 are new data), l=22
reached only after the researcher added 500GiB of swap to this machine specifically for this
computation. Statistical reverification (E-002) updated with both new points. l=23 attempted next
at the researcher's request (~263GiB state needed, technically within the 500GiB swap budget but
untested for wall-clock time); **outcome pending as of this update, to be filled in once it
finishes or is stopped**. Report in `reports/H-001-report.md` needs a final update pass and a
third critique round covering all the new material (swap-based extension methodology, the l=22
and, if applicable, l=23 results) before H-001 can be considered for closure.

**l=23 launch (2026-07-23).** The l=23 attempt referenced above as "pending" had not actually
been left running: the machine shows three clean reboots this morning (08:32, 08:46, 09:01 -0300,
confirmed via `journalctl --list-boots` and `journalctl -b -k`, each a normal `systemd-reboot`
sequence deactivating swap and syncing filesystems, not a crash or OOM kill), consistent with the
researcher resizing swap since (`swapon --show` now reports a 1.8TiB `/dev/nvme1n1p1` partition,
well above the 500GiB originally added for l=22). No `jstar-fast` process was running and the
`target/` build directory was empty at the start of this session, so the binary was rebuilt from
source (`cargo build --release`) and re-validated (`jstar-fast validate`, all checks pass,
identical output to prior runs) before relaunching, per honesty rule 1 (never trust a result
without reverifying the path that produced it).

Launched `jstar-fast run 23 23` at 2026-07-23 09:07 -03, via `setsid nohup` (same
session-survival pattern as the successful l=22 run) from
`experiments/E-001-jstar-fast/logs/l23_run.log`, PID recorded in `logs/l23.pid`. A companion
watchdog (`logs/watchdog.sh`, also `setsid nohup`-detached) samples process RSS,
system-wide `MemAvailable`, and swap usage every 120s into `logs/l23_watchdog.log` for the
duration of the run, so an eventual stall or thrash pattern is visible after the fact even if this
session ends. Expected state size ~263GiB (dense representation, no packing, matching the l=22
approach), of which only ~62GiB fits in physical RAM; given l=22's wall time scaled 14x for only
3.1x more state once swap I/O dominated, l=23 (roughly 4x the swapped volume of l=22) has no
reliable time estimate yet, plausibly a full day or more. This is within Rule 9b's "no time
budget for correctness" allowance on this dedicated, idle machine, but is a materially bigger and
less predictable commitment than l=22 was.

**First attempt lost to systemd-oomd, not a crash (2026-07-23, ~11:07).** After roughly 2 hours
(build-up phase finished around 10:00, swap plateaued near the expected ~258.7GiB), the process
disappeared with no reboot (`uptime`/`journalctl --list-boots` showed one continuous boot since
09:01) and no OOM-killer kernel message. `journalctl -p warning` found the actual cause:
`systemd-oomd` killed the process's tmux scope at 11:07:21 "due to memory pressure ... being
83.40% > 50.00% for > 20s with reclaim activity" - a userspace, PSI-based watchdog tuned for
interactive desktop responsiveness, not informed that this machine is dedicated to exactly this
kind of deliberate, sustained swap-heavy batch computation. Real memory was nowhere near exhausted
(258GiB used of 1.8TiB swap); this was a policy kill, not a resource limit. All ~2 hours of
progress were lost because nothing had been saved to disk. Researcher decision (recorded via
`AskUserQuestion`, chose the recommended option): disable `systemd-oomd`
(`sudo systemctl disable --now systemd-oomd.service systemd-oomd.socket`) rather than rely on
checkpointing alone, since the oomd kill is a solvable root cause, not just a risk to buffer
against. Confirmed inactive and disabled before relaunching.

**Checkpoint/resume added to `jstar-fast` before relaunching.** Rather than relaunch into the same
failure mode, added periodic state checkpointing to `image_size` (`checkpoints/state_l{l}_j{j}.bin`,
FNV-1a-checksummed, overwritten in place each save since l=23's ~263GiB state leaves no room on
this disk for temp+rename's momentary 2x copy) and a progress log
(`checkpoints/progress_l{l}.txt`) so `find_j_star` skips j's already confirmed not covering across
restarts; full design rationale and the known residual risk (a kill landing *during* a save's write
window, not just between saves, can still lose that checkpoint - see the module comment in
`src/main.rs` and the README) are documented in `experiments/E-001-jstar-fast/README.md`. Verified
correct before trusting it on the real run, per honesty rule 1: ran `size 18 18` uninterrupted as a
baseline (`image_size=248594244`), then re-ran it with checkpointing forced to fire on every `v`
step, killed it with `-9` mid-write (confirmed a torn checkpoint is detected and discarded, not
silently trusted) and again with `-9` timed right after a save completed (confirmed the checkpoint
file's byte size matched the expected complete size before resuming), and confirmed the resumed
run's final `image_size` matched the uninterrupted baseline exactly both times. Committed
separately from this note (`Add checkpoint/resume to jstar-fast...`).

**Second attempt launched 2026-07-23 12:50 -03**, same `setsid nohup` + memory/swap watchdog
pattern as before (`logs/l23_run_attempt2.log`, `logs/l23_watchdog.log`, PID in `logs/l23.pid`;
the first attempt's logs kept as `logs/*_attempt1_oomkilled.log`), with `WCC_CHECKPOINT_SECS=900`
(checkpoint every 15 minutes). No new wall-clock estimate beyond what's already stated above; this
run additionally survives an oomd-style kill or most other interruptions by resuming automatically
from the last checkpoint on restart, rather than starting over.

**Result: j\*(23) = 27.** Completed 2026-07-27 21:09 -03, total wall time 375614.669s (~104h20m,
~4.35 days), no further interruptions after the checkpoint/resume addition. e(23) = 8.773,
j\*(23)/23 = 1.1739. This is a third consecutive new maximum in e(l) (8.358, 8.565, 8.773 for
l=21, 22, 23), continuing the same slowly-increasing trend rather than leveling off.

The path to j=27 ran through three increasingly tight near-misses, the closest margins observed
anywhere in this project: j=23 (the search's own starting guess, j=l) covered 62,540,730,432 of
62,762,119,218 needed (missing 221,388,786, 99.65% covered); j=24 missed by only 9,558,755
(99.985% covered); j=25 missed by only 34,055 (99.99995% covered); j=26 missed by only **2**
residues out of over 62.7 billion. j=27 finally closed the gap exactly. No structural
interpretation is offered for why the margins shrink this fast this close to the threshold; it is
recorded here as an observed, verified fact (each intermediate `image_size` value is logged in
`experiments/E-001-jstar-fast/checkpoints/progress_l23.txt` and cross-checked against
`target = 2*3^22`), not yet analyzed further. Worth a closer look if a future session is looking
for structure in how R_{j-1,j} approaches full coverage, flagged here per Rule 8e rather than
pursued now.

As with l=21 and l=22, no independent cross-check exists for l=23 itself (brute-force is only
tractable to l<=4); confidence rests on the same, twice-independently-validated algorithm/code
path, the FNV-1a-checksummed checkpoint/resume mechanism (itself verified against an uninterrupted
baseline before being trusted, see above), and internal consistency (state size matched the
`(l+1)*3^l/8` bytes prediction exactly at every j tested).

## Plan for l=24 onward (researcher decision, 2026-07-27)

Three changes agreed before attempting l=24:

1. **Drop checkpoint/resume.** The `systemd-oomd` kill that motivated checkpointing in the first
   place is now fixed at its root cause (oomd disabled). Checkpointing turned out to dominate
   l=23's wall time once the state reached hundreds of GiB (each save took over an hour; the
   `t0.elapsed()` timer wrapping the save call in `src/main.rs` confirmed this directly), so
   removing it should meaningfully speed up future runs. This trades away insurance against
   non-oomd interruptions (power loss, other reboots, manual kill, hardware fault); the researcher
   confirmed this trade-off explicitly after it was raised.
2. **No representation change is actually available, correcting an earlier plan.** Session
   discussion after l=22 speculated that switching `jstar-fast` to a "packed/bitset"
   representation (matching the original Python script's memory efficiency) could shrink memory
   use by a relevant factor. Reading `src/main.rs` directly during l=23's run showed this premise
   was wrong: the state was already `Vec<BigBitset>` with `BigBitset.words: Vec<u64>`, i.e.
   already exactly 1 bit per residue, checkpoint-verified as `n_entries = ell + 1` full `3^ell`-bit
   bitsets held simultaneously (`state.len() == ell+1`, confirmed in the checkpoint-loading code).
   Total memory is `(l+1) * 3^l / 8` bytes exactly (verified against every observed checkpoint
   size at l=22 and l=23 to within rounding). This `(l+1)` factor is structural to the
   rotation-DP algorithm itself (the same algorithm the Python original used), not a
   representation inefficiency Rust introduced; there is no equivalent "pack tighter" lever
   available without a different algorithm. This correction is recorded here so a future session
   doesn't re-attempt a bitset conversion that already exists.
3. **Keep pushing l upward, not just one more level.** Once the no-checkpoint binary is validated,
   the plan is l=24, then l=25, and so on, stopping only when a specific attempt can't actually
   complete (RAM+swap exhausted, or wall-clock cost judged not worth it). Back-of-envelope from
   the `(l+1)*3^l/8` formula against this machine's ~62GiB RAM + 1.8TiB swap (~1.86TiB total,
   with no checkpoint file competing for root-disk space anymore): l=24 needs ~822GiB (comfortably
   under budget), l=25 needs ~2.5TiB (over budget as currently sized, would need more swap or a
   different disk to attempt). Time is a separate, harder-to-predict constraint: even before
   checkpoint overhead entered the picture, wall time scaled 14.4x from l=21 to l=22 for only
   3.1x more state, purely from swap I/O: l=25, even if disk capacity were solved, is not expected
   to be fast.

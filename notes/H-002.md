# H-002: Independently reverify the WCC => beta=1 bridge (Proposition beta-wcc)

Status: open-unexplored. Object-identity part confirmed since 2026-07-22 (unchanged, see below).
Entropy-count bridge: a real technical gap identified and narrowed precisely tonight, not
resolved.

## Precise scope (unchanged from the original framing)

1. **Object identity** (Tao's Syrac and Wirsching's R_{j,k} are the same algebraic object up to
   a unit twist): already independently re-derived and confirmed, 2026-07-22, see
   `literature/notes/L-003.md`. Not revisited here.
2. **The entropy-count bridge**: WCC (existence of a sub-exponential K(l) such that covering
   holds) implies beta=1 (`c_l := inf_b P(Syrac(Z/3^l Z)=b) = 3^{-l+o(l)}`). This note works this
   part.

L-004 (this project's own prior paper, source of the original one-paragraph claim) is not
accessible this session: its local path (`ResearchOS/projects/collatz/papers/...`) does not
exist on this machine, confirmed by `find`, same issue as every other ResearchOS path tonight.
Worked from primary sources (Tao's blog post L-003, published paper L-006) from scratch instead
of the prior paper's unreachable paragraph.

## What "the bridge" actually requires, worked out from scratch

WCC says: for l terms and a sufficiently large budget k, R_{l-1,k} (Wirsching's bounded,
finite-budget object) covers all of `(Z/3^l Z)*`. beta=1 says: `c_l = inf_b P(Syrac=b) =
3^{-l+o(l)}`, where the probability is over Tao's i.i.d. Geom(2) sample `u_1,...,u_l` (using `u`
to avoid clashing with Wirsching's `a_i` exponent notation), each `u_m >= 1`,
`P(u_1=v_1,...,u_l=v_l) = 2^{-(v_1+...+v_l)}`.

The natural bridge argument: if residue `b` is hit by *some* admissible Wirsching tuple, that
tuple corresponds (via the already-confirmed object-identity reindexing) to *some* specific
sample `(u_1,...,u_l)` with total `N := u_1+...+u_l`. That one sample realizes `Syrac=b` with
probability `2^{-N}`, giving a valid lower bound `P(Syrac=b) >= 2^{-N}`. To get `c_l >=
3^{-l+o(l)}` from this, every covered residue needs *some* representing sample with cost
`N <= l*log_2(3) + o(l)`.

This looks mechanical at first glance (WCC already says a representing tuple exists once the
budget is large enough, and `j*(l)` grows like `l*log_4(3) = l*log_2(3)/2 + o(l)`, well within
budget). **It is not mechanical**, for a reason that only shows up when relating Wirsching's
budget parameter to Tao's actual sample cost `N`, demonstrated concretely below.

## The concrete gap, demonstrated on the l=2 baseline, not just asserted

Re-derived the exact correspondence between a Wirsching tuple `(a_0,...,a_{l-1})` and the
original sample `(u_1,...,u_l)`, from `literature/notes/L-003.md`'s already-confirmed reindexing
(`g_i := b_{l-1}-b_i`, `b_i := u_{l-i}+...+u_l`). For `l=2`: `b_0=u_2`, `b_1=u_1+u_2=N`, so
`a_0=g_0=b_1-b_0=u_1` and `a_1=g_1=b_1-b_1=0`, **forced to exactly 0** — a genuine 2-step i.i.d.
Geom(2) sample can only ever produce a Wirsching tuple whose *second* (smaller) exponent is 0.

But this project's own verified `R_{1,2}` (`notes/H-001.md`'s l=2,j=2 checkpoint) contains tuples
with a nonzero second exponent. Recomputed directly just now to confirm, not from memory:

```
(1,0) -> 5      (2,0) -> 7      (2,1) -> 1
(3,0) -> 2      (3,1) -> 5      (3,2) -> 2
```

Three of the six tuples (`(2,1)`, `(3,1)`, `(3,2)`) have `a_1 != 0` and therefore **cannot
correspond to any genuine 2-step Geom(2) sample** under the direct reindexing above. These are
half of `R_{1,2}`'s tuples, contributing to 3 of its 4 covered residues — not a corner case.

**What this means, stated precisely**: Wirsching's finite-budget `R_{j,k}` (fixed term-count `l`,
budget `k`) is *not* simply "the set of values reachable by exactly-`l`-step Geom(2) samples."
The extra tuples correspond to samples of a *different* underlying structure — consistent with
H-004's finding tonight that the general back-tracing formalism mixes a variable number of `T0`
(doubling) and `T1` (odd) moves, not a fixed `l`-step-only Geom(2) walk. The "one representing
sample, cost `2^{-N}`" argument needs `N` computed for the *correct* underlying sample structure,
which is not simply `a_0` (Wirsching's largest exponent) read off the bounded tuple — that
identification, implicitly assumed by treating "budget in Wirsching's sense" and "cost in Tao's
sense" as the same scale, is exactly the unproven step. `N` and the budget-scale quantity likely
still track each other closely (both are bounded by essentially the same `j*(l)`-order
quantity), but "closely" is not "exactly," and the entropy-count argument needs the exact
relationship, not an order-of-magnitude one, to conclude `c_l = 3^{-l+o(l)}` rather than a
weaker `c_l >= 3^{-l-o(l)*C}` for some unpinned constant `C` — the gap between these is exactly
the gap between beta=1 and a weaker density statement.

## The inversion computation, done: real sample cost for the rarest residues

Worked out the general inversion (not just the l=2 special case above) from `literature/notes/L-003.md`'s
reindexing (`g_i := b_{l-1}-b_i`, `b_i := u_{l-i}+...+u_l`, using `u` for Tao's original i.i.d.
Geom(2) draws). For a Wirsching tuple with smallest exponent `a_{l-1}=0` (a genuine `l`-step
sample, per the finding above), inverting gives `u_{l-i} = a_{i-1}-a_i` for `i=1,...,l-1`, and
these telescope to `u_1+...+u_{l-1} = a_0`. The final draw `u_l` is a genuinely **free
parameter** (it cancels out of every `g_i`/`a_i` relation — a subtle point verified algebraically,
not assumed): any `u_l>=1` gives the identical Wirsching tuple. So the minimum total cost over
all length-`l` Tao samples realizing a given genuine tuple is `N_min = a_0 + 1` (achieved at
`u_l=1`), not `a_0` itself.

**Verified this formula independently against the already-established l=2 case** before trusting
it at larger l: for l=2, the general derivation collapses to `u_1=a_0` directly (no telescoping
step, since the loop range is empty for l=2), giving `N_min=a_0+1` by the same direct argument —
consistent with, not just re-deriving, the l=2 result above.

**Applied to concrete min-count residues** (the rarest-hit residues at `j=j*(l)`, per
`notes/H-003.md`'s data), for `l=5..11`: found the covering tuple for each rarest residue, checked
it has `a_{l-1}=0` (all of them did, at every l checked, l=9's five-way-tied argmin residues
included), and computed `N_min=a_0+1` vs. the beta=1 threshold `l*log_2(3)`:

```
 l   j*  a_0(min,genuine)  N_min=a_0+1  l*log2(3)   gap
 5    9         8              9          7.925    1.075
 6   10        12             13          9.510    3.490
 7   11        14             15         11.095    3.905
 8   12        14             15         12.680    2.320
 9   13        16             17         14.265    2.735
10   15        19             20         15.850    4.150
11   16        21             22         17.435    4.565
12   17        19             20         19.020    0.980
```

(Full enumeration, `l+j` choose `l` tuples per row for l<=11, cross-checked `l=9`'s five tied
argmin residues, `{1048,7051,13927,17059,17815} mod 19683`, all give the same `a_0` structure.
l=12 computed 2026-07-28 via a DP method described below, argmin residue 193922 mod 531441,
witness tuple `(0,1,3,4,5,7,8,9,10,11,17,19)` found by a direct (non-DP) targeted scan, confirming
`a_0=19`, `a_11=0` (genuine sample) directly, not just via the DP's count alone.)

**Extension attempt beyond l=11, and why it stopped at l=12 (2026-07-28)**: implemented a DP
method to compute the full per-residue tuple-count histogram in one pass (mirroring
`jstar-fast`'s own rotation-DP structure in `experiments/E-001-jstar-fast/src/main.rs`, but
tracking integer counts per residue instead of a bitset), which is dramatically faster than brute
combinatorial enumeration once validated (l=11: 0.13s vs. 37.7s for the same result). **Verified
this DP method exactly reproduces the brute-force histogram for every l=1..11 before trusting it
for new values** (Rule 11), not assumed correct by construction alone.

Pushing further than l=12 was not attempted: **the primary l=24 `jstar-fast` computation
(H-001, `experiments/E-001-jstar-fast/logs/l24.pid`) was using essentially all of this machine's
62GB RAM at the time** (`free -h` showed ~224MB available), a state deliberately checked before
each new l rather than assumed. l=13's DP needs roughly 89MB for its own arrays alone, more than
half of what was free; l=14+ needs proportionally more. Given the primary computation is a
multi-day run already ~11h in and this project already lost ~2h of an earlier attempt to a
memory-pressure kill (see `notes/H-001.md`, the systemd-oomd incident), risking that run to
extend a secondary side-investigation was judged the wrong tradeoff, not a case where Rule 9b's
"use resources freely" latitude should override protecting an active, expensive, hard-to-restart
computation. **This is a resource-contention stopping point, not a computational-hardness one**:
the DP method itself would comfortably reach l=16-17 (est. 5-19GB) if run when this machine's RAM
is not otherwise committed, and l up to ~20 with the same swap-based approach `jstar-fast` itself
uses for l=22+, should the researcher want this extended further later (e.g. once l=24 finishes).

**What this shows, precisely**: for every rarest residue checked (now l=5..12, 8 points), the
naive one-sample lower bound `P(Syrac=b) >= 2^{-N_min}` falls short of `3^{-l}` by a real,
positive margin in every case, but the margin is not monotonic: `0.98` to `4.6` bits, and the
newest, largest point (l=12) has the *smallest* gap of the whole set, immediately after l=11's
gap of 4.565 was the largest. This is NOT itself a refutation of beta=1 (Tao's own conjecture
allows a `3^{-l+o(l)}` slack term, and `c_2=2/63` — Tao's own worked example — is already smaller
than `3^{-2}` at that small n, so some slack at finite l is expected and not alarming by itself).
**What would matter, and is still NOT resolved**: whether this gap grows *unboundedly* with `l`
(which would threaten the `o(l)` claim) or stays bounded/sublinear (consistent with beta=1). 8
data points (l=5..12), with the newest point swinging from the prior max to well below the prior
min, is if anything *less* suggestive of a clean trend than the 7-point set was, not more —
honestly, this looks like genuine, structure-dependent noise from residue to residue at this
range of l, not a signal resolvable by a few more brute-force points even if memory allowed it
right now. Resolving bounded-vs-growing likely needs either substantially more l (l>=20, feasible
with the validated DP method once RAM is free, or an analytic argument for how the argmin
residue's `a_0` scales, tied to the same open question E-002 already carries for `j*(l)` overall,
not a new independent question.

**One nuance worth flagging honestly**: "rarest at `j=j*(l)`" (this project's min-count
statistic) is not the same as "hardest residue to cover" — a residue's unique tuple at `j*(l)`
can have `a_0` well below the `(l-1)+j*(l)` ceiling (confirmed for several of the l=9 residues
above, e.g. `a_0=16` against a ceiling of 21), meaning that residue was actually already covered
at a smaller `j` and just happens to have exactly one hitting tuple by the time `j` reaches
`j*(l)`. This doesn't invalidate the `N_min` computation (it's still the true minimum cost for
that specific residue, independent of when it happened to get covered), but it means this
specific min-count statistic is not automatically identifying the single hardest-to-realize
residue overall — a real distinction, not pursued further here.

## Connection to H-003's non-uniformity finding: a concrete, testable next step

`notes/H-003.md`'s fourth/fifth dated sections found, empirically, that at `j=j*(l)` the
distribution of tuple-counts per residue is extremely non-uniform: the rarest residue is hit by
as few as 1 tuple, the most common by hundreds (l=11: min=4, max=401). This is directly relevant
here: **the rarest residue's `c_l` lower bound is determined entirely by that one tuple's actual
Tao-cost `N`**, not by `a_0` or by the tuple count. If that single tuple has an unusually large
`N` relative to `l*log_2(3)`, `c_l` could be smaller than beta=1 predicts even though the residue
is technically "covered" in Wirsching's sense.

**Concrete next step, not yet done, flagged rather than attempted here** (this task's scope was
to narrow the gap, not close it): for a residue with `min_count=1` at `j=j*(l)`, invert the
Wirsching tuple back to its actual underlying `(u_1,...,u_l)` sample (via the general back-tracing
correspondence H-004 established, not the naive `l=2`-only reindexing above, now known to be
incomplete) and compute its real cost `N`. Compare `N` against `l*log_2(3)`. If `N` tracks
tightly, the entropy-count bridge likely holds with a more careful derivation. If `N` is
significantly larger for the rarest residues, that is a genuine, specific obstruction to beta=1
as currently stated, not just an unproven technicality.

## Confidence and what needs a second opinion

Confident in the concrete finding above (three of six `R_{1,2}` tuples don't correspond to any
2-step Geom(2) sample under the direct reindexing) — verified by direct recomputation. Not
confident about how large the resulting gap actually is in practice. Whether to invest in the
"invert tuples to real Geom(2) cost" computation above is a real research-direction decision, not
a mechanical next step, and should get an independent view (different-vendor model or the
researcher) before committing effort — the same standing note as every H-003 round tonight.

## Status

Left `open-unexplored`. The entropy-count bridge is not confirmed and not refuted: a real,
demonstrated structural gap now has a precise description (budget-vs-cost mismatch, concretely
shown on the l=2 case) and a specific, bounded next computation (invert min-count-1 tuples to
real sample cost) rather than the vague "one non-mechanical step" framing this hypothesis
started with. Per Rule 8, a critique round is required before any status change either way.
